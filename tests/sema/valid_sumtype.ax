type Option = Some(i32) | None

fn find(target: i32) -> Option:
    return None

fn main():
    let result = find(2)
    match result:
        Some(v):
            let x = v
        None:
            let x = 0
