fn sub(a: i32, b: i32) -> i32:
    return a - b

fn add(a: i32, b: i32) -> i32:
    return a + b

fn recursive_sum(n: i32, acc: i32) -> i32:
    if n == 0:
        return acc
    return recursive_sum(sub(n, 1), add(acc, n))

fn main():
    let res = recursive_sum(2000, 0)
    println(res)
