type Option = Some(i32) | None

fn get_value(opt: Option) -> i32:
    match opt:
        Some(v):
            return v
        None:
            return 0

fn main():
    let opt = Some(42)
    let res = get_value(opt)
