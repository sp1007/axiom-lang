type Result = Ok(i32) | Err(string)

fn divide(a: i32, b: i32) -> Result:
    return Ok(a)

fn main():
    let r = divide(10, 2)
    match r:
        Ok(v):
            let x = v
        Err(msg):
            let y = msg
