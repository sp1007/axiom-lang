// HARNESS: SKIP — consumes geolib's struct through the .lib interface (no source recompile).
import geolib

fn main() -> i32:
    let p = geolib.origin()
    let q = geolib.origin()
    return geolib.add_pt(p, q)
