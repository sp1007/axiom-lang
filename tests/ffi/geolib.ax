// HARNESS: SKIP — library exporting a struct + a fn returning it (RFC 0011 P4 inc4b).
library geolib

pub struct Point:
    x: i32
    y: i32

pub fn origin() -> Point:
    return Point(x: 3, y: 4)

pub fn add_pt(a: Point, b: Point) -> i32:
    return a.x + a.y + b.x + b.y
