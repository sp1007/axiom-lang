fn main() -> i64:
    let v = Vec[i64].new()
    v.push(5)
    v.push(3)
    v.push(42)
    v.push(60)
    // first element > 10 is at index 2 → 2, then read that element
    let p = v.position(|x: i64| -> bool x > 10)
    let idx = p.unwrap()
    let got = v.get(idx)
    return got.unwrap()
