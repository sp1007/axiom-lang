// RFC 0006 §4 (E3031): a struct-field INITIALIZER against the field's declared integer
// type. `P(a: 3.5)` had no diagnostic and stored float bits into an integer slot.
struct P:
    a: i32

fn main() -> i64:
    let p = P(a: 3.5)
    return p.a as i64
