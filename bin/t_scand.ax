// Oracle (exit 0): short-circuit `false and side()` must NOT evaluate the RHS.
// Reachability correctness, adjacent to the defer-in-control-flow class.
mut c: i64 = 0
fn side() -> bool:
    c = c + 1
    return true
fn main() -> i64:
    if false and side():
        c = c + 100
    return c
