// Oracle (exit=42): RFC 0017 P2 — byte indexing on a `str` module-level global.
// `s[i]` is BYTE-level; the global's inline {ptr,len} is loaded and the ptr half indexed.
mut g: str = "*BC"
fn main() -> i32:
    return (g[0] - 0) as i32   // '*' = 42
