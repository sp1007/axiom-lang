fn maxof[T](a: T, b: T) -> T:
    if a > b:
        return a
    return b
fn id[T](a: T) -> T:
    return a
fn main() -> i64:
    let m = maxof(3.5, 2.5)         // 3.5
    let r = id(2.5)                 // 2.5
    return ((m + r) * 7.0) as i64   // 6.0*7 = 42
