import std.result

fn ok_i64(x: i64) -> Result[i64, str]:
    if x > 0:
        return Ok(x)
    return Err("neg")

pub fn main() -> i32:
    let r1 = ok_i64(42)
    let v = r1.unwrap()
    if v != 42:
        return 6
    return 0
