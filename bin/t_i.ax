import std.result

fn ok_i64(x: i64) -> Result[i64, str]:
    if x > 0:
        return Ok(x)
    return Err("neg")

pub fn main() -> i32:
    let r1 = ok_i64(42)
    let raw = (&r1) as ptr[u64]
    let p = raw[0]
    let v = (p as ptr[i64]).*
    return v as i32
