// CONTROL for bin/t_p6methodname.ax — identical in every respect except that the method
// name collides with NOTHING the bundled stdlib interns. This row passed before the P6
// fix and must keep passing after it: it is what proves the oracle next to it measures
// the defect rather than the harness or the println path.
// MUST print exactly: Kết quả P6 control: 46
struct Logger:
    factor: i64
    fn zorkzork(self: Logger, n: i64) -> i64:
        return n * self.factor

fn main() -> i64:
    let a: Logger = Logger(factor: 2)
    let x = a.zorkzork(8)
    println("Kết quả P6 control: ", x + 30)
    return 0
