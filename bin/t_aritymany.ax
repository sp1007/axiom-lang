// Oracle (reject): a free-function call with too MANY args was silently accepted
// (extras dropped) -> now rejected.
fn add2(a: i32, b: i32) -> i32:
    return a + b
fn main() -> i32:
    return add2(5, 6, 7, 8)
