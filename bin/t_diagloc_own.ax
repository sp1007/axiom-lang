// Diagnostics stage 2b fixture: an OWNERSHIP error (error[E4002], assignment to a binding
// declared without `mut`) must now carry a source location too. That pass was the THIRD
// copy of the location-rendering idea and the one that had drifted furthest -- it printed
// a bare byte offset into the concatenated bundle and no snippet at all. It now renders
// through the same print_diag_location as the parser and the typechecker.
import std.string

fn main() -> i32:
    let x: i64 = 1
    x = 2
    return 0
