// RFC 0006 §4 (E3032): assignment to a struct FIELD. A field's type is always declared, so
// the annotation gate is satisfied by construction (same reasoning as t_f2ifieldasg).
struct S:
    v: f32

fn main() -> i64:
    let d: f64 = 3.5
    mut s = S(v: 1.0)
    s.v = d
    return s.v as i64
