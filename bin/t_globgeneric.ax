// Oracle (exit=42): RFC 0017 P2 — a struct global passed by-value to a generic function.
struct V:
    a: i64
    b: i64
fn ident[T](x: T) -> T:
    return x
mut g: V = V(a: 19, b: 23)
fn main() -> i32:
    let r = ident(g)
    return (r.a + r.b) as i32   // 42
