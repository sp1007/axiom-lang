// Oracle (reject): n.trim() — `trim` is a str-only free fn, not applicable to an
// i64 receiver. Must REJECT cleanly (receiver-specific UFCS test), not segfault.
import std.string
fn main() -> i64:
    let n = 5 as i64
    return n.trim().len
