// Diagnostics stage 2b fixture: a TYPECHECK error (RFC 0006 §6.1, error[E3030]) must now
// carry a `--> file.ax:LINE` header and a gutter that shows the REAL line number, while
// keeping the caret span that print_annotated_expr_snippet computes from the AST.
// `import std...` lines are present on purpose: they are what stage 2a made length-
// preserving, so line 11 below must be reported as line 11.
import std.string
import std.io

fn main() -> i32:
    let a: i64 = 1
    let x: u8 = 300
    return 0
