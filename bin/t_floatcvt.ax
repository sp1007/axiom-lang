fn take64(x: f64) -> i64:
    return (x + 0.5) as i64
fn take32(x: f32) -> i64:
    return (x + 0.5) as i64
fn main() -> i64:
    let a: f32 = 3.0
    let b: f64 = 7.0
    let w = take64(a as f64)
    let n = take32(b as f32)
    return w + n
