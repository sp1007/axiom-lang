type Shape = Circle(i64) | Empty
fn area(s: Shape) -> i64:
    match s:
        Circle(r): return r * r
        Empty: return 0
fn main() -> i64:
    let big = true
    let s = if big: Circle(7) else: Empty
    return area(s) - 7
