// array-literal RHS on ASSIGNMENT to an [i64;N] lvalue — does it coerce element width?
fn main() -> i64:
    mut a: [i64; 3] = [1, 1, 1]
    a = [10, 15, 17]
    return a[0] + a[1] + a[2]     // want 42
