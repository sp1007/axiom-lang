// REALISTIC naming: `buf_len` declared before `len` on the same struct.
struct Buf:
    n: i64
    fn buf_len(self: Buf) -> i64:
        return 7
    fn len(self: Buf) -> i64:
        return 42
fn main() -> i64:
    let b = Buf(n: 0)
    return b.len()
