// RFC 0006 §4 (E3032): `return` against a DECLARED `f32` return type. The declared return
// type is an explicit annotation exactly like a `let`'s.
fn g() -> f32:
    let d: f64 = 3.5
    return d

fn main() -> i64:
    return g() as i64
