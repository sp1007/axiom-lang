import std.string

pub fn main() -> i32:
    let arr = @alloc(3 * 16) as ptr[str]
    arr[0] = "hello"
    arr[1] = "world!!"
    arr[2] = "ax"
    let x = arr[1 as i64]
    if x == "world!!":
        return std.string.len(x) as i32
    return 99
