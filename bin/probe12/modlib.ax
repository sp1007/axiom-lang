// An imported module carrying a deliberate type error, to prove that a diagnostic
// raised while checking an IMPORTED module names that module's own file and line.
pub fn helper() -> i64:
    let bad: u8 = 300
    return bad as i64
