// Oracle (reject): member access on a primitive scalar was silently lowered to a
// garbage field load -> SIGSEGV. Now rejected (primitives have no members).
fn main() -> i32:
    let n = 5
    return n.foo
