struct Box2:
    v: i64
struct Logger:
    factor: i64
    fn actor(self: Logger, n: i64) -> i64:
        return n * self.factor
fn main() -> i64:
    let a: Logger = Logger(factor: 2)
    let b = Box2(v: a.actor(21))
    return b.v
