struct Logger:
    factor: i64
    fn run(self: Logger, n: i64) -> i64:
        return n * self.factor
fn main() -> i64:
    let a: Logger = Logger(factor: 2)
    let x: i64 = a.run(8)
    return x + 30
