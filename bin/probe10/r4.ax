struct Logger:
    factor: i64
    fn actor(self: Logger, n: i64) -> i64:
        return n * self.factor
fn main() -> i64:
    let a: Logger = Logger(factor: 2)
    let x = a.actor(21)
    let arr = [0 as i64, 0 as i64]
    mut b := arr
    b[0] = x
    return b[0]
