struct Logger:
    factor: i64
    fn actor(self: Logger, n: i64) -> bool:
        return n > 0
fn main() -> i64:
    let a: Logger = Logger(factor: 2)
    let x = a.actor(21)
    if x:
        return 42
    return 7
