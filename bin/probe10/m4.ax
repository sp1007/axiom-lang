struct Logger:
    factor: i64
    fn zork(self: Logger, n: i64) -> i64:
        return n * self.factor

fn main() -> i64:
    let a: Logger = Logger(factor: 2)
    let b: Logger = Logger(factor: 3)
    return a.zork(8) + b.zork(10)
