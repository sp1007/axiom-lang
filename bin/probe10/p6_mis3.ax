struct One:
    v: i64
struct Logger:
    factor: i64
    fn AxSupervisorTable(self: Logger, n: i64) -> One:
        return One(v: n * self.factor)
fn main() -> i64:
    let a: Logger = Logger(factor: 2)
    let p = a.AxSupervisorTable(21)
    return p.v
