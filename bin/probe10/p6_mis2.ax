struct Logger:
    factor: i64
    fn actor(self: Logger, n: i64) -> Option[i64]:
        return Some(n * self.factor)
fn main() -> i64:
    let a: Logger = Logger(factor: 2)
    let x = a.actor(21)
    return x.unwrap()
