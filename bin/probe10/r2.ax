struct Holder:
    v: i64
struct Logger:
    factor: i64
    fn actor(self: Logger, n: i64) -> i64:
        return n * self.factor
fn main() -> i64:
    let a: Logger = Logger(factor: 2)
    mut h := Holder(v: 0 as i64)
    h.v = a.actor(21)
    return h.v
