// D: `<` looks up `lt`; only `total_lt` exists.
struct Num:
    v: i64
    fn total_lt(self, o: Num) -> bool:
        return true
fn main() -> i64:
    let a = Num(v: 10)
    let b = Num(v: 2)
    if a < b:
        return 7
    return 42
