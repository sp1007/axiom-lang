// STATIC call form: the receiver is spelled as the first argument.
struct S:
    v: u8

fn setv(mut self: ptr[S], x: u8):
    self.v = x

fn main() -> i64:
    mut s := S(v: 0 as u8)
    S.setv(&s, 300)
    return s.v as i64
