// B (overreach): same, DECLARATION ORDER REVERSED (the original defect was
// order-sensitive).
struct Buf:
    n: i64
    fn buf_len(self) -> i64:
        return 2
    fn len(self) -> i64:
        return 40
fn main() -> i64:
    let b = Buf(n: 0)
    return b.len() + b.buf_len()
