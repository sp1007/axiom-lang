// G: free-fn overloads on the same receiver type, differing in arity, called in
// UFCS method form.
struct S:
    n: i64
fn f(s: S) -> i64:
    return 1
fn f(s: S, x: i64) -> i64:
    return x
fn main() -> i64:
    let s = S(n: 0)
    return s.f(41) + s.f()
