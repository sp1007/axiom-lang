// F: conformance at a struct-FIELD init site.
interface I:
    fn r(self: Self) -> i64

struct S:
    n: i64
    fn a_r(self: S, x: i64) -> i64:
        return x

struct Holder:
    h: I

fn main() -> i64:
    let hold = Holder(h: S(n: 1))
    if hold.h.r() == 42:
        return 42
    return 7
