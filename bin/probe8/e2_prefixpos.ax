// E: control -- "eq" occurs at index 0 of `eq_fast` but is followed by a single
// `_`, not end-of-name or `__`, so the window must NOT match. Expect a REJECT.
struct Num:
    v: i64
    fn eq_fast(self, o: Num) -> bool:
        return true
fn main() -> i64:
    let a = Num(v: 1)
    let b = Num(v: 2)
    if a == b:
        return 7
    return 42
