// INLINE-declared method (inside the struct body) -- does it share the path?
struct S:
    v: u8

    fn setv(mut self: ptr[S], x: u8):
        self.v = x

fn main() -> i64:
    mut s := S(v: 0 as u8)
    s.setv(300)
    return s.v as i64
