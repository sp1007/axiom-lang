// B (overreach): two structs each defining the SAME method name.
struct A:
    v: i64
    fn get(self) -> i64:
        return 20
struct B:
    v: i64
    fn get(self) -> i64:
        return 22
fn main() -> i64:
    let a = A(v: 0)
    let b = B(v: 0)
    return a.get() + b.get()
