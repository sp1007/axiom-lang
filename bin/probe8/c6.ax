// INTERFACE DISPATCH: the interface decl spells the parameter type.
interface Sink:
    fn take(self: ptr[Sink], x: u8)

struct S:
    v: u8

fn take(mut self: ptr[S], x: u8):
    self.v = x

fn main() -> i64:
    mut s := S(v: 0 as u8)
    let i: Sink = s
    i.take(300)
    return 42
