// Control: same method but the return type written CONCRETELY (f64), not `T`.
struct Box[T]:
    v: T
    fn getf(self) -> f64:
        return 2.5
fn main() -> i64:
    let c = Box[f64](v: 2.5)
    let n = c.getf() as i64
    return 40 + n
