// G: the SAME overload pair called in ordinary free-call form (not UFCS).
struct S:
    n: i64
fn f(s: S) -> i64:
    return 1
fn f(s: S, x: i64) -> i64:
    return x
fn main() -> i64:
    let s = S(n: 0)
    return f(s, 41) + f(s)
