import std.collections
struct Buf:
    n: i64
fn buf_len(b: Buf) -> i64:
    return 100
fn main() -> i64:
    let s = "abc"
    let b = Buf(n: 0)
    let total = std.string.len(s) + (buf_len(b) / 100)
    if total != 4:
        return 7 + total
    return 42
