// Control for a3: same shape, method named `cleanup` (no `_drop` window).
mut sink: i64 = 0
struct Res:
    id: i64
    fn cleanup(self):
        sink = sink + self.id
fn main() -> i64:
    let a = Res(id: 42)
    let b = a
    return b.id
