// A: UFCS/free-fn selection. `s_len` contains "len" at a `_`-bounded position
// and its first param is S, but its NAME is not `len`. No exact `len` exists.
struct S:
    n: i64
fn s_len(x: S, k: i64) -> i64:
    return k
fn main() -> i64:
    let s = S(n: 5)
    return s.len()
