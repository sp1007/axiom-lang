import std.result

fn ok_str(flag: i64) -> Result[str, str]:
    if flag > 0:
        return Ok("payload-ok")
    return Err("payload-err")

pub fn main() -> i32:
    let s1 = ok_str(1)
    let ok_payload = s1.unwrap()
    return ok_payload.len as i32
