fn opaque(x: u32) -> u32:
    return x
fn main() -> i64:
    let a: u32 = opaque(3000000000)
    let b: u32 = opaque(5)
    mut r := 0
    if a > b:
        r = r + 10
    if b < a:
        r = r + 32
    return r
