// Oracle (reject): an Option/Result/sum value ASSIGNED to a plain-typed lvalue without
// unwrapping must be REJECTED. Was accept-then-miscompile: the 8-byte box stored raw into
// the scalar -> garbage (`x = o` returned 8). Assignment sibling of t_optletreject /
// t_optargreject. See knowledge/bug-option-as-let-binding-not-rejected.md. Reject-path.
fn main() -> i64:
    mut x: i64 = 3
    let o: Option[i64] = Some(55)
    x = o
    return x
