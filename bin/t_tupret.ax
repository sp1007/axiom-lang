// tuple returned from fn, destructured at call site + arithmetic across fn boundary
fn mkpair(a: i64, b: i64) -> (i64, i64):
    return (a * 2, b + 100)

fn main() -> i64:
    let (x, y) = mkpair(5, 7)
    // x=10, y=107 -> 117
    return x + y
