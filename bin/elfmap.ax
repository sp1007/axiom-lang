import std.collections
pub fn main() -> i32:
    mut m: HashMap[i64, i64] = HashMap[i64, i64]()
    m.insert(1 as i64, 40 as i64)
    m.insert(2 as i64, 2 as i64)
    return (m.get(1).unwrap() + m.get(2).unwrap()) as i32
