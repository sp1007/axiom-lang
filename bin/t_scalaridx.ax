// Oracle: indexing a scalar variable (`x[0]` where x is i64) must be REJECTED
// (was OP_INDEX on the scalar -> segfault). See [[bug-malformed-input-robustness-cluster]].
fn main() -> i64:
    let x = 5
    return x[0]
