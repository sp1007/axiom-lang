fn main() -> i64:
    let v = Vec[i64].new()
    v.push(10)
    v.push(20)
    v.push(12)
    // find returns Option; chain: find >15 -> Some(20); map to *2 -> Some(40); unwrap+2
    let r = v.find(|n: i64| -> bool n > 15)
    let m = r.map(|n: i64| -> i64 n * 2)
    return m.unwrap() + 2
