fn main() -> i64:
    let v = Vec[i64].new()
    v.push(5)
    v.push(42)
    v.push(3)
    v.push(60)
    // two elements > 10 → count == 2, scaled to 42
    let n = v.count(|x: i64| -> bool x > 10)
    if n == 2:
        return 42
    return 0
