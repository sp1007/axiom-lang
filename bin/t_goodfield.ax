// Oracle (exit 5): a real field access still compiles and runs (guards against a
// false positive rejecting valid member access).
struct Cell:
    x: i32
fn main() -> i32:
    let c = Cell(x: 5)
    return c.x
