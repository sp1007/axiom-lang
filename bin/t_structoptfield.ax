// Struct with an Option field: construct, then match the field.
struct Node:
    val: i64
    next: Option[i64]
fn main() -> i64:
    let n: Node = Node(val: 40, next: Some(2))
    match n.next:
        Some(x):
            return n.val + x
        None:
            return n.val
