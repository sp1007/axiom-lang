// Oracle (reject): a free-function call with too FEW args was silently accepted
// (missing args read uninitialized) -> now rejected.
fn add3(a: i32, b: i32, c: i32) -> i32:
    return a + b + c
fn main() -> i32:
    return add3(5)
