// RFC 0006 §4 (E3031): assignment to a struct FIELD. A field's type is always declared,
// so the annotation gate is satisfied by construction (same reasoning as E3030's
// t_intrangefieldasg row).
struct S:
    v: i32

fn main() -> i64:
    mut s = S(v: 1)
    s.v = 2.5
    return s.v as i64
