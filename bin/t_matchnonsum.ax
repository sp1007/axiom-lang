// Oracle (reject mode): matching a NON-sum value (plain i64) against variant patterns
// (Some/None) previously accepted then SEGFAULTED (payload never binds). Must REJECT.
// [[bug-malformed-input-robustness-cluster]] (m4)
fn main() -> i64:
    let x = 5
    match x:
        Some(v):
            return v
        None:
            return 0
