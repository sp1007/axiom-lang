fn sumto(n: i64, acc: i64) -> i64:
    if n == 0:
        return acc
    return sumto(n - 1, acc + n)

fn main() -> i64:
    mut t: i64 = 0
    mut i: i64 = 0
    while i < 2000:
        t = t + sumto(10000, 0)
        i = i + 1
    return t % 256
