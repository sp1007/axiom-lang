import std.string
extern "C" fn putchar(c: i32) -> i32

pub fn main() -> i32:
    let a = "a1"
    let b = "as"
    let r = (a == b)
    if r:
        putchar('T' as i32)
    else:
        putchar('F' as i32)
    putchar(10 as i32)
    return 0
