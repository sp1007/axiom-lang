// Oracle (exit=80): RFC 0017 P2 — a module-level fixed-ARRAY global; element
// read-modify-write across a function.
mut arr: [i64; 3] = [10, 20, 30]
fn bump():
    arr[1] = arr[1] * 2      // 40
fn main() -> i32:
    bump()
    return (arr[0] + arr[1] + arr[2]) as i32   // 10+40+30 = 80
