// Oracle (exit=7): an unsigned u64 literal above 2^63 must compare as a large
// positive value (unsigned compare), not as a negative i64.
fn main() -> i32:
    let big: u64 = 18000000000000000000
    let small: u64 = 100
    if big > small:
        return 7
    return 1
