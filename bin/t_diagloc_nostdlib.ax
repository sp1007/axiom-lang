// Diagnostics stage 2b fixture for the path where the driver bundles NOTHING
// (`--no-stdlib`, and likewise `dump-air`/`emit-c`/`fmt`): the source map then has a single
// region covering the whole buffer, so the offset IS an offset into the user's own file.
// Deliberately imports nothing, so `--no-stdlib` produces this one diagnostic and no
// unrelated resolver noise. The mistake is on line 7.
fn main() -> i32:
    let p = Point{a: 1}
    return 0
