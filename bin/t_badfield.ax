// Oracle (reject): accessing a field that does not exist on the struct (and is
// not a method/UFCS name) was silently lowered to field offset 0 -> read the
// wrong field. Now rejected.
struct Cell:
    x: i32
fn main() -> i32:
    let c = Cell(x: 5)
    return c.zzz
