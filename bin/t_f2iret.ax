// RFC 0006 §4 (E3031): `return` against a DECLARED integer return type. The declared
// return type is an explicit annotation exactly like a `let`'s.
fn g() -> i32:
    return 3.0

fn main() -> i64:
    return g() as i64
