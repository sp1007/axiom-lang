; Generated by AXIOM Compiler (NASM format)
bits 64
section .text


global main

main:
    push rbp
    mov rbp, rsp
    push rbx
    sub rsp, 8
.L_b_0:
    mov rbx, 42
    mov rax, rbx
    add rsp, 8
    pop rbx
    pop rbp
    ret

