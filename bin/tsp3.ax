// 24-byte struct (3 i64) by-value param+return
struct T:
    a: i64
    b: i64
    c: i64
fn mk(x: i64) -> T:
    return T(a: x, b: x*2, c: x*3)
fn s(t: T) -> i64:
    return t.a + t.b + t.c
pub fn main() -> i32:
    let q = mk(2 as i64)
    return s(q) as i32   // 2+4+6 = 12
