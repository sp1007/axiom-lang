// RFC 0006 §4 (E3032): a struct-field INITIALIZER against the field's declared `f32` type.
// `P(a: d)` had no diagnostic and stored f64 bits into a 4-byte float slot.
struct P:
    a: f32

fn main() -> i64:
    let d: f64 = 3.5
    let p = P(a: d)
    return p.a as i64
