// Oracle (reject mode): a struct with a field of its OWN type BY VALUE has infinite
// size and previously CRASHED the compiler during codegen. Must REJECT cleanly with a
// diagnostic (use ptr[...] for recursion). [[bug-malformed-input-robustness-cluster]]
struct S:
    x: S
    n: i64
fn main() -> i64:
    return 0
