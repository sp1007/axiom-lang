// Oracle: an aggregate/Option condition (`if o` where o: Option) must be REJECTED
// (forgot .is_some()/match). Scalar/bool conditions stay valid.
fn main() -> i64:
    let o: Option[i64] = Some(5)
    if o:
        return 1
    return 0
