// Oracle (exit=50): RFC 0017 P2 — array-global element write through a function that takes the index.
mut g: [i64; 5] = [0, 0, 0, 0, 0]
fn setat(i: i64, v: i64):
    g[i] = v
fn main() -> i32:
    for i in 0..5:
        setat(i, i * 10)
    return (g[1] + g[4]) as i32   // 10 + 40 = 50
