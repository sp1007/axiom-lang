import std.string
pub fn main() -> i32:
    let a: i64 = std.string.to_i64("40").unwrap()
    let b: i64 = std.string.to_i64("-8").unwrap()
    let f: f64 = std.string.to_f64("2.5").unwrap()
    return (a + b + (f as i64) * 4 as i64) as i32
