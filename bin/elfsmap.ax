import std.collections
pub fn main() -> i32:
    mut m: HashMap[str, i64] = HashMap[str, i64]()
    m.insert("alpha", 40 as i64)
    m.insert("beta", 2 as i64)
    return (m.get("alpha").unwrap() + m.get("beta").unwrap()) as i32
