fn pick[T](a: T, b: T) -> T:
    return a
fn main() -> i64:
    let q: f32 = 1.0
    let y = pick(2.5, q)      // T inferred f32 from b; a is a float literal
    return (y * 100.0) as i64 // expect 250
