struct W[T]:
    v: T
    fn tag(self: W[T]) -> str:
        return "wrapped"
    fn get(self: W[T]) -> T:
        return self.v
fn main() -> i64:
    let w = W[i64](v: 41)
    let t = w.tag()
    let g = w.get()
    if t.len == 7 and g == 41:
        return 42
    return t.len
