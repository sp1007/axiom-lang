fn pick[T](a: T, b: T) -> T:
    return a
fn main() -> i64:
    let x = pick[f64](3.5, 1.0)
    let y = pick[f32](2.5, 1.0)
    mut r: i64 = 0
    if x > 3.49 and x < 3.51:
        r = r + 1
    if y > 2.49 and y < 2.51:
        r = r + 2
    return r   // expect 3
