fn pick[T](a: T, b: T) -> T:
    return a
fn main() -> i64:
    let f = (pick[f32](2.5, 1.0) * 100.0) as i64
    let b = pick[u8](300, 1) as i64
    if f != 250:
        return 1
    if b != 44:
        return 2
    return 42
