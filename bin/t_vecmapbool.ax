fn main() -> i64:
    let v = Vec[i64].new()
    v.push(5)
    v.push(42)
    v.push(15)
    let flags = v.map(|x: i64| -> bool x > 10)
    mut c := 0
    mut i := 0
    while i < 3:
        if flags[i]:
            c = c + 1
        i = i + 1
    return c
