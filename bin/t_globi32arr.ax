// Oracle (exit=130): RFC 0017 P2 — narrow-int [i32;N] array global (element stride) via for-in + index.
mut g: [i32; 4] = [10, 20, 30, 40]
fn main() -> i32:
    mut t: i32 = 0
    for x in g:
        t = t + x
    return (t + g[2]) as i32   // 100 + 30 = 130
