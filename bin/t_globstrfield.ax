// Oracle (exit=10): RFC 0017 P2 — struct global with a str field; read .len + scalar field.
struct Rec:
    name: str
    n: i64
mut g: Rec = Rec(name: "hello", n: 5)
fn main() -> i32:
    return (g.name.len + g.n) as i32   // 5 + 5 = 10
