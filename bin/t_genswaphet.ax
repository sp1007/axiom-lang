struct Two[A, B]:
    first: A
    second: B
fn swap[A, B](p: Two[A, B]) -> Two[B, A]:
    return Two[B, A](first: p.second, second: p.first)
fn main() -> i64:
    let p = Two[i64, f64](first: 40, second: 2.5)
    let q = swap(p)
    return (q.first as i64) + q.second
