// Oracle (exit=42): RFC 0017 P2 — reading a struct global as a WHOLE aggregate
// value (passing it by-address to a function).
struct S:
    a: i64
    b: i64
mut g: S = S(a: 20, b: 22)
fn sum(s: S) -> i64:
    return s.a + s.b
fn main() -> i32:
    return sum(g) as i32   // 42
