// H5: int literal to an f64 param, with a float literal call elsewhere.
fn probe(a: f64) -> i64:
    if a > 2.99 and a < 3.01:
        return 42
    if a == 0.0:
        return 1
    return 2
fn main() -> i64:
    let w = probe(3.0)
    if w != 42:
        return 3
    return probe(3)
