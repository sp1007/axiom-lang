// H1: minimal -- the ONLY float local is `let a: f64 = <int literal>`.
fn main() -> i64:
    let a: f64 = 3
    if a > 2.99 and a < 3.01:
        return 42
    if a == 0.0:
        return 1
    return 2
