// H2: same, but a real float-literal binding exists FIRST.
fn main() -> i64:
    let ctl: f64 = 3.0
    let a: f64 = 3
    if a > 2.99 and a < 3.01:
        return 42
    if a == 0.0:
        return 1
    return 2
