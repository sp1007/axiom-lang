// G11: the OPPOSITE direction. RFC 0006/BUG#35: float -> int implicit must be an ERROR
// ("float → int: CẤM ngầm, phải `as`"). Does the compiler reject `let a: i64 = 3.0`?
fn main() -> i64:
    let a: i64 = 3.0
    if a == 3:
        return 42
    if a == 0:
        return 1
    return 2
