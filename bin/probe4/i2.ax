// I2: the f32 TWIN of i1.ax, and it PASSES (42) — proof the asymmetry is a typecheck
// hint that was "scoped to F32 only" (typecheck.ax:5208), not a codegen limit.
struct H:
    b: f32
fn main() -> i64:
    let h = H(b: 3)
    if h.b > 2.99 and h.b < 3.01:
        return 42
    return 1
