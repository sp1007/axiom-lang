// H4: int literal to an f64 PARAM, minimal.
fn probe(a: f64) -> i64:
    if a > 2.99 and a < 3.01:
        return 42
    if a == 0.0:
        return 1
    return 2
fn main() -> i64:
    return probe(3)
