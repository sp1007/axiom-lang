// RFC 0006 §4 (E3032): the same rule at a GLOBAL annotated binding. Globals are lowered
// through their own initializer path, so they get their own row -- a partial regression
// must name the position that broke.
let D: f64 = 3.5
let S: f32 = D

fn main() -> i64:
    return S as i64
