import std.io
import std.result

pub fn main() -> i32:
    let r = std.io.open("nonexistent_file_xyz_123.txt")
    if r.is_ok():
        return 1
    let msg = r.unwrap_err()
    if msg.len > 0:
        return 0
    return 2
