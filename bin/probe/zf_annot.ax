struct Sq:
    s: f64
    fn area(self: Sq) -> f64:
        return self.s * self.s
fn main() -> i64:
    let q = Sq(s: 3.0)
    let a: f64 = q.area()
    if a > 8.99 and a < 9.01:
        return 42
    return (a * 10.0) as i64
