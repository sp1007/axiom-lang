struct Sq:
    s: f32
    fn area(self: Sq) -> f32:
        return self.s * self.s
fn main() -> i64:
    let q = Sq(s: 3.0)
    let a = q.area()
    if a > 8.99 and a < 9.01:
        return 42
    return 7
