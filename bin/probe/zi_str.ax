interface Shape:
    fn label(self: Self) -> str

struct Sq:
    s: i64
    fn label(self: Sq) -> str:
        return "square"

fn useit(sh: Shape) -> i64:
    let n = sh.label()
    return n.len
fn main() -> i64:
    return useit(Sq(s: 3))
