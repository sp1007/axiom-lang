// tuple stored in struct field, read across a method
struct Box:
    p: (i64, i64)
    fn total(self) -> i64:
        return self.p.0 + self.p.1

fn main() -> i64:
    let b = Box(p: (40, 2))
    return b.total()
