// Oracle: a bare value-position reference to an UNDECLARED name must be REJECTED
// (was silently accepted -> raw name_id indexed a stray symbol -> garbage). See
// [[bug-undefined-name-accept]].
fn main() -> i64:
    return totally_undeclared_name_xyz
