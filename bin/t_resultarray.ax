// Regression: read a builtin Result element out of a fixed array and match it.
fn main() -> i64:
    let arr: [Result[i64, i64]; 2] = [Ok(40), Err(9)]
    match arr[0]:
        Ok(v):
            return v
        Err(e):
            return e
