// assign Some((tuple)) to an Option[(i64,i64)] lvalue — inner tuple coercion on assign
fn main() -> i64:
    mut o: Option[(i64, i64)] = None
    o = Some((30, 12))
    let p = o.unwrap()
    return p.0 + p.1     // want 42
