// RFC 0006 §6.1 (E3030): an unsigned type cannot represent a negative literal. The
// asymmetric signed minimum (`i8 = -128`) stays LEGAL -- t_intrangeok row 5.
fn main() -> i64:
    let x: u8 = -1
    return x as i64
