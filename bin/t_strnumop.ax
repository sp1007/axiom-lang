// Oracle: a binary op mixing a string and a numeric operand (`"hello" + 5`)
// must be REJECTED (was pointer+int garbage). str+str concat stays valid.
fn main() -> i64:
    let s = "hello"
    let n = 5
    return s + n
