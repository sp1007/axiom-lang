// RFC 0006 §4 (E3031): a NEGATED float literal. `-2.5` parses as UNARY(-) over
// FLOAT_LIT(2.5), so the reject must unwrap the unary exactly as E3030 does for `-128`,
// otherwise the most obvious float form slips through the check.
fn main() -> i64:
    let a: i64 = -2.5
    return a
