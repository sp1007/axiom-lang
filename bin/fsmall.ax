struct P2:
    x: i64
    y: i64
    fn add(self, o: P2) -> P2:
        return P2(x: self.x + o.x, y: self.y + o.y)
pub fn main() -> i32:
    let a = P2(x: 3, y: 4)
    let b = P2(x: 10, y: 20)
    let r = a.add(b)
    return (r.x + r.y) as i32
