// Oracle (exit=42): a no-initializer STRUCT global is a zeroed aggregate .data slot;
// fields are then written and read back.
struct S:
    a: i64
    b: i64
mut g: S
fn main() -> i32:
    g.a = 20
    g.b = 22
    return (g.a + g.b) as i32
