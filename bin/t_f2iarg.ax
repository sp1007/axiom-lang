// RFC 0006 §4 (E3031): a call argument against a DECLARED integer parameter. Every
// parameter carries a user-written type, so the float argument has an explicit integer
// target exactly like a `let` annotation.
fn takes(v: i32) -> i64:
    return v as i64

fn main() -> i64:
    return takes(4.5)
