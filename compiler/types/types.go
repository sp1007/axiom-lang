// Package types defines the AXIOM type system representation.
// It is dependency-free from the ast package. AST nodes reference
// types only by TypeID (uint32), never by pointer to TypeInfo.
package types
