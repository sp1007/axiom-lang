pub fn println(s: string):
    let x = 0

pub fn main() -> i32:
    let s = "Hello"
    let p = s as *mut u8
    if p[0] == 72 as u8: // 'H'
        println("String casting to *mut u8 works!")
    return 0
