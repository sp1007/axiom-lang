- [✅ FIXED 2026-07-24g `998F7199`, 546/546, A==B — f32 `==` float LITERAL nay đúng](bug-f32-compare-float-literal.md) — **probe-found**: `let c: f32 = 4.0; if c == 4.0` vào nhánh FALSE (`4.0f32 == 4.0` = false). f32 arith + f32==f32 OK; chỉ f32 so với float LITERAL sai (mọi thứ tự toán hạng, param lẫn local). Root: float literal default f64; nhánh **arithmetic** (op!=1) đã coerce float-lit về width toán hạng kia (RFC 0006 ~L4124), nhưng nhánh **comparison** (op==1 ~L4047) chỉ coerce INT literal → f32==f64 mixed-precision compare sai. Fix: mirror float-lit coercion vào comparison branch (cả 2 thứ tự). A==B inert. Oracle t_f32cmplit(42). ⭐ Bài học: rule coercion thêm ở 1 nhánh binop mà quên nhánh sibling = partial fix; áp cho MỌI nhánh (arith + comparison + compound-assign + match-arm).
- [🟢 SHIPPED 2026-07-24f `c957193` — RFC 0031 DFE **ON BY DEFAULT** (533→538/538), driver `DA7F8D2D`](../rfcs/0031-dead-function-elimination.md) — **ĐỌC ĐẦU TIÊN**. User chọn bật mặc định. Flip `dfe_enabled` false→true + opt-out `-no-dfe`. KHÔNG inert (compiler prune 138/993 hàm của CHÍNH NÓ) → cần **pruned-gen B==C** (đạt, self-reproduce byte-identical). Bật lên **lộ 2 lỗ root-set latent** của flag opt-in — compiler ko còn self-compile được dưới prune tới khi vá: **(kind 4) extern-"C" shadow** — `ax_driver_load_module` gọi qua `extern "C"` by-name (né circular resolver↔driver) → ko có AIR edge → prune → unresolved; fix: `dfe_compute` quét symbol table, root hàm nội bộ có emitted-symbol trùng một extern symbol (C-runtime externs ko có def nội bộ nên ko over-root). **(OP_SPAWN edge)** — `spawn worker(...)` giữ entry sym ở `type_id`, `src1`=callee reg≠0 nên nhánh OP_CALL bỏ qua → walk ko follow → worker bị prune, reloc dangling (unresolved tên rác); fix: walk follow `OP_SPAWN→type_id`; bắt bởi `t_spawnsmoke` (self-build ko dùng spawn nên ko thấy). ⭐ **BÀI HỌC**: cùng họ "reached-by-name, no OP_CALL edge" như ABI-shadow; **oracle-của-chính-compiler KHÔNG đủ** — 2 lỗ này chỉ hiện khi compile chương trình DÙNG feature (spawn) hoặc dùng extern-shadow, mà suite mới bắt. Gate: fast A==B, B==C, regression **538/538** (+`t_dfeexternshadow` bites root cũ), ELF 12/12, so_export ✓, exe_size 4/4 (`--staticlib` `.lib` byte-identical), ctgc 16/16. **`--staticlib`/`--shared` KHÔNG prune** (giữ public surface); `.lib` link qua `-l` thì DFE ở codegen ko thấy nội bộ.
- [✅ FIXED 2026-07-24g `C53A2DD1`, 540/540 — `let x: T = <Option/Result/sum>` (no `.unwrap()`) nay REJECT](bug-option-as-let-binding-not-rejected.md) — **probe-found** (nested `Vec[Vec[i64]]` batch): let-binding sibling của call-arg reject. `let x:i64 = o` (o:Option) đọc box raw → 8; `let x:i64 = v.get(0)` → rác. Fix mirror call-arg reject ở NODE_VAR_DECL (~L3498): annotated exp_type kind PRIMITIVE|STRUCT + RHS true-type kind OPTION/RESULT/SUM → reject. RHS true-type: IDENT đọc symbol type_id (annotated Option local = **SUM-kind** ở đây), non-ident **REUSE `inferred`** (L3477). ⚠️ **Suýt hỏng**: bản đầu re-infer `infer_node(init_expr, TYPE_UNKNOWN)` → **regress t_u64cmp 7→1** (infer_node có side-effect, re-default int literal `18e18` về i64 → signed compare); reuse `inferred` (side-effect-free, vẫn giữ Option cho call-result vì hint ko coerce Option→scalar). Frontend A==B inert. ⭐ **nested `Vec[Vec[i64]]` CHẠY ĐÚNG** khi viết đủ `.get(0).unwrap().get(1).unwrap()`; "segfault" ban đầu chỉ là chính miscompile này. Oracles t_optletreject(reject)+t_optletok(37). **+ASSIGNMENT sibling cùng phiên** (`C3A821C0`, 542/542): `x=o`→8, `x=v.get(0)`→127, `s.a=o`→8 cũng miscompile; fix mirror ở NODE_ASSIGN_STMT (~L6329), reuse `rhs_inferred` sẵn có (đừng re-infer). Oracles t_optasgreject+t_optasgok(42).
- [📋 TASK (do later, user 2026-07-24) — giải quyết TRIỆT ĐỂ name-collision cross-library (std + non-std)](task-cross-library-name-collision.md) — fn-vs-fn cross-module đã có mangling (BUG#50 flag 2048 → `ax_<name>__m<idx>`); CÒN HỞ **fn-vs-struct** ([[bug-user-fn-stdlib-struct-name-collision]] hole #5) + cần scheme symbol module-namespaced NHẤT QUÁN (ko dựa sym_idx) + **collision diagnostic ở linker** (thay first-wins âm thầm). ABI/linker change → cần RFC + B==C. Kèm câu trả lời user: sau DFE default-on, thư viện non-std import-as-source **chỉ attach hàm reachable** (dead code pruned), trừ `--staticlib`/`--shared`.
- [✅ MITIGATED 2026-07-29 `C432EA9E`, 548/548, A==B — `fn worker` vs bundled `struct worker` nay REJECT sạch](bug-user-fn-stdlib-struct-name-collision.md) — hole thứ 5 của collision family (4 hole trước là fn-vs-fn; đây là **fn-vs-STRUCT name**). **Root nailed**: `SymbolTable.define` (resolver.ax ~L509) chỉ merge FUNC-với-FUNC thành overload chain; MỌI clash cross-kind khác rơi xuống `return prev.symbol_idx` (~L574) → `NODE_FUNC_DECL.payload` trỏ vào symbol STRUCT (resolver.ax:876) → user fn ko bao giờ được gọi. Bất đối xứng làm nó im lặng: `resolve_type` (L611) lọc theo kind, nhánh value/call thì KHÔNG, mà 1 scope entry chỉ giữ 1 symbol ⇒ ko cách nào với tới hàm bị che. **Fix**: typecheck NODE_FUNC_DECL — symbol resolve ra kind STRUCT/INTERFACE/TYPE_ALIAS/VARIANT → diagnostic + reject (detector CHÍNH XÁC, ko heuristic). Verify inert TRƯỚC khi code: giao của tên struct bundled(27)×fn bundled(206) và struct compiler(113)×fn compiler(778) = RỖNG mọi chiều. Cure thật (namespacing per-module + linker diagnostic) vẫn là [[task-cross-library-name-collision]] (cần RFC + B==C). ⭐ **Reject bắt ngay 1 ca sống trong chính suite**: `t_spawnsmoke.ax` khai `fn worker` — spawn handler bị alias sang struct suốt, mà test VẪN PASS vì oracle chỉ assert exe chạy trả 5, ko bao giờ dispatch message tới handler ⇒ **oracle ko exercise symbol mà nó đặt tên thì mis-link đi lọt**. Đổi tên `zqspawnhandler`. ⭐ **BÀI HỌC cũ**: minimization bị đánh lừa vì đổi TÊN cùng lúc với feature — mọi test FAIL tên `worker`, mọi test PASS tên `setit`/`dbl`. Giữ TÊN cố định khi thu hẹp. Oracles t_fnstructcollide(reject)+t_fnstructok(105, guard over-reject: overload set fn-vs-fn + struct+method).
- [🟢 RFC 0033 concurrency v1 — Phase A `81e6f29` + B `6812cbb` + C threads, 537/537](rfc0033-concurrency-v1.md) — User: dứt điểm async/spawn-await + timer + thread cross-platform. **A (async/await):** `await` từng trả **0/rác MỌI nền** (OP_AWAIT ko case selector) + top-level `async fn` ko parse → fix parser+typecheck+selector theo **v1 synchronous-cooperative**. Oracle t_awaitv1(42). **B (timer):** `std/time.ax` ko compile (`impl{}` — ko có parser `impl`) → `std/timer.ax` grammar-clean (sleep_ms+now_ns Win kernel32/Linux syscall) + linker kernel32 list (thiếu Sleep/QPC/CreateThread/WaitForSingleObject). Oracle t_timerv1(42). **C (thread) SHIPPED Windows:** `std/thread.ax` (thread_spawn/join, kernel32 CreateThread/WaitForSingleObject THẬT); oracle t_threadv1(42, O0+O1). Linux deferred (freestanding ELF+heap blocked). ⚠️ "Phase C blocked by miscompile" (`6d6eba5`) là **BÁO ĐỘNG SAI** = [[bug-user-fn-stdlib-struct-name-collision]] (tên `worker`), đã CORRECT. spawn=actor vẫn chạy.
- [✅✅ FULLY FIXED 2026-07-24g `641D2653`, 543/543, A==B — generic infer qua NESTED ptr-self param defaulted T→i32](bug-generic-struct-inline-method.md) — ⭐ **Framing cũ ">8B return codegen" SAI HOÀN TOÀN.** Re-probe tên UNIQUE (`fetchxq`) + XQCALL trace: `Hold[str]` mono ra **`Hold__i32__fetchxq__i32`** — T default i32. "8B-T chạy" chỉ là TRÙNG HỢP (42 vừa 4 byte); i64=5000000123 bị TRUNCATE. KHÔNG riêng inline-method, KHÔNG riêng >8B: mọi generic fn có T CHỈ nằm trong `ptr[Hold[T]]` (inline/free-fn, UFCS/direct) đều hỏng. **Root** `infer_generic_type_args` (typecheck.ax ~L990/L1026): nhánh `ptr[..]` chỉ recurse khi ARG là POINTER/REF; UFCS auto-ref truyền receiver dạng VALUE (`Hold[i64]` chưa ptr) → điều kiện fail → T ko bind → i32. **Fix** 2 dòng: thêm `else` recurse inner-param với arg trực tiếp (auto-ref case). Vec/HashMap thoát vì builtin-intercepted. A==B inert. Oracle `t_genptrselfmono`(42, str-return + i64>2^32 non-truncate + free-fn). ⭐ Bài học: bug bank là "return-ABI codegen, cần deep session" hoá ra fix INFERENCE 2 dòng — sai layer; misdirection do trace tên chung `get` (stdlib) + i64 "pass" che mono sai. Re-probe tên unique + đọc MANGLED instance name. (8B-T inline-method dispatch fix cũ `ccfd1af7` vẫn đúng, tiền đề.) **+EXTENDED `09E41CC5`, 545/545**: probe tiếp thấy TUPLE `(T,i64)` + ARRAY `[T;N]` param CŨNG thiếu branch trong `infer_generic_type_args` → cùng i32-default truncation; thêm 2 branch (array mirror slice; tuple match `__tupN` struct fields). Oracles t_genptrself2param(42, 2-param) + t_gentuplearrayinfer(42). Bare literal `wrap(5000000123)`→i32 là int-literal-default (ko phải inference bug); mutator `set(self,v:T)` OK sẵn.
- [✅ FIXED 2026-07-24e (đầy đủ) `cf42579b`, 533/533 — `Option`/sum passed to scalar/struct param nay REJECT](bug-option-as-call-arg-not-rejected.md) — `f(o)` (`o:Option[i64]`, `f(x:i64)`) từng accept-then-miscompile (box đọc raw→rác 9). ⭐⭐ **Bài học lớn: annotated `Option[i64]` local resolve ra kind SUM(6) KHÔNG phải OPTION(11)** → mọi reject cũ (check kind 11/12) trượt; `node_types[arg]` UNSET ở loop 4616 → đọc type từ SYMBOL (ident) hoặc `infer_node` (call-result). Fix: param kind PRIMITIVE|STRUCT + arg true-type kind ∈ OPTION/RESULT/SUM → reject. Phủ cả ident (`f(o)`) LẪN call-result (`f(v.get(0))`), param primitive LẪN struct. B==C, oracles `t_optargreject`+`t_optargreject2`, positives sạch (unwrap, Option→Option). ⭐⭐⭐ **Meta-lesson [[lesson-bash-grep-not-powershell-selectstring]]**: PowerShell Select-String NUỐT match trace native → 5 layer chẩn đoán sai; DÙNG bash grep. Niche follow-up: SUM param nhận sum/Option KHÁC (cần so type_id).
- [✅ FIXED 2026-07-24e (`03F808DE`, 531/531) — recursive NON-generic multi-field sum `Tree=Leaf(i64)|Node(Tree,Tree)`](bug-recursive-nongeneric-multifield-sum.md) — Probe-found trên plateau. Match-bound payload `Node(l,r)`→`l` bị type sai (synth STRUCT thay `Tree`) → `sum()`=0 thay 7, `match l` reject. Fix: **forward-declare** sum type_id TRƯỚC khi resolve variant field (typetable `reserve_sum_type`/`set_sum_variants` + pre-scan trong `pre_infer_type_alias`), **GATED trên bare self-ref** (`NODE_TYPE_EXPR` payload==sym_idx) — template/mono-clone tự tham chiếu qua `Tree[..]` (NODE_GENERIC_TYPE) nên bị loại, giữ t_gentree=15. B==C. ⭐ Lần 1 (chưa gate) reserve cho MỌI non-generic sum → vỡ mono-clone `Tree[i64]`; field-kind gate mới tách được recursion thật khỏi clone. Oracle `t_rectreesum`(28).
- [⚡ M6 PERF FOCUS — Fib(40) 2.58x clang; opt A (cmp-imm) SHIPPED `AFA6529F`, harness `scripts/perf_fib.ps1`](m6-perf-baseline.md) — **HƯỚNG HIỆN TẠI** (user chọn 2026-07-24e). Driver=`AFA6529F` (opt A: `fuse_cmp_immediate`, B==C, 530/530). ⭐ Opt A ~5% -O3, ratio ko đổi. ⭐⭐ **Opt B (lea) PROTOTYPED + REVERTED — ĐO ĐƯỢC CHẬM HƠN** (922→974ms): bỏ `mov dst,base` kéo dài live-range của `n` → linear-scan allocator SPILL `n` ra stack + reload trước mỗi lea → traffic bộ nhớ > 1 insn tiết kiệm. **BÀI HỌC: instruction-count KHÔNG phải metric, TIME mới là.** ⇒ **Gap 2.58x là CẤU TRÚC (allocator/selection maturity), KHÔNG phải peephole.** ≤5% gần như bất khả nếu không làm allocator (đa-session, rủi ro cao). **CẦN USER quyết: commit allocator work HAY đổi mốc M6 (vd ≤2x).** Opt A giữ lại. Target ≤5% clang. Profiled 4 systemic codegen taxes: (1) prologue đẩy tới 8 callee-saved reg dù dùng ~2; (2) immediate materialize + redundant width-mask (`mov $C;mov $0xff;and` thay vì `and $0xff`, mask cả hằng đã vừa); (3) không dùng `lea` cho `n±const`; (4) shuffle/spill thừa. Thứ tự làm (mỗi cái isolated+measured, backend→**B==C bắt buộc**): #2 (immediate-AND + const-fold, blast radius nhỏ nhất) → #3 (lea) → #1 (lean prologue). 2.59x→1.05x là chương trình nhiều session; mốc gần: hạ #2/#3 xuống ~1.5x rồi tái đánh giá. ĐỪNG batch — attribute từng delta.
- [🟢 STATE 2026-07-24e — RFC 0015 P3 RETURN-PATH FREE SHIPPED (`12DBE1D8`, 530/530, ctgc 16/16)](session-state-2026-07-24e.md) — **ĐỌC ĐẦU TIÊN**. User: "giải quyết dứt điểm #8". Freeable locals live on a `return` path now freed on that path too (were leaking — CTGC only freed at fall-through). **No AST temp needed** (the RFC anticipated one): `lower_return` already materialises `ret_val` before its pre-`OP_RETURN` hook (same point `flush_defers` uses), so CTGC destroys ride there. 2 edits: (1) ctgc.ax block traversal → **ordered-descent** (push freeable var only AFTER descending past its decl → at a return the whole stack is provably initialised → safe to free; fall-through set/order byte-unchanged) + new `NODE_RETURN_STMT` branch appends `NODE_DESTROY_STMT` as TRAILING return-node children; (2) `air_builder.lower_return` lowers them after `ret_val`, before `flush_defers`. Sound because EscapeAnalyser already marks any return-reachable local as escaping → never freeable (pinned `t_ctgcfreeesc`). Oracles `t_ctgcretfree`(0/2)+`t_ctgcretorder`(0/3). Remaining safe leak: `break`/`continue` paths (untouched). Fixpoint inert (freeable=0).
- [🟢 STATE 2026-07-24d — #1 ELF re-verify 12/12; #2 RESOLVED: RFC 0032 CLOSED — real cross-module ELF `.so` (P1 ET_DYN+`.dynsym`, P2 globals, P3 spawn→RIP-relative)](session-state-2026-07-24d.md) — **ĐỌC ĐẦU TIÊN**. Driver `0D46A5C5`, **529/529**. Investigator ground-truth: codegen ALREADY fully RIP-relative → PIC "wall" mostly illusory. **P1** (linker-only, A==B): ET_DYN + PT_GNU_STACK + defined `.dynsym`/`.hash` exports, gated `is_shared`; `axmath.so` dlopen+dlsym+call works under ASLR. **P2** globals/intra-call/string-return verified (`so_export_check.sh`, python3 ctypes, no gcc). **P3** OP_SPAWN ABS64→lea+PC32 (one grep-confirmed site = OP_FUNC_ADDR encoding), target-indep → **B==C `0D46A5C5`**; added `t_spawnsmoke` (spawn had ZERO runtime coverage). Caveat: no direct spawn-in-rebased-.so oracle (actor-model needs message infra). Notes: `--shared` source arg BEFORE flag; `nm -D` not `readelf --dyn-syms`; PT_GNU_STACK required or dlopen refuses exec-stack.
- [🟢 STATE 2026-07-24c — RFC 0015 P3 CTGC free FLIPPED ON BY DEFAULT (`AF899BDF`, 528/528)](session-state-2026-07-24c.md) — **ĐỌC ĐẦU TIÊN**. User chose "RFC 0015 P3 → flip default-on". `-ctgc-free` OFF→ON (main_air.ax:801) + new `-no-ctgc-free` opt-out. The 07-24b "HIGH-RISK / needs borrow-alias tracking" framing was **STALE** — that shipped 07-16…07-22; self-host freeable set=0 made the flip INERT (fast fixpoint A==B `AF899BDF`, regression 528/528 rebuilt by default-on compiler, ctgc_free_check 14/14). Lesson: reconcile a "remaining task" note against what actually shipped (grep flag wiring + run gate) before trusting its risk label. Next: ELF path re-verify under default-on (low risk); return-path free still leaks (safe).
- [🟢 STATE 2026-07-24b — feature backlog CLOSED (`000971c`, 528/528); safe backlog exhausted, next=user-directed](session-state-2026-07-24b.md) — **ĐỌC ĐẦU TIÊN**. `Vec.for_each`/`Vec.reduce` shipped (only new code); `to_hex_upper` + interface-vtable-dispatch(BUG#71) were STALE notes (already shipped) — closed + banked `t_ifacevecpoly(70)`. 19-probe clean sweep (0 silent miscompiles, incl. bug92b `Pair[str]`/`Tree[str]` guard). No OPEN bugs; remaining backlog (RFC 0009 P3 ELF / 0015 P3 CTGC / M4-M6) all heavy/risky → needs user direction.
- [🟢 RFC 0014 drop-glue COMPLETE 2026-07-24 (`70D2EEBC`, 526/526)](rfc0014-drop-glue-complete.md) — User: "giải quyết dứt điểm RFC 0014". Backlog note "BLOCKED on bug69" đã **STALE** (bug69 CTGC activation ship P1+P2+P3 từ lâu). Đóng 2 success-criteria còn lại: **P1** copy-by-value của value có `drop` nay **REJECT E4003** (ownership.ax, chống double-free, §5.2/§6-P1/§9-#3; gate `has_drop_types` → inert khi ko có drop type → A==B); **P3** bignum auto-free **SCOPED OUT** với root cause chính xác — `escape.ax::expr_is_owning` chỉ coi init = ctor trực tiếp là owning, bignum dựng qua HÀM (`bu_alloc`/`bu_new`…) → ko value nào owned → drop ko bao giờ chạy; cần interprocedural return-value ownership (RFC-scale). Oracles t_dropcopy(reject)/t_dropcopyok(42).
- [🟢 STATE 2026-07-24 — 3 "bug còn mở" ĐÓNG DỨT ĐIỂM (`7f3ec7a`)](bug92-generic-recursive-multifield-open.md) — **ĐỌC ĐẦU TIÊN**. HEAD `7f3ec7a`, driver `axc_native` A==B `23C1E261`, **524/524** (+t_gentreestr). User: "giải quyết dứt điểm các bug còn mở". (1) **BUG#92b FIXED** — generic multi-field variant có field mono ra >8B (str): `lower_variant_construct` dùng TEMPLATE synth struct (T=8B) trong khi `lower_match` đọc MONO (str=16B) → drop len/shift field (`Pair("hello",5)`→5, `Tree[str]`→SEGV). Fix: `resolve_mono_sum_for_ctor` re-derive payload từ mono sum của ctor node, khớp bằng **generic_args + tên variant** (KHÔNG name_id — mono sum mang tên MANGLED, đó là lý do `resolve_sum_type` cũ trả template). Inert trên self-build (A==B). (2) **bug90** Option/Result method trên untyped receiver (`m.get(k).unwrap()`) — verify CLOSED, không cần sửa; spinoff còn lại là BUG#93 (HashMap.contains) tách riêng. (3) **bug-const-shift-fold** — correctness đã an toàn (fold tắt, `5<<1`+`160>>2`=50); khôi phục fold DECLINED theo §10 (lợi ích fold 2 literal ~0, rủi ro silent-miscompile). Bài học: predicate so kiểu qua ranh giới template/mono thì **mangled-name là một phần phép so** — khớp bằng generic_args, không phải name_id.
- [🟢 RFC 0031 — DFE ĐÃ SHIP (`-dfe`, opt-in): `return 42` **77.824 → 22.016 B (−71,7%)**](../rfcs/0031-dead-function-elimination.md) — `9ce3d69`. Opt-in nên đường mặc định byte-identical **theo cấu trúc**, không phải nhờ test. **Thứ THỰC SỰ kiểm chứng root set** (oracle KHÔNG đủ — lỗi là gọi vào địa chỉ chưa từng emit, chỉ nổ khi đúng path chạy): (1) `AXEXTRA=-dfe` mới thêm vào `regression_repros.sh` + `elf_linux_check.sh` → chạy LẠI toàn bộ **511/511 + ELF 12/12** dưới chế độ prune; (2) **compiler tự dịch chính nó khi đã bị xoá 138/993 hàm của chính nó, ra binary GIỐNG HỆT TỪNG BYTE** bản không prune — 1,96 MB source ép cả frontend+backend+linker, mạnh hơn mọi oracle; (3) không có entry và không có export (`--staticlib`/`--shared`) ⇒ `dfe_compute` trả null, KHÔNG prune gì (nếu không sẽ prune sạch thư viện thành archive rỗng mà vẫn "đúng"). ⚠️ **ELF suýt ship hỏng và COFF không thể phát hiện**: `AXEXTRA=-dfe` trên ELF ra **4/12 SIGSEGV** trong khi COFF 511/511 — xem [[dfe-elf-runtime-is-in-program]]. `-dfe` in TÊN từng hàm bị xoá; chính listing đó biến sự cố ELF thành chẩn đoán trong 1 lần build. Giữ opt-in: "chưa test nào bắt được root sai" ≠ "root set đã đủ"; bật mặc định là quyết định của USER.
- [📐 RFC 0031 — bối cảnh đo lường ban đầu](../rfcs/0031-dead-function-elimination.md) — nền của mục trên. `return 42` = **77.824B**, `.text` chiếm **96,7%**; chương trình dùng Vec+HashMap+string = 82.432B → chênh chỉ **4,6KB** ⇒ ~75KB là chi phí CỐ ĐỊNH mọi exe, gần như toàn bộ unreachable. Gốc: native path bundle nguyên stdlib; DCE hiện chỉ intra-procedural (`remove_unreachable_blocks`), KHÔNG có ở mức HÀM. Thiết kế: prune ở **CODEGEN** (không phải linker — offset gán lúc emit nên hàm bị bỏ không vào layout, khỏi renumber reloc). ⚠️ **Toàn bộ rủi ro nằm ở ROOT SET** — sai là thành lời gọi hoang chứ không phải file nhỏ hơn. **`-dfe-report` ĐÃ SHIP (report-only, chứng minh inert bằng `cmp`)**. ⭐ **Con số đầu tiên SAI 60 lần và chính pass tự bắt được**: seed từ `main` cho `return 42` = **1/184**, sau khi seed đủ root = **61/184**. Khảo sát read-only đoán sai 2/3: drop-glue + operator-overload KHÔNG phải root ẩn (đều phát `OP_CALL` mang symbol) — đúng; nhưng "`ax_free` là root ẩn cần seed" và "allow-list runtime ABI là hạt giống" đều **ngược**: DLL import KHÔNG nằm trong `mod.funcs` nên prune không với tới được. Hiểm hoạ thật là **SHADOW**: `std/runtime.ax` định nghĩa `ax_str_len`/`ax_str_concat`/`ax_str_slice`/`ax_str_replace`/`ax_panic`… là hàm AXIOM thường, và chúng được bind **THEO TÊN** (`x86_resolve_callee_name` đổi `free`→`"ax_free"`, `std.string.len`→`"ax_str_len"`; callee ma -1/-2/-10..-22 cũng ra tên đó) ⇒ **caller không để lại cạnh nào trong AIR**. 60/61 hàm reachable của `return 42` treo dưới 10 shim này. **Bài học: tiền đề nào rẻ đến mức assert được bằng code thì đừng để nó ở dạng văn xuôi** — counter kiểm tra tiền đề đã bắt lỗi ngay lần chạy ĐẦU TIÊN. Root thứ 3 `#[export]` cũng vậy: ship ra báo **0** vì `export_syms` giữ intern name-id còn symbol phát ra đã mangle → phải khớp CẢ id LẪN tên. Entry = `main`/`_AX_main_main_v_v` (`linker.ax:~3101`). Oracle: **`t_indirectcall`(42)** (fn-ptr / lambda-arg / vtable RFC 0029 / mảng fn-ptr, hiệu chỉnh bằng dump-air: đúng 5 `funcaddr`) + **`t_dfeexport`(42)** — và chính oracle thứ 2 này lòi ra [[bug-export-in-exe-image-file-dll]].
- [🟢⭐ ĐÃ SỬA: allocator nhỏ từng chặn cứng ở **256 MB** → nâng lên **1 GB**, MỞ KHOÁ crash RFC 0029](bug-small-alloc-256mb-segment-slab-cap.md) — **ĐÂY MỚI LÀ NGUYÊN NHÂN** của `OOM size requested: 24` chặn [[bug-iface-variant-payload-no-vtable-box]], KHÔNG phải "hết RAM". `std/mem/alloc.ax:375` trong `alloc_segment_meta()`: `if slab_used >= 4096: return null`. Vượt ngưỡng → `ax_segment_acquire` trả null → `malloc` panic, và request chỉ 24 BYTE trong khi tiến trình đang giữ nhiều GB. **Vì sao mọi manh mối khác đánh lừa:** (a) tiến trình đỉnh 7,9 GB nên trông như cạn RAM thật — nhưng `ax_large_alloc` (>4096 B) đi thẳng ra OS và KHÔNG bị chặn, nên tiến trình phình tự do trong khi pool NHỎ kẹt ở 256 MB; (b) máy còn dư: 15,9 GB RAM, free 8,0 GB, commit limit 37,9 GB / dùng 12,7 GB ⇒ còn ~25 GB commit, `VirtualAlloc` 64KB không đời nào hỏng — chính mâu thuẫn này chỉ đúng chỗ; (c) `MAX_SEGMENTS` **thật sự không được tham chiếu**, giới hạn là **literal 4096** ở dòng 375 ⇒ note trước của tôi ("không có gì enforce") SAI ở đúng chỗ quan trọng: giá trị CÓ được enforce, chỉ là không qua hằng số đặt tên — đúng kiểu drift mà hằng số sinh ra để ngăn. Giải thích luôn biên ~30 MB và vì sao mọi vị trí fix đều chết như nhau, còn câu lệnh TRƠ trong hàm nguội thì không (nó không cấp phát gì). ⛔ **CẢNH BÁO TRƯỚC KHI SỬA: 5 site ghép cứng, 4 cái là magic number.** `Segment` = **48 byte**, và **48 × 4096 = 196.608 CHÍNH XÁC**. Slab được cấp đúng 196608 byte ở **3 chỗ** (`:160` VirtualAlloc, `:166` mmap Linux, `:352` memset init), cap thật là literal `4096` ở `:375`, hằng có TÊN `MAX_SEGMENTS` ở `:22` thì KHÔNG AI ĐỌC, còn `runtime/axalloc/*.c` giữ bản C riêng. ⇒ **Nâng số segment mà không nâng cả ba chỗ 196608 = ghi descriptor #4097 ra ngoài vùng đã map = hỏng heap ÂM THẦM, trong chính allocator, trên CẢ HAI nền tảng.** Đó mới là defect sâu: một quyết định dung lượng viết ở 5 nơi qua 2 ngôn ngữ, 4 nơi là literal trần, và nơi DUY NHẤT có tên thì không ai dùng. **Hướng sửa:** (1) nâng cap — slab là mảng cố định nên chỉ tốn descriptor, segment vẫn lazy; (2) dùng `MAX_SEGMENTS` ở dòng 375 để `.ax` và `runtime/axalloc/*.c` không lệch nhau nữa; (3) giảm giữ object nhỏ ở typecheck (đúng đắn nhất, lớn nhất). (1)+(2) là thay đổi nhỏ, gọn, mở khoá được crash của một feature đã ship — nhưng là ALLOCATOR nên phải làm có gate.
- [✅ RESOLVED 2026-07-23 `5bfd5c7` — self-link `-O1` peak **7,7 GB → 1,2 GB (−84%)**](compiler-selfhost-peak-memory-8gb.md) — **ĐÃ SỬA.** Gốc KHÔNG phải per-node retention/mono-clone/type-table như note cũ chỉ, mà là MỘT leak trong `clone_subtree_from` (ast.ax): nối text token vào `self.src` monolithic bằng `std.string.concat` MỖI TOKEN clone mà không free `old_src` → mỗi append leak ~2 MB, hàng nghìn token clone qua các instantiation = **~7,76 GB (3.936 buffer ~2 MB, đúng bucket 512K–4M)**. Fix: free buffer concat trung gian (concat đã copy nội dung sang buffer mới nên mọi offset vẫn resolve); guard `AstTree.orig_src` để KHÔNG BAO GIỜ free source gốc caller-owned. Memory-only, output byte-identical (A==B `E4E10E2D`, regression 518/518). ⭐ **Cách tìm quyết định: đo PHÂN BỐ KÍCH THƯỚC alloc lúc shutdown** (probe tạm ở `__ax_runtime_shutdown`: small pool chỉ 268MB/slab 4099 → bác giả thuyết get_str-small-leak; large-live 7,76GB/~3936 buffer ~2MB → trỏ thẳng concat), KHÔNG phải suy luận "gì đính vào node". Note cũ ("1,9KB/byte, per-node, không O(n²), bắt đầu ở typecheck") là **hướng SAI** — synthetic nhỏ ít instantiation nên leak concat nhỏ, trông như hằng per-byte đều. **Gỡ trần này mở khoá [[bug-iface-variant-payload-no-vtable-box]]** (mọi nỗ lực fix trước chết vì OOM ~30MB margin; nay có ~6,5GB headroom). Chi tiết đo lường lịch sử ↓.
- [📜 (lịch sử) ĐO ĐƯỢC: self-link `-O1` ĐỈNH ~**7,9 GB**](compiler-selfhost-peak-memory-8gb.md) — build **THÀNH CÔNG** của `tmp_concatenated_air.ax` (source 1,9 MB) đã ngốn **~7.865 MB working set**. Con số này giải thích trọn blocker phải reframe 4 lần: thêm 1 call site vào `lower_call_expr` → OOM. **KHÔNG phải trần call-site, KHÔNG phải pass tối ưu, KHÔNG phải cap allocator** — build vốn đã chạy sát mép RAM, đụng thêm chút là rơi. Đã loại từng giả thuyết bằng ĐO: (a) `MAX_SEGMENTS=4096`×64KB=256MB trong `std/mem/alloc.ax` **là hằng CHẾT, không nơi nào tham chiếu** — đọc như một cap nhưng không phải (nên xoá hoặc nối vào cho khỏi lừa người sau); (b) `--time` cho thấy codegen XONG (1868 ms, 15.281 reloc, 2.447 sym, đã ghi object) rồi mới chết ở **Stage 5 self-linking**; (c) câu lệnh không-có-call thêm bao nhiêu cũng được, chỉ câu CÓ call mới đổ — khớp với việc mỗi call site nhân số relocation mà linker phải giữ trong RAM. ⭐ **QUY TRÁCH NHIỆM XONG (đối chiếu chuỗi working-set với mốc `--time`): TYPECHECK ngốn ~5,3 GB / 7,9 GB (~67%)**, air-build+ssa-opt ~1,5 GB (19%), **codegen chỉ ~120 MB**, self-link ~500 MB. Tức là **NGƯỢC HẲN với mọi chỗ mà cuộc điều tra chỉ tới**: thông báo OOM nêu tên linker, câu lệnh gây lỗi nằm ở lowering, và cả 2 bản note trước của chính tôi đều chỉ người đọc sang `linker.ax` rồi sang optimizer — trong khi ngân sách thật tiêu ở `typecheck.ax`, sớm hơn 3 phase, và không có gì trong lúc crash hé lộ điều đó. **Bắt đầu ở typecheck.** ⚠️ **Đây mới là defect đầu bảng**: mọi feature thêm một lời gọi vào hàm lowering nóng đều đụng tường này, và triệu chứng là OOM lúc link chẳng liên quan gì tới thay đổi gây ra nó.
- [✅ PROBE SẠCH — guard "chỉ closure zero-capture" (RFC 0008 P2 chưa impl) enforce ĐÚNG](probe-closure-capture-guard-sound.md) — mọi dạng capture (let local, param, field `c.base`, loop var, trong arithmetic/call-arg/array-index/method-call/nested-closure) đều REJECT sạch kèm diagnostic, KHÔNG cái nào miscompile. Guard đệ quy vào mọi sub-form expr ⇒ không có kẽ "capture ở vị trí lạ" (khác họ match-scrutinee). Zero-capture thật thì chạy đúng (mảng closure gọi theo index). ⚠️ Caveat tương lai (CHƯA phải bug): closure BODY dạng block `|x| -> T:` + indent KHÔNG parse ⇒ capture-check không chạy trên block body; nếu sau này thêm cú pháp đó, phải verify lift-pass đệ quy vào STATEMENT trong block. ĐỪNG probe lại vùng này.
- [✅ CLOSED 2026-07-24 `f614c11` — interface CONFORMANCE nay check SIGNATURE (param-count + return type)](bug-iface-conformance-signature-not-checked.md) — F2 (return i64 vs bool), F3 (ít param → drop arg), F3b (thừa param → đọc garbage) ĐỀU reject; exact-signature vẫn compile+run. 3 helper trong typecheck.ax (`interface_method_sig` đọc sig từ interface decl AST, `struct_method_sig`, `interface_signature_mismatch`) wired vào `check_iface_conformance`. Chỉ reject thứ CHỨNG MINH được khác (param-count, hoặc return type khi cả 2 concrete/non-generic/không-Self); cross-tree + Self/generic thì SKIP → không false-reject. Fixpoint A==B `C7F430A2` (compiler tự dịch không bị over-reject), regression 523/523 (+t_ifacesigok=15, +3 reject). ⭐ **Latent bug né được:** `interface_method_ret_type` (typecheck.ax:1600) match interface decl bằng `dn.payload == iname` — SAI vì resolver ghi đè `NODE_INTERFACE_DECL.payload` thành SYMBOL INDEX (thấy ở :2544); helper cũ mang bug này vô hại vì caller (:4132) dung thứ return 0. Bản copy đầu của tôi dính y hệt (SIGDBG cho have_iface=0). Sửa: match qua `symbols[payload].type_id == iface_type`. Bài học: helper "chạy được" có thể sai âm thầm nhờ caller che — verify predicate với node-rewrite của resolver trước khi tái dùng.
- [📜 (lịch sử) OPEN — interface CONFORMANCE chỉ check TÊN method](bug-iface-conformance-signature-not-checked.md) — `check_iface_conformance`→`interface_missing_method` chỉ xác minh struct CÓ method đúng TÊN, KHÔNG check arity/kiểu-param/kiểu-return. Struct có method cùng tên nhưng SAI signature được chấp nhận là conform → vtable dispatch qua signature lệch. 3 mức: **F2** sai return type (`area()->bool` cho iface `->i64`) accept; **F3** ít param hơn → **drop arg âm thầm** (`scale(3)` bỏ qua 3); **F3b** nhiều param hơn → **đọc GARBAGE** cho param thừa (UB, phi định). Đều accept-then-miscompile, ĐÁNG LẼ reject. Gốc chính xác: `interface_missing_method` trả tên method thiếu THEO TÊN, không so signature. Fix = mở rộng conformance check so param-count/type + return-type (dedicated session, reject-path, rủi ro over-reject theo lịch sử malformed-input). RFC 0029 `7858aa5` mới ship nửa name-presence; nửa signature chưa làm. Trực giao với họ coercion-site.
- [✅ CLOSED 2026-07-23 `efc533b` — interface trong VARIANT PAYLOAD: CẢ HỌ đã xong (Option/Result Some/Ok/Err nay box payload)](bug-iface-variant-payload-no-vtable-box.md) — nửa Option/Result cuối cùng ĐÃ SỬA sau khi gỡ trần bộ nhớ ([[compiler-selfhost-peak-memory-8gb]] `5bfd5c7` — chính là blocker mọi lần thử trước, không phải logic fix). Fix 2 vế: (1) **typecheck** `try_instantiate_variant_call` bind type param = INTERFACE khi payload struct conform (gated `interface_missing_method==0`) → ctor node types `Option[Shape]`; (2) **air_builder** `opt_res_ctor_iface` đọc type ctor node, box struct→interface qua `coerce_struct_to_interface` trước OP_STORE. `node_types[payload_idx]` giữ concrete Sq, interface target lấy từ CTOR node (gỡ nút "type recovery"). Verified Some/Ok/fn-param/None đúng O0–O3 (oracle t_ifacevariantpayload=42), A==B `4A5929D0`, regression 519/519. ⚠️ Note cũ có MÂU THUẪN nội bộ (payload node vừa "là interface" vừa "remained concrete struct") — sự thật là concrete; đọc code 2 hàm giải quyết trong vài phút thứ prose để mập mờ. Chi tiết đầy đủ + lịch sử probe ở topic file.
- [📜 (lịch sử) PARTIAL — interface trong VARIANT PAYLOAD](bug-iface-variant-payload-no-vtable-box.md) — **BUG THẬT, probe 2026-07-23**. `let o: Option[Shape] = Some(Sq(side:3))` rồi `o.unwrap().area()` → **SEGV**. **NỬA TUPLE ĐÃ SỬA** (`t_ifacetuple`, ship `909e9e5`); **nửa VARIANT vẫn open vì LÝ DO KHÁC**: sau khi nâng cap, fix biên dịch được nhưng **vẫn segfault lúc chạy** — cùng kiểu fix ở ctor `Some`/`Ok` đọc `node_types[idx]` của node lời gọi ctor mà nó KHÔNG PHẢI `Option[Shape]` (IFPROBE đếm 0 hit), nên không box gì. **Phân biệt cốt lõi: typecheck gán cho TUPLE literal kiểu aggregate đã khai báo, nhưng gán cho VARIANT ctor kiểu SUY TỪ payload, không phải `Option[Shape]` của annotation** ⇒ kiểu interface khai báo không tới được node ctor từ dưới lên ⇒ đây là candidate 2 (rebuild ở binding site / luồn kiểu target của `let` vào ctor lowering), không phải one-liner như tuple. Trần bộ nhớ che khuất điều này suốt: trước đây fix còn chẳng CHẠY được để thấy nó đọc sai kiểu. Cả họ variant payload đều dính: `Option`/`Result`/**user sum**/**đối số hàm**. ⭐ **DỰ ĐOÁN RỒI TÌM ĐƯỢC (probe sau): TUPLE cũng dính** — `let t: (Shape, i64) = (Sq(..), 7)` → **SEGV**, cùng workaround. Suy ra từ chính root cause (danh sách site liệt kê tay bị sót): payload của tuple cũng là vị trí aggregate-constructor y như variant payload. ⇒ lỗi là **THIẾU SITE**, không phải đặc thù `Some`/`Ok`. Probe cùng lúc mà SẠCH: HashMap value, gán lại `mut s: Shape`, field của generic struct, param generic, array literal ⇒ phạm vi sót HẸP: **payload của CONSTRUCTOR aggregate (variant + tuple)**, không phải "container nói chung". NHƯNG `Vec[Shape]`, field struct `s: Shape`, `let s: Shape = Sq(..)` đều **OK** ⇒ khu trú chính xác ở payload của constructor. **Workaround**: ép qua local có kiểu trước (`let s: Shape = Sq(..)` rồi `Some(s)` → đúng 9). Gốc: `coerce_struct_to_interface` (air_builder.ax:1744) tự liệt kê 5 site trong comment — *call-arg, let-binding, return, struct-field init, assignment* — **THIẾU variant payload**; 7 call site, không cái nào là đường `Some`/`Ok`/user-variant (~L2165, chỉ `lower_expr` trần). ⚠️ **KHÔNG phải fix 1 dòng**: ctor lower **bottom-up**, `box_ty` tính TỪ payload (`register_option(payload_t)`) nên `Some(Sq)` dựng `Option[Sq]`, kiểu khai báo `Option[Shape]` không bao giờ tới nơi; ở site `let` thì coercion có chạy nhưng bail vì target kind là OPTION chứ không phải INTERFACE. Reject width sẵn có (typecheck.ax:4971) **không thể** bắt: chỉ áp cho đối số VÀ so **SIZE** — iface box 8B, `Sq` 8B, giống hệt nhau. Repro: `bin/known_fail_iface_variant_payload.ax` (KHÔNG đưa vào regression khi chưa fix). ⛔ **BLOCKER THẬT (bisect 2026-07-23): vùng variant-ctor của `lower_call_expr` KHÔNG CHỨA THÊM ĐƯỢC MỘT DÒNG NÀO.** Thêm *bất kỳ* câu lệnh nào vào trong `if variant_ctor != 0:` → seed compiler chết `AXIOM RUNTIME PANIC: Out of memory` khi dịch source 1.9MB — kể cả câu lệnh TRƠ hoàn toàn `if variant_ctor == 99: ax_puts_local(..)`; cùng câu đó đặt trong `coerce_struct_to_interface` thì **build ngon**. Probe đếm điều kiện interface: **0 hit** (coercion chưa hề chạy) mà vẫn OOM ⇒ lỗi KHÔNG liên quan logic fix. Giống họ "stage0 ceiling" nhưng ở phía stage1 và cục bộ theo hàm. ⭐ **QUY TẮC CHÍNH XÁC (bisect tiếp): `lower_call_expr` KHÔNG NHẬN THÊM ĐƯỢC MỘT CALL SITE NÀO NỮA.** Câu lệnh KHÔNG có lời gọi (`mut probe := idx`) thêm ở đâu trong hàm cũng **OK**; câu lệnh CÓ lời gọi thì OOM bất kể đặt ở đâu trong hàm, còn đúng lời gọi đó đặt ở hàm bên cạnh lại **bình thường**. Giải thích luôn được ca tưởng là phản ví dụ: `node_types[idx]` nhìn thì không gọi hàm, nhưng index mảng phát **`ax_bounds_check`** ⇒ cũng là call site. ⭐⭐ **CHÍNH XÁC HƠN NỮA: đây là giới hạn của `-O1`, KHÔNG phải giới hạn cố hữu** — cùng source đó build **`-O0` THÀNH CÔNG**, chỉ `-O1` mới OOM. ⚠️ **ĐÍNH CHÍNH (cùng phiên): KHÔNG phải pass tối ưu — là LINKER.** Chạy repro với `--time` cho thấy codegen **HOÀN TẤT** (`[time] codegen: 1868 ms`, 15281 reloc, 2447 sym, đã ghi object) rồi mới chết ở **Stage 5: self-linking**. `-O1` chỉ đổi object đưa cho linker (số reloc/symbol), chứ pass tối ưu không hề hỏng. ⇒ **Hướng (3) bắt đầu ở `linker.ax`, KHÔNG phải pipeline optimizer** — phạm vi hẹp hơn nhiều và là FILE KHÁC với chỗ note trước đó chỉ sai. (Bài học: 1 lần chạy `--time` lật đổ một giả thuyết hợp lý mà tôi đã commit.) Là giới hạn **backend** (áp lực register/spill theo call site), KHÔNG phải giới hạn kích thước source. ⛔ **ĐÍNH CHÍNH (đã THỬ): hướng (1) và (2) là ẢO.** Đã cài nửa TUPLE trong `lower_tuple_lit` — hàm KHÁC, nhỏ hơn — gọi `coerce_field_to_interface` sẵn có: **vẫn OOM**. Control trước đó đánh lừa: câu `if x == 0xFFFFFFFF` TRƠ trong `coerce_struct_to_interface` build được, nhưng một nhánh không bao giờ chạy trong hàm NGUỘI thì gần như không thêm gì; một lời gọi THẬT trên đường NÓNG (mọi tuple literal) là phép thử khác hẳn — và nó đổ. ⇒ **Trần KHÔNG cục bộ theo hàm, mà là biên ~30 MB TOÀN CỤC.** Không có "ngân sách call site theo hàm" để đánh đổi. **Chỉ còn MỘT hướng: sửa trần bộ nhớ** ([[compiler-selfhost-peak-memory-8gb]]) — mọi nỗ lực fix, ở mọi vị trí, đều chết vào đó. **Cùng dạng lỗi với root-set RFC 0031: một danh sách site liệt kê tay bị sót một mục, và mục sót đó hỏng thành wild dispatch.**
- [⚠️ 2 cái bẫy khi test tiêu thụ `.lib`/`.dll` — CẢ HAI ra SEGFAULT chứ không phải build fail](lib-consumption-cwd-and-export-traps.md) — (1) **phải chạy compiler từ ROOT repo**: `std/*.ax` và mọi `import` resolve theo **CWD**; chạy từ `tests/ffi` thì bundle stdlib im lặng mở lỗi → 8 dòng `Error: open failed:` KHÔNG kèm tên file → exe ~2KB segfault. `tests/ffi/README.md` ghi `cd tests/ffi` + path tương đối nên **không thể chạy đúng như viết** — đã sửa. (2) **`-l` chỉ link được `#[export] pub fn`**, `pub fn` trơn ra `Unresolved external symbol` rồi **vẫn emit exe** → segfault khi gọi. ⭐ **Hệ quả quan trọng**: bẫy (2) làm tưởng `pub` không-export là KHÔNG tiêu thụ được từ `.lib` ⇒ guard "không prune `--staticlib`" của RFC 0031 là thừa. **SAI** — đường `import` + `--auto-lib` tiêu thụ được (đã verify: `pub_only` không hề `#[export]`, `--auto-lib` → **exit 42**). Vậy guard là **có căn cứ thực nghiệm**, không phải chỉ bảo thủ. Bài học: **hai đường tiêu thụ có yêu cầu symbol KHÁC NHAU** — chỉ test `-l` sẽ kết luận ngược.
- [⚠️ ĐỪNG đọc prune-list của `-dfe` mà quên INLINER](dfe-prune-list-vs-inlining.md) — ở `-O1`+ inliner đã xoá cạnh gọi, nên callee nhỏ bị inline là **chết thật**, prune nó là ĐÚNG. Probe `--shared` thấy `ax_helper` bị prune trong khi `lib_triple` (export root) gọi nó vẫn sống → trông y hệt "export root không lan truyền" = wild call trong DLL; **thực ra KHÔNG phải bug**. Control case tự tố cáo: `main→mid→leaf` prune `leaf` mà vẫn trả đúng `14*3=42`. Quy trình trước khi báo lỗi: (1) làm callee KHÔNG inline được (vòng lặp + vài nhánh) rồi chạy lại; (2) xem KẾT QUẢ chương trình — root thiếu thật thì SIGSEGV/sai số, không phải chỉ là một cái tên trong list; (3) so `-O0` với `-O1`. Cũng đã verify lần đầu end-to-end: **`--shared` + `-dfe`** giữ nguyên export directory, `IMAGE_FILE_DLL`, mọi hàm export + transitive callee của chúng. Oracle `t_dfeexport` đã được vá điểm yếu: bản đầu để mọi hàm export là LEAF nên chỉ chứng minh root sống sót, KHÔNG chứng minh walk đi XUYÊN QUA root — nay có `exported_via_helper → deep_helper` (cố ý quá lớn để inline).
- [⚠️ ELF: runtime `ax_*` nằm TRONG chương trình, không phải DLL import](dfe-elf-runtime-is-in-program.md) — `--target linux` không có `ax_runtime.dll`: cả họ `ax_println_*`/`ax_str_eq`/`ax_panic`/syscall được biên dịch VÀO chương trình (**206 hàm** so với **184** trên COFF) và chỉ tới được qua callee ma. Trên COFF chúng là import (không nằm trong `mod.funcs`) nên prune không đụng tới ⇒ **thiếu root ở ELF là vô hình trên COFF**. Bug cụ thể: test root gọi `is_valid_runtime_dll_symbol(nm)` với tên đã emit, nhưng codegen thêm tiền tố `ax_` ⇒ tên thật là **`ax_ax_println_str`**, không bao giờ khớp → mọi ELF bị prune đều SIGSEGV trong khi COFF 511/511. `dfe_is_abi_name` nay strip 1 tiền tố `ax_` trước khi tra. **Bài học chung: predicate so TÊN qua ranh giới tầng thì MANGLING là một phần của phép so** — đây là lần thứ 3 trong cùng phiên (sau `#[export]` id-vs-mangled và chính ABI shadow).
- [🐛✅ `#[export]` trong EXE làm exe không chạy được — ĐÃ SỬA](bug-export-in-exe-image-file-dll.md) — `linker.ax` ép `is_dll = true` chỉ vì **có** export directory, nhét `IMAGE_FILE_DLL` (0x2000) vào `Characteristics` của EXECUTABLE (`0x2026` thay vì `0x0026`) ⇒ Windows từ chối nạp: *"not a valid application for this OS platform"*, chết ở loader TRƯỚC `main` nên không có output lẫn exit code để lần. **Có export ≠ là thư viện**: PE exe được phép export; chỉ `--shared` mới được set bit đó. Tìm ra khi viết oracle `t_dfeexport` cho RFC 0031 — `#[export]` trước nay KHÔNG có coverage nào ở dạng exe nên hỏng toàn tập mà suite im lặng. Đã pin cả 2 chiều (row regression + `--shared` vẫn `0x2026`). B==C `7EAFE11B`.
- [🟢 STATE 2026-07-22e — NHÓM A (4 design decision) ĐÃ THỰC HIỆN](group-a-design-decisions-2026-07-22.md) — **ĐỌC ĐẦU TIÊN**. User: "Thực hiện #A, đưa ra option để chọn" + ràng buộc mới **"không muốn làm phình file thực thi"** ([[feedback-no-exe-bloat]]). **A3** ascii/utf16/utf32 = KHÔNG thêm type, `Vec[i64]` là chính thức (RFC 0020 §10). **A4** dialect M4 (`=>`/`.length()`/`impl Trait`) = KHÔNG nhận cái nào. **A2** RFC 0027 path D SHIPPED `f1a7f9b` — free field iff pointer-typed VÀ mọi write là **cấp phát tươi tại chỗ**; ⭐ bản mạnh hơn (theo vết local) **UNSOUND, gây UAF thật**: `Buf(data: d)` vs `View(borrowed: d)` giống hệt cú pháp, khác nhau ở **aliasing** chứ không phải provenance → oracle `t_ctgcborrow` bắt (42→99). **Vậy option annotation mà user từ chối mới là thứ tổng quát hoá được** — nay có bằng chứng ĐO ĐƯỢC. **A1** ⭐ tìm ra **defect 2× kích thước exe KHÔNG liên quan .bss**: `x86_coff.ax` canh lề global theo `% gsize` (SIZE) thay vì ALIGNMENT → global lớn đệm tới bội số của chính nó rồi ghi lại → `[i64;200000]` **3,278,336 → 1,678,336 (−48.8%)**. Ghi chú cũ scope `.bss` là "RFC-scale" đã tính trên con số 3.2MB mà **một nửa là bug 1 dòng**. **RFC 0030 SHIPPED trọn P1–P4** (`fbb70d0` align, `5660034` .bss PE/COFF, + ELF): image KHÔNG cần section thứ 4 (chỉ `VirtualSize > SizeOfRawData` / `p_memsz > p_filesz`), nhưng OBJECT thì CẦN vì linker thấy global là khối byte mờ. `[i64;200000]`: **3.278.336 → 78.336 (−97,6%)**; delta chỉ còn 512B (PE) / 648B (ELF), KHÔNG còn tỉ lệ với storage. ⚠️ 2 defect mà size-check KHÔNG thấy: (1) bộ lọc `section != 3` ở linker.ax ~1953/~2067 để lọt symbol `.bss` → coi offset là TEXT offset → mọi `OP_GLOBAL_ADDR` trỏ vào `.text` → SIGSEGV **trong khi exe đã đúng kích thước**; (2) `.bss` là section **3** chứ không phải 4 khi MỌI global đều zero-init. ELF hardcode index 1–6 nên `.bss` phải **append ở index 7**, linker normalize theo TÊN về section logic 4. Guard mới `scripts/exe_size_check.sh` (budget siết còn 8KB) + row `t_bssglobal` ở cả regression lẫn elf_linux_check. STATE cũ ↓.
- [🟢 STATE 2026-07-22d — TIER-1 BACKLOG EMPTY](bug-two-array-payload-instantiations.md) — **ĐỌC ĐẦU TIÊN**. HEAD `bf67c7d`, driver `axc_native` = **`44688E182D4C0084`**, **508/508** (từ 501). User: "giải quyết dứt điểm #1, #2, #3" — cả 3 đóng. **#1** (two array-payload Option instantiations SIGSEGV) = **mangled-name collision**: `get_type_name_recursive` (mono.ax) đọc `entry.name_id` như tên, nhưng với ARRAY đó là **LENGTH** → mọi array cùng length mangle ra CÙNG tên (`[i32;2]` và `[i64;2]` đều ra `Box__T`) → 2 instantiation DÙNG CHUNG 1 mono type. **Thứ BA** của footgun này (sau `82d0565`, `b966f73`). **#2** (narrow-element array payload) = **note STALE** — đã fix từ `82d0565`, đo lại = 48 đúng. **#3** (`Box[[i32;3]](v: ..)` parse error) = parser led `[` nay parse TYPE khi có `;` ở depth 1. Kèm 1 silent-miscompile mới tìm ra: generic-ctor array element WIDTH (`let b: Box[[i64;2]] = Box(v:[5,6])` → 6 thay vì 11). ⚠️ **BÀI HỌC**: 2 giả thuyết trước đó bị "REJECTED" **SAI** — collision có thật, chỉ là nó **vô hại khi layout trùng nhau**; "dự đoán collision mà không crash" KHÔNG bác bỏ được giả thuyết. 1 lần trace tên mangled giải quyết thứ mà bảng hành vi không giải quyết được. Oracles t_twoarraypayload(104)+t_arrayctorgeneric(80). STATE cũ ↓.
- [🟢 STATE 2026-07-22c — RFC 0028 CLOSED (B3)](rfc0028-closed-2026-07-22.md) — **ĐỌC ĐẦU TIÊN**. HEAD sau `7962784`, driver `axc_native` = **`57738F36C9A0F262`**, **485/485** (từ 481). User: "thực hiện dứt điểm b3". Ship **§7e if/elif-chain compare-tree** (`try_lower_if_chain_bsearch`+`emit_bsearch_blocks`, opt-in `-jumptable`): nhận cả **or-arm** `x==A or x==B` (nhiều value → MỘT block, body lower 1 lần), **named const** RHS (`op == OP_IADD`), **unsigned** scrutinee (sort unsigned khớp CC_B), threshold **8**. Oracle t_ifchainjt(53). ⭐ **QUYẾT ĐỊNH ĐO ĐƯỢC: `-jumptable` GIỮ OPT-IN, KHÔNG default-on** — match 1.38x, if-chain **1.47x** trên user program, nhưng compiler self-build **−0.8% (ZERO)**: chain dài nhất của compiler xen kẽ clause `and`-guard (`inst.opcode == OP_CALL and inst.src1 == 0`) làm hỏng cả chain, và self-build KHÔNG dispatch-bound. §7e đoán "WOULD speed the compiler" là **SAI, đã bác bỏ bằng số**. Cũng KHAI TỬ luôn biến thể O(1) `.rodata`+reloc (§3–§7c): log-factor ko phải giới hạn. Self-host an toàn: compiler build CÓ `-jumptable` cho output **byte-identical** với bản không. ⚠️ **2 bug mà TEST KHÔNG BẮT ĐƯỢC**: (1) rewrite **im lặng KHÔNG BAO GIỜ chạy** — pass 1 dùng chung 1 `scratch` cho mọi clause nên slot 0 mang value clause CUỐI → trùng → dup-guard loại mọi chain; **mọi oracle vẫn PASS** (fallback linear luôn đúng), chỉ so `linear_hash != jt_hash` mới lộ → **optimization pass test ≠ optimization ĐÃ CHẠY, phải assert output ĐỔI**; (2) **bail SAU khi `fb.new_block()` = leak block** vĩnh viễn → cộng với bug (1) làm rò hàng ngàn block chết, vỡ **trần allocator 256MB** (`SEGMENT_SIZE 65536 * MAX_SEGMENTS 4096`) → OOM func ~950/983; **suýt "sửa" nhầm bằng cách nâng threshold** — thực ra chỉ cần mint block SAU mọi bail check. STATE cũ ↓.
- [🟢 STATE 2026-07-22b — GROUP A CLOSED](backlog-group-a-closed-2026-07-22.md) — **ĐỌC ĐẦU TIÊN**. HEAD=**`a40f3e2`**, driver `axc_native` = **`4043C7B942EBB203`**, **481/481** (từ 474). User: "thực hiện #A, dứt điểm" → cả 6 item đóng. Ship: **A3** `to_hex_upper` + **A6** RFC 0020 §4 (`is_ascii`/`to_ascii_*`/`char_indices`/`to_utf16`/`from_utf16`/`to_utf32`/`from_utf32`) `485d02f`; **A4** RFC 0022 P5 nested pattern `let ((a,b),c)` + `for (a,b) in v` `e4bea71`; **A1** Option/Result payload-width reject `171ea83`; **A2** interface GLOBAL `a40f3e2` (A==B==C). **A5**: uninit-decl + ELF `.data` ĐÃ chạy sẵn (note cũ STALE) — chỉ bank oracle `a1ce9f9`; **`.bss` RECLASSIFIED = RFC-scale linker work** (section thứ 4 COFF+ELF+self-linker image layout), KHÔNG phải residual. ⭐ **PROBE-FOUND miscompile `19c1ab1`**: `match` trên scrutinee AGGREGATE (struct/tuple/array) hoặc str/float lower ra RỖNG → garbage RAX (`match p: _: return p.x` trả 0 thay 42); reject `07139af` cũ chỉ bắt LITERAL pattern nên `_`/binding arm lọt. Nay reject ở mức SCRUTINEE. ⚠️ Bài học A1 (4 cycle): param tới dạng kind **SUM** còn local unannotated là **GENERIC_INST** — phải đo CẢ HAI; và 2 nhánh đo 2 ĐẠI LƯỢNG KHÁC NHAU (variant-payload vs type-arg) chỉ trùng nhau ở Option/Result → `Tree[T]=Node(T,Tree,Tree)` lệch 24 vs 8 làm over-reject t_gentree (regression bắt). **So payload SIZE, đừng bao giờ so type_id.** STATE cũ ↓.
- [🟢 STATE 2026-07-22 (autopilot)](session-handoff-2026-07-09.md) — **ĐỌC ĐẦU TIÊN**. HEAD=**`770dfc3`**, daily driver `axc_native` = **`2FC562C7`**, **474/474**. Session: user chỉ định "dứt điểm #1" = RFC 0029 interface vtable. **ĐÓNG TRỌN họ T→I coercion**: dispatch (`af26946`) trước đây CHỈ box ở call-arg; mọi site khác (let / return / struct-field / assign / array-elem) lưu raw struct → **SIGSEGV**. Nay 1 helper chung `coerce_struct_to_interface` wired 7 site + `check_iface_conformance` reject ở let/return/ctor-field → interface dùng được như **VALUE type** (dạng `Box[Interface]`). Interface global vẫn clean-REJECT (RFC 0017 gap, ko phải miscompile). **Shared static vtables = WON'T-DO** (định lượng: N=1 tiết kiệm 0, N=2 một store; đổi lại thêm 1 load mỗi dispatch + global mutable vtable registry §3 + gate A==B→B==C). std/log `Box[LogSink]` KHÔNG phải blocker vtable mà là milestone rewrite aspirational-dialect; consumer thật chứng minh bằng oracle t_ifaceconsumer(46). Oracles mới: t_ifaceconsumer(46)/t_ifaceassign(75)/t_ifacearray(43)/t_ifacefieldconf(reject). Chi tiết [[rfc0029-vtable-progress]]. ⚠️ Bài học: **git status đầu phiên có thể bỏ sót in-flight work** — air_builder.ax đã có sẵn coercion let/return/field CHƯA COMMIT từ phiên trước, và daily driver thì CŨ hơn diff đó → probe ban đầu segfault gây hiểu nhầm "chưa làm". LUÔN `git diff` + rebuild driver trước khi kết luận. STATE cũ ↓.
- [🟢 STATE 2026-07-20c (autopilot)](session-handoff-2026-07-09.md) — HEAD=**`f509506`**, daily driver `axc_native` = **`DF48C954`**, **470/470**. Đóng gần trọn họ width-coerce (residual #1/#2/#3): **#1 Result-Ok-tuple FIXED** `3f7c171` (prefer concrete-variant-payload over mangled exp_args); **#2 inline-param coerce FIXED** `f509506` (`use_it(Some((20,22)))` → thread param Option/Result type tới ctor arg) — nhánh **pp_var** (biến đã build 8B truyền vào param 16B) reject BỊ HOÃN (reject rộng over-reject vì cùng Option[i64] có nhiều type_id; cần so payload-SIZE); **#3 g_annot ĐÃ tự fix** collateral qua Vec-push option B, banked t_vecoptvar. Oracles t_vecoptpush/t_optparamcoerce/t_vecoptvar. Chi tiết [[bug-unannotated-some-aggregate-match]]. STATE cũ ↓.
- [🟢 STATE 2026-07-20b (autopilot)](session-handoff-2026-07-09.md) — HEAD=**`f301ae9`**, daily driver `axc_native` = **`61D345BD`**, **468/468**. Tiếp nối phiên 2026-07-20: (3) **struct-field width-coerce FIXED** `6132b15` (thread field type→variant-ctor value); (4) **Vec-push mono coercion FIXED** `f301ae9` (option B — flagged-hard case đã giải: generic-method-arg re-infer + concrete-sum variant-payload hint fallback trong try_instantiate_variant_call; A/B đã revert trước đó). `Vec[Option[(i64,i64)]].push(Some((20,22)))` gen3 20→42, Option multi-push, Result-Err-tuple. Oracle t_vecoptpush(42). Residual hẹp còn: Result-**Ok**-tuple (tuple ở param ĐẦU, vp_res=20, get_generic_args ordering mismatch 2-param) + param-passing pp (cần REJECT). Chi tiết [[bug-unannotated-some-aggregate-match]]. STATE cũ ↓.
- [🟢 STATE 2026-07-20 (autopilot)](session-handoff-2026-07-09.md) — HEAD=**`a5c410f`**, daily driver `axc_native` = **`93D1B4B1`**, **465/465**. Session: (1) **re-validated CTGC P3** — general-free (`18db268`) + container free-glue (`10eceb6`) đã SHIPPED & holding (fixpoint 6BF63ABD, ctgc_free_check 12/12, regression 464/464); chỉ còn RFC 0027 **path D** (user-container field-ownership annotations) cần design. (2) **PROBE-FOUND + FIXED silent-miscompile** (`56cab6e` tagged Option/Result path + `a5c410f` linear user-generic-sum path): unannotated `let x = Some(<agg>)` / `Wrap(<agg>)` + match đọc mọi field = field 0 (GENERIC_INST scrutinee bind payload var vào template generic param T) — 2-part fix (typecheck `subst_variant_payload_type` + air_builder deref/getfield-type ưu tiên concrete bound-var type), backend B==C `93D1B4B1`, oracle t_optmatchagg(57). Chi tiết [[bug-unannotated-some-aggregate-match]]. ~~Residual OPEN: unannotated `Some((int,int))` không width-coerce~~ → **ĐÃ ĐÓNG, re-probe 2026-07-23** (`171ea83` payload-width reject đã xử lý; note này STALE, ĐỪNG đuổi lại). Đo lại O0–O3: constructor thẳng vào param typed = **đúng**; qua Vec[Option[(i64,i64)]] element = **đúng**; chỉ shape thật sự không suy diễn được (`let v = Some((3,4))` rồi `take(v)`) là **REJECT sạch kèm diagnostic chỉ đúng cách sửa** — tức wrong-answer đã thành compile error, không còn silent miscompile. Oracle `t_optuptuplewiden`(42) pin 2 shape CHẠY được (shape reject cố ý không đưa vào: chương trình phải fail-to-compile thì không thể đồng thời là exit-code oracle). Backlog lớn còn lại cần user chỉ đạo: shared vtable perf (đóng RFC 0029), M6 perf/fib recursion→loop, RFC 0028 jump-table benchmark, async/Mach-O. Gate cmd ở STATE cũ bên dưới.
- [✅ unannotated Some/Ok/Wrap(aggregate) match-payload field-0 bug — FIXED 56cab6e+a5c410f](bug-unannotated-some-aggregate-match.md) — GENERIC_INST match scrutinee bound payload var to template generic param T → all fields read field 0 (16B: field1=field0; 8B multifield: field1=0). Fix cả tagged (Option/Result) lẫn linear (user generic sum) path: typecheck subst + air_builder deref/getfield-type prefer concrete bound-var type. A==B==C 93D1B4B1, 465/465, oracle t_optmatchagg(57). Residual width-coerce follow-up open.
- [✅ RFC 0029 interface dynamic dispatch — SHIPPED (closes BUG#71) + conformance diagnostic](rfc0029-vtable-progress.md) — dispatch `af26946`; structural-conformance reject `7858aa5` (driver `6bf63abd`, **464/464**). `fn f(s: Shape): s.area()` now dispatches through a per-value inline vtable (interface value = 8-byte ptr to heap box `{data, m0..m(N-1)}`, built at T→I coercion via OP_ALLOC+OP_SET_FIELD+OP_FUNC_ADDR, dispatched via OP_GET_FIELD+OP_CALL callee_reg). NO linker/reloc/COFF/ELF changes (runtime-inline design). Inert on compiler self-build → A==B. Oracle t_ifacedispatch(37); multi-method+args=51; O0-O3 clean. Remaining (optional): shared static vtables (perf), structural-conformance diagnostic, std/log `Box[LogSink]` rewrite as the real consumer.
- [🟢 STATE 2026-07-19b (autopilot)](session-handoff-2026-07-09.md) — **ĐỌC ĐẦU TIÊN** (mới nhất). HEAD=**`6323e56`**, daily driver `axc_native` fixpoint **`2965E009`**, **462/462**. Session này ĐÓNG TRỌN họ **match literal-pattern**: (1) **negative match-arm literals** `match n: -5:` SHIPPED `cbc4fdd` (sign mang trong NODE_LITERAL_PAT PAYLOAD — né blocker "hết flag bit"; bug thật là clamp `variant_tag<0→0` cũ zero hóa số âm); (2) **bool/char literal-match VALUES** fixed `35d6655` (`parse_int_from_str("true"/"'a'")`=0 cho mọi arm → miscompile; helper `pat_literal_value` true→1/false→0/char-codepoint); (3) **float/string/bytes literal-match → REJECT** `07139af` (was silent accept-then-miscompile, BUG#53); (4) jump-table deep oracle t_jumptable3 + RFC 0028 edge-equivalence findings `9207296`. Probe xác nhận: or-pattern/range-pattern/guard = clean parse-REJECT (chưa impl, ko miscompile); strings-in-loop, 3-level generics = OK/clean-reject. Chi tiết [[backlog-open-items]]. **Backlog còn lại toàn item LỚN cần user chỉ đạo**: interface vtable dynamic dispatch (BUG#71 — cần RFC chọn ABI fat-ptr vs mono), RFC 0028 if-chain recognition (cần benchmark, §10), jump-table default-on (product decision), Vec HOF for_each/reduce (cần capturing closures), 3-HashMap teardown (mitigated, cần AX_CANARY session), async/ELF-thread/Mach-O. Gate cmd + STATE cũ bên dưới.
- [🟢 STATE 2026-07-19 (autopilot)](session-handoff-2026-07-09.md) — HEAD=**`ccfd062`**, daily driver `axc_native` = **`9A178747`** (CTGC-activated + container free-glue), **436/436**, ctgc 12/12. Session shipped: (1) 3-hashmap teardown SIGSEGV FIXED `cebea3f` (self-link early-exit; 0/160 stress; AX_CANARY infra committed disabled) [[bug-3hashmap-mono-teardown-crash]]; (2) CTGC P3 general-free ACTIVATED `18db268` + **RFC 0027 container free-glue `10eceb6`** (frees Vec/HashMap/HashSet buffers; opt-in `-ctgc-free`; sweep clean, no UAF) [[ctgc-p3-scoping-2026-07-18]]; (3) m2b shadow deferred `b97b1b3` (arity-spare load-bearing). Older STATE below.
- [🟢 STATE 2026-07-18 (autopilot)](session-handoff-2026-07-09.md) — HEAD=**`d4285b0`** (code fixpoint `11EB77BD`), **435/435**, daily driver `axc_native` fixpoint `11EB77BD`. ⭐ **SESSION 2026-07-18 (CTGC P3): SHIPPED `-ctgc-free-report` + TWO escape-analysis soundness fixes → compiler freeable set 6→1→0. Then ATTEMPTED general-free activation → gate RED → REVERTED.** Activation (lower_destroy: plain OP_DESTROY for non-drop non-escaping owned locals) was self-host-clean (A==B==C `28DCDE0A`, freeable=0, 435/435, ctgc_free_check 10/10) BUT the broad `-ctgc-free` sweep over 401 programs caught **t_hashi64 crashing the COMPILER** during `-ctgc-free` codegen (HashMap[K,V] locals = non-escaping owned generic-inst aggregates; OP_DESTROY on a container that owns nested heap chokes codegen). Reverted cleanly (revert-on-red). ⚠️ **CORRECTION (later same session): the sweep crash was a RED HERRING** — `t_hashi64` crashes ~15% on a NORMAL build too (flag-independent), it's a PRE-EXISTING low-severity bug: 3+ HashMap monomorphizations → intermittent TEARDOWN segfault (output exe always correct, exit 0) [[bug-3hashmap-mono-teardown-crash]]. So the "OP_DESTROY free-glue codegen crash" attribution was WRONG; **CTGC activation status is INCONCLUSIVE not blocked** — it passed every deterministic gate (A==B==C `28DCDE0A`, freeable=0, 435/435, ctgc_free_check 10/10), only red-flag was the unrelated flake. Escape-soundness half COMPLETE+shipped; activation re-validation just needs the 3-HashMap teardown flake fixed/discounted first. Full detail [[ctgc-p3-scoping-2026-07-18]].** Both escape holes behind the historic `-ctgc-free` self-build UAF now CLOSED: (1) container-store escape (`(&mod.exports).push(v_exp)` — bare call-stmt args never flowed to escape; f873948, 6→1); (2) reassign-to-borrow (`dst_alloc = allocs[v]` — ctor-local reassigned to an INDEX borrow stayed "owning"; 68d2c78, 1→0). Escape analyser now provably conservative-sound on the whole self-host build (freeable=0). `-ctgc-free` self-build reproduces fixpoint; 9 ctgc-free oracles still free legit single-owner locals (CTGC_FREE_OK). Remaining activation lever (remove drop-narrowing air_builder.ax:4225) is now LOW-risk but self-host-inert (freeable=0) — a future opt-in for non-drop user aggregates. Full detail [[ctgc-p3-scoping-2026-07-18]]. Earlier this session: SHIPPED `-ctgc-free-report` + escape-analysis container-store fix.** Then used the report to enumerate the P3 blocker (freeable set = 6 locals) and root-cause it: a bare call statement `(&mod.exports).push(v_exp)` is an unwrapped NODE_CALL_EXPR at stmt position, and `analyze_stmt` (escape.ax) had NO case for it → args never flowed to escape → ctor-then-pushed locals wrongly freeable (UAF at `-ctgc-free` activation). Fix = one `elif kind==NODE_CALL_EXPR` routing to `analyze_expr(…,escape_node)`. **Freeable set 6→1** (only `dst_alloc` genuine local scratch remains); inert (A==B `48E17C7B`, 434/434, CTGC_FREE_OK 9/9). Closes the "un-enumerated pervasive aliasing" framing — real blocker was 5 container-store escapes from 1 dispatch hole. Details [[ctgc-p3-scoping-2026-07-18]]. Earlier this session: SHIPPED `-ctgc-free-report` — dump-only freeable-set enumeration, precursor to RFC 0015 P3. Activation itself DEFERRED (un-enumerated missed borrow-locals UAF-segfault a `-ctgc-free` self-build; the inert marking substrate already shipped as P2.1 — see [[ctgc-p3-scoping-2026-07-18]]). New flag `-ctgc-free-report` traverses like the free pass but PRINTS `[ctgc-freeable] <name>` per non-escaping owning heap local and injects NO `OP_DESTROY` → default build byte-identical (A==B `01AD798F`), report-flag build byte-identical to no-flag (proven via `cmp`). Converts the P3 blocker's "un-enumerated missed borrows" into an inspectable artifact for the future activation session. 3 sites: ctgc.ax (report_only field + run-guard + emit branch), main_air.ax (flag parse + wire). Oracle t_ctgcreport(20). Prev ⭐ **(fib+m2b): NO code shipped — 2 findings recorded, tree CLEAN at HEAD.** (1) **fib tree-recursion→loop DEFERRED definitively** (rationale at top of [[m6-perf-gate-fib-benchmark]]): tree-recursion needs an accumulator/recurrence transform whose call reassociation is only sound for provably-pure callees → high B==C self-host risk for narrow single-benchmark ROI; §10 says don't force it. Not "todo" — "deferred until a dedicated, user-prioritized perf session." (2) **m2b arity-match shadow ROOT-CAUSED** [[bug-malformed-input-robustness-cluster]]: the arity-spare is LOAD-BEARING — an unconditional reject of `twice(20)`-style scalar-var-shadow calls BREAKS self-build (rejects the compiler's own 5 `let len = std.string.len(s)` self-reference sites: a local is wrongly in scope during its own initializer, so `len(s)` binds to the LOCAL at typecheck but to `fn len` at air_builder). Real fixes: (a) resolver hardening (local NOT in scope during own initializer) or (b) air_builder rebind scalar-shadow→fn; both dedicated gated sessions, LOW priority. ⚠️ **CRITICAL GATE LESSON:** `fast_fixpoint.ps1` reported a false `SUCCESS: A==B` from a STALE leftover `fpB.exe` when A's build of B actually FAILED (rejected the source) — tell = B-hash==seed-hash EXACTLY + 2-cycle `B!=C`. ALWAYS `Remove-Item bin/axc_fp{A,B,C}.exe` first + grep B log for `aborting before code generation`. ⭐ **RFC 0026 getter/index multi-block inlining SHIPPED (`d94b3b3`)** — inline branchy callees reading a scalar field/element of an aggregate param (clampb b.lo/b.hi in ifs); cfgetter +12%. **The earlier "deferred due to regression stall" was a DEFENDER MISDIAGNOSIS** (re-ran 430/430 in 181s under fast builds). LESSON: a gate STALL ≠ failure — check Defender/load first. ⭐ **RFC 0026 §2b TAIL-SELF-RECURSION→LOOP SHIPPED (`7b4aaa1`)** — `selfrec_to_loop` rewrites `return self(args)` → staged param reassignment + `jump block_0` (in-function loop). Needs **option-B codegen move** (`emit_param_prologue` BEFORE `.L_b_0` label → block 0 re-entrant). 3 subtleties: run LAST (copy_prop collapse), NO contiguity guard (tail block low-index/emitted-last), two-phase staging (swaps like gcd). Perf tailrec 1214→246ms=**4.9x**. Oracle t_selfrec(67). fib tree-recursion still out of scope. Details [[m6-perf-gate-fib-benchmark]]. Prev: **RFC 0026 P2 CONTROL-FLOW (multi-block) INLINER SHIPPED (`6b40dec`+loop-guard `12f39f2`, C22EED17)** — `inline_multiblock_func` clones a small LOOP-FREE multi-block callee (if/else) into its caller in **reverse-post-order** (RPO), + reusable `recompute_cfg(f)` groundwork (ordering PROVEN exact via unconditional-rebuild B==C). 2 bring-up lessons: (1) clone-in-index-order put a merge/exit block before its preds → miscompiled float fns (t_mathfill/t_fft) at -O1 **while B==C held** → fixed by RPO clone (**B==C necessary NOT sufficient, full user regression is the real gate**); (2) inlining a LOOPY callee pessimizes (collatz +11%, call amortized over callee iters + reg pressure) → `inl_has_loop` DFS back-edge guard skips them. Perf: cfhot (call-dominated 2-if callee) **15% faster**; collatz unchanged (loopy → skipped); fib needs accumulator-recursion→loop (remaining P2). Oracle t_inlinecf(17). Details [[m6-perf-gate-fib-benchmark]]. Prev `25b44df` 400/400 `22DA5200`. m2b PARTIAL FIX: shadow-call sai arity (`fn helper(); helper(0)`) nay REJECT (arity-aware guard; trace xác nhận 5 false-pos `len` = local `let len=std.string.len(s)`, spared vì 1-arg/1-param). CÒN OPEN: arity-MATCH shadow (`fn twice(n); let twice; twice(20)`) vẫn segfault (air_builder bind local, cần resolver hardening; low-pri). Oracle t_shadowcall. ⭐ **2 bug bound ĐÓNG DỨT ĐIỂM (2026-07-18):** (1) sum-of-primitives `type ID=i32|string` → REJECT declaration-level, GATED variant_count>1 (single alias `type X=i64` KHÔNG bị reject) [[bug-sum-of-primitives-match-segfault]] (t_sumprimmatch); (2) m2 gọi non-fn `x(3)` → REJECT receiver-agnostic [[bug-malformed-input-robustness-cluster]] (t_callnonfn) + guard t_fnptrcall (fn-ptr call vẫn chạy). Boundary probe: sum payload-less/enum/fn-ptr đều OK. ⚠️ Residual RARE **m2b OPEN**: local var shadow fn rồi gọi → vẫn segfault (guard tha same-named fn); low-pri. Prev: `61768b5`, 396/396 (`1C2E3D6A`). +oracle t_optstructpay (Option-struct-payload across boundary). ⚠️ **GATE FIX (`61768b5`):** regression_repros.sh giờ DEFAULT `AXC=bin/axc_native.exe` (trước default stale `axc_stage1` = seed cũ → miscompile aggregate → spurious SIGSEGV "fail"; đó KHÔNG phải regression) + build vào `$REGTMP` (default /tmp; đặt `REGTMP=bin/_regtmp` trên box Defender vì bin/ excluded → ~1s/build thay vì ~99s). User đã thêm bin/ + bootstrap/stage1/ vào Defender exclusion → gate lại nhanh, unblock m2. Lệnh gate: `REGTMP=bin/_regtmp bash scripts/regression_repros.sh`. Chi tiết [[infra-defender-build-throttle]]. +FIX condition-type: `if o`/`while o`/`elif o` với o là string/bytes hoặc aggregate (struct/sum/Option/Result/array) → reject "forgot .is_some()?" (was accept-then-miscompile; helper `check_cond_type` wired 3 site NODE_IF/ELIF/WHILE; SCALAR-truthy giữ nguyên vì compiler source dùng 11 site truthy `if <i64>` → chỉ non-scalar bị bắt, self-build-safe; oracle t_condagg reject). Probe casts (f64↔int/u64→u8/f32→i64/neg-i64→u32) SẠCH — banked t_castneg2u. str↔numeric mismatch family ĐÓNG HOÀN TOÀN 8 path: let/return/binop/comparison/argument/assignment/array-element/**ctor-field**(m3 `P(x:"hi")`, t_ctorfieldstrnum, gated same-tree+literal). +FIX m2: array literal trộn string+numeric `[1,2,"three"]` → reject (t_arrmixstrnum). +FIX m5agg: `return p` (p struct) từ fn `->i64` → reject (aggregate↔scalar, gated NODE_IDENT; oracle t_retagg). str↔numeric family đóng 6 path: let/return/binop/comparison/argument/assignment (t_assignstrnum literal-gated). +FIX index-a-scalar-variable `x[0]` (x:i64)→reject (was OP_INDEX→segfault; GATED collection==NODE_IDENT vì infer_node trả i64 imprecise cho CALL-collection — bẫy m2/m5 lại; oracle t_scalaridx). SESSION 2026-07-18 malformed-input pass — str↔numeric mismatch family ĐÓNG toàn bộ 5 path: let(m5)/return(t_retmismatch)/binop(t_strnumop)/comparison(t_cmpstrnum)/argument(t_argstrnum) — tất cả literal-gated reject, str+str concat & numeric giữ nguyên. + undefined-name accept→reject [[bug-undefined-name-accept]] (t_undefname). Tất cả A==B, regression sạch. NOT-bug: missing struct field=zero-fill, generic-arg-infer lenient. Trước đó: FIX return-type mismatch `return "hello"` từ fn `->i64` → reject (analog m5, literal-gated str↔numeric; oracle t_retmismatch). Trước đó: FIX undefined-name silently-accepted → reject [[bug-undefined-name-accept]] (`return undeclared_x` build+chạy=garbage; resolver để raw name_id làm payload → index nhầm symbol. Fix: resolver set payload=0 khi unresolved + typecheck reject NODE_IDENT UNKNOWN+payload==0. Bounded-safe: TRACE thấy compiler source ZERO unresolved. Oracle t_undefname reject). Trước đó backend fold [[bug-const-fold-narrow-int-wrap]]. Session 2026-07-18 probe SWEEP (8 area, ĐỪNG lặp): narrow-int width (fold+aggregate) FIXED+covered [[bug-const-fold-narrow-int-wrap]]; strength-reduction/signed-div-mod/UTF-8-string/defer/control-flow/generic-mono-heterogeneous/nested-16B-aggregate-return đều SẠCH. Oracles mới: t_foldu32wrap/t_foldi8wrap/t_u32fieldwrap/t_genswaphet/t_deepnestmut/t_optvecnest/t_vectupmix. Compiler MATURE PLATEAU — backlog còn lại design-level (m2 fn-ptr type, milestones M4/M6) cần user chỉ đạo. Pitfall probe: exit code bash 8-bit + NEGATIVE→127 clamp (dùng oracle 0..255 dương). Mới nhất: FIX narrow-int CONST-FOLD wrap (`u32 4e9+5e8` fold giữ 4.5e9 thay vì wrap 205032704 — O0-vs-O1 miscompile; u16/u32/i32 add/mul) [[bug-const-fold-narrow-int-wrap]]. ⚠️ GUARD operand-fit: AIR `type_id` KHÔNG tin được (negated i64::MIN mang tid=i32 → mask thô hóa 0, vỡ t_tostr); chỉ wrap khi operand vừa width. Oracle t_foldu32wrap(205). BÀI HỌC: A==B ≠ correctness — full regression bắt vụ to_str. Trước đó: CLOSE stale Vec-of-tuple backlog (re-probe full matrix ALL pass — fixed bởi aa419a2; oracle t_vectupmix 47) [[backlog-open-items]]. Trước đó: probe session 2026-07-18 CLEAN (0 bug thật, ~22 cross O0-O3, banked t_optvecnest) [[probe-batch-1012-clean-2026-07-18]] + defer m2 (fn-ptr type/inference = hard blocker). Trước đó: FIX m5 `let x:i64="hello"` accept-then-garbage → reject [[bug-malformed-input-robustness-cluster]] (gate trên RHS **literal node kind**, KHÔNG trust `inferred`: infer_node trả i64 cho str-returning CALL → attempt#1 over-reject 3 site compiler source A!=B; oracle t_letstrmismatch). Cluster giờ m1✅m4✅m5✅, chỉ m2 OPEN. Trước đó: FIX m1 recursive-struct COMPILER CRASH → reject (oracle t_recstructreject); trước đó FIX generic-fn float-RETURN miscompile [[bug-generic-fn-float-return-default-int]] (caller đọc TEMPLATE signature thay vì mono INSTANCE; dùng `symbols[callee.payload].type_id` khi flag 2048) + oracle t_genfloatret. Trước đó cùng session `32F4DF0E`/367: Session 2026-07-18: (1) `89a2b8d` REVERT inert immediate-operand folding + bank [[perf-immediate-operand-folding-inert.md]] (const_imm32 dừng ở OP_CAST; sound fix = M6 session riêng) + oracle t_immfold; (2) `4aa4868` FIX None-first generic-inference segfault [[bug-none-first-generic-inference]] (concrete arg override generic binding) + oracle t_noneinfer; (3) `2438ba0` FIX un-inferable generic param trong CONCRETE context → REJECT sạch (was segfault; `current_fn_is_generic` discriminator ở NODE_FUNC_DECL + defer site) + oracle t_uninferreject (reject mode). **None cluster ĐÓNG HOÀN TOÀN** — vG "residual" là ĐỌC NHẦM exit code (999 & 0xFF = 231; None round-trip đúng). ⚠️ Bài học: exit code bash = 8-bit, giữ oracle < 256. Trước đó: **`f885c75` RFC 0025 follow-up — LICM invariant-CHAIN hoisting (copy_prop trước licm) + sound LOOP UNROLL re-enabled** (persistent `cur[]` threading + conservative gates; đóng loop_unroll của [[bug-o2-large-code-miscompile]]); trước đó `bd6616e` **RFC 0025 model-A LICM re-enabled** (blocker-1 "RFC 0016 liveness" = misdiagnosis). Chi tiết + gate + oracles ở [[rfc0025-licm-shipped]]. **GATE quyết định cho loop pass = build compiler AT -O2 rồi regression trên -O2-built compiler** (self-build -O1 mù với LICM/unroll). Lịch sử session: `570d5cb` disable loop_unroll (nay re-enabled), `3703d52` -O2/-O3 loop crash fix [[bug-o2-o3-loop-compile-crash]], `38a9d81` div/pow2 strength-reduction [[perf-div-pow2-strength-reduction]], M6 perf (`a4784ee` compare-branch fusion + `50796cc` move-coalescing) [[m6-perf-gate-fib-benchmark]]. LESSON: **backend change → B==C KHÔNG đủ, full regression + -O2 acceptance bắt buộc** (opcode collision, unroll body-shape bug chỉ -O2-built compiler bắt).
  **Gate cmd:** `& scripts/fast_fixpoint.ps1`(A==B) → `cp bin/axc_fpB.exe bin/axc_native.exe` → `AXC=bin/axc_native.exe bash scripts/regression_repros.sh`; loop/opt pass thêm: build `-self-link -O2` → `AXC=bin/axc_O2built.exe bash scripts/regression_repros.sh`. Linux: `scripts/elf_linux_check.sh`. Edit module→regen tự động (ĐỪNG sửa tmp_concatenated); frontend=A==B, backend/ABI/linker=B==C. Lambda=`|x: T| -> R expr`.
- [✅ RFC 0025 LICM + loop-unroll — SHIPPED](rfc0025-licm-shipped.md) — model-A LICM (`bd6616e`) + chain-hoist & sound loop-unroll (`f885c75`) re-enabled -O2/-O3. Gate = -O2-built-compiler regression. Blocker-1 "RFC 0016 liveness" = misdiagnosis. Oracles t_licmhoist/t_loopcross/t_licmchain/t_licmunroll.
- [✅ -O2 LARGE-code miscompile — loop_unroll (nay RE-ENABLED)](bug-o2-large-code-miscompile.md) — root = loop_unroll per-iter rename-reset (acc đọc giá trị gốc); fixed persistent cur[] + gates ở [[rfc0025-licm-shipped]]. `570d5cb` từng disable, `f885c75` re-enable.
- [✅ -O2/-O3 loop compile crash — FIXED `3703d52`](bug-o2-o3-loop-compile-crash.md) — insert_inst_at physical-order + ICONST src2 guard (shared bởi strength/unroll). licm_func từng disable ở đây, nay re-enabled.
- [⭐ div/mod-by-pow2 strength-reduction `38a9d81`](perf-div-pow2-strength-reduction.md) — idiv→shift, collatz 10.5x→2.89x. non-SSA multi-def divisor + RAX/RDX phys + opcode collision. Oracle t_divpow2.
  **NEXT backlog (large, cần user chỉ định):** div-pow2 signed self-host, fib recursion→loop (RFC), async/spawn-await (cross-platform incomplete, hỏng cả Win+Linux), macOS/Mach-O, RFC 0015 P3 CTGC-free, RFC 0014 bignum-leak, LICM chaining model-B (reaching-defs). Tick-sized: INTRINSIC-not-lowered audit (std/sync.ax native path trả garbage), Vec HOF for_each/reduce/zip. Gate cmd ở STATE line trên.
- [✅ Windows to_i64/to_f64 panic FIXED `4cf8239`](bug-windows-to-i64-panic.md) — reimpl pure-AXIOM parse trong std/string.ax (bỏ extern C ax_str_parse_* hỏng, class BUG#29). Fix cả Windows lẫn Linux.
- [📊 M6 perf gate: Fib 2.44x vs clang -O2](m6-perf-gate-fib-benchmark.md) — Fib(42) 2379ms vs clang 974ms, gate ≤1.05x CHƯA đạt; perf half = optimizer effort lớn (inline/recursion→loop). §10 ĐỪNG optimize blind.
- [✅ None-first generic-inference segfault FIXED (+un-inferable→reject)](bug-none-first-generic-inference.md) — `pick(None, Some(42))` (generic `[T]`, bare None + concrete sibling) mis-monomorphized → segfault; None=Option TEMPLATE poisoned T qua first-wins. Fix1 `4aa4868`: concrete arg override generic binding (oracle t_noneinfer). Fix2 `2438ba0`: un-inferable param trong CONCRETE context (s3/n2) → REJECT sạch qua `current_fn_is_generic` (oracle t_uninferreject). **ĐÓNG HOÀN TOÀN** (vG là đọc nhầm exit code 999&0xFF=231, ko phải bug).
- [✅ malformed-input robustness cluster — ALL FIXED (CLOSED)](bug-malformed-input-robustness-cluster.md) — m2 `x(3)` gọi value-var nay REJECT (`'x' is not a function`; new NODE_IDENT elif trong call-path, gated SYM_VAR/PARAM/CONST + **receiver-agnostic**: chỉ reject khi KHÔNG có SYM_FUNC cùng tên → phân biệt `len` field-vs-`fn len`). A==B `EC01FECA`, oracle t_callnonfn. m1/m4/m5 fixed từ trước. Cũ: — probe batch 9 (bad-input pass). ✅ m1 `struct S: x:S` self-recursive by value → was COMPILER CRASH, nay REJECT (`pre_infer_struct` field type_id==struct type_id; oracle t_recstructreject). ✅ m4 `match i64 với Some/None` → REJECT (t_matchnonsum). CÒN OPEN: m2 gọi non-fn `x(3)` → accept+segfault; m5 `let x:i64="hello"` → accept+garbage (rủi ro over-reject, cần coercion predicate). Tất cả BUG#53, frontend=A==B. m6 KO phải bug.
- [✅ generic-fn float-RETURN → default int miscompile FIXED](bug-generic-fn-float-return-default-int.md) — `fn id[T](a:T)->T; id(3.5)` ko annotate → float return bị đọc thành int → `itof` bậy → rác. ROOT (trace-nailed): caller `infer_node(callee)` L3627 đọc TEMPLATE signature (ret=generic/0) thay vì INSTANCE (pre_infer đúng: f64=10). Fix: khi call đã mono (`callee.flags & 2048`) → dùng `symbols[callee.payload].type_id` thay vì infer_node. A==B `6F1A1ED5`, oracle t_genfloatret. Cũng sửa generic STRUCT return. Probe batch 6.
- [⚠️ immediate-operand folding INERT → REVERTED](perf-immediate-operand-folding-inert.md) — in-flight `cmp/add/sub reg,imm` fold (`const_imm32`) đã REVERT về `97A0703F`: inert vì `const_imm32` dừng ở `OP_CAST` (int-literal tới op qua widening cast; dump-air ẩn cast). Fix ngây thơ (follow OP_CAST) UNSOUND (narrowing `300 as u8`→t_castwidth 13≠15). Sound fix (value-preserving cast, 8-byte-int target) = M6-perf session riêng. Oracle `t_immfold` giữ lại.
- [🎉 RFC 0009 P3 Linux ELF SHIPPED](rfc0009-p3-elf-linux-target-wip.md) — `--target linux -self-link -O1` → runnable ELF 10/10 WSL. CÒN: actor/scheduler thread port, ax_time_now_ns. WSL run cmd + digs ở topic file.
- [✅ accept-then-miscompile cluster (5 fixes)](bug-accept-then-miscompile-cluster-0711.md) — undefined-method/non-exhaustive-enum/arity/field/Vec `v[i]` → clean REJECT (rejects phải receiver-AGNOSTIC). Vec `v[i]` shipped RFC 0021.
- [✅ variant-name shadow REJECT](bug92-generic-recursive-multifield-open.md) — sum reusing builtin variant name (`Pair[T]=P(T,T)|None`) miscompiled → REJECT in `pre_infer_type_alias`. `62c0619`.
- [✅ BUG#92 generic recursive multi-field FIXED](bug92-generic-recursive-multifield-open.md) — `Tree[T]=Node(T,Tree[T],Tree[T])` child field sai→segfault; `field_is_pointer_sum` nhận kind-8 generic_inst-of-SUM. `938c48b`. Oracle `t_gentree`.
- [✅ match Option[str]/Result[str] payload length FIXED](bug-match-optstr-payload-length.md) — match `str` payload dropped len half; `lower_match_tagged` pl_type=12. `6c0b976`. Oracle `t_optstrmatch`.
- [✅✅ free-fn/stdlib collision cluster CLOSED](bug-freefn-stdlib-collision-noarg.md) — user fn trùng stdlib `ax_<name>` → link-merge SIGSEGV; fix arity+arg0 disambig + MODDUP mangle. `f7bc186`.
- [✅ array element-coercion cluster FIXED](bug-array-return-type-dropped.md) — int-array-lit element coercion trọn cụm (param/return/for-in/let/ctor-field): [i32;N]→[i64;N] stride; `c103b9f`.
- [⚠️ string = UTF-8 default](string-utf8-default.md) — `str` UTF-8; `for c in s` theo codepoint (rune), `s[i]`/`s.len` = BYTE-level (Rust model). RFC 0020 P1+P2+P3 SHIPPED (`beab7d1`: slice/UFCS/as_bytes/chars/char_at); ascii/utf16-32/char_indices DEFER out-of-scope.
- [✅ missing-return diagnostic](missing-return-diagnostic-shipped.md) — typecheck REJECT non-void fn rơi khỏi cuối body không `return` (trước: silent garbage RAX); `stmt_terminates` conservative. `ebca3d3`. Thêm `reject` cmp mode cho regression harness.
- [✅ sum-of-primitives match segfault FIXED `EC01FECA`](bug-sum-of-primitives-match-segfault.md) — `type ID = i32|string` (union of BARE primitive types) nay REJECT tại DECLARATION (was accept-then-SEGFAULT trên match `i32(v)`). Fix = `is_primitive_type_name(vnm)` trong `pre_infer_type_alias` (gated `not sum_is_builtin` → Option/Result an toàn). Attempt-1 (match-site reject) unsound; declaration-level né imprecision `payload_type==UNKNOWN`. Self-build-safe (0 sum/enum pipe decl trong source). Oracle t_sumprimmatch (reject).
- [🧭 autopilot direction 2026-07-18 (user-chosen)](autopilot-direction-2026-07-18.md) — M4=rewrite suite in real grammar (✅ done), next=M6 perf, CTGC P3=attempt with tight gate. Standing autonomy granted.
- [✅ M4 compliance RESOLVED (real-grammar rewrite, bin/t_compliance.ax 60/60)](m4-compliance-suite-spec-vs-impl-gap.md) — `tests/axiom_compliance_suite.ax` (100 test/10 nhóm) + `std/testing.ax` viết bằng SPEC dialect (brace `{}`, `!not`, default args, trait bounds, format(), async/gpu/quantum) mà compiler impl KHÔNG parse → M4 "100 pass" KHÔNG phải bounded bug mà là chương trình lớn. Cần user chọn hướng (a) implement spec dialect / (b) rewrite suite theo grammar impl (nhóm 1-6 ~60 test, đề xuất) / (c) redefine M4. Đây là **derived next direction** cho autopilot — mỗi feature-gap = bounded task (backlog giàu hơn random probing đã cạn).
- [⚠️ infra: Defender throttles builds ~5hr regression](infra-defender-build-throttle.md) — AV scan trên mỗi exe mới = binding constraint cho MỌI build-gated work (fixpoint/regression/m2 fix). Mitigation user-side: Defender folder exclusion cho bin/ + bootstrap/stage1/. Concurrency trap: KHÔNG chạy 2 regression cùng lúc (contention → spurious build-fail); TaskStop để sót orphan bash, kill cả cây.
- [🟢 probe session 2026-07-19b — 1 real find (shift bug) + ~17 clean loop crosses](probe-batch-2026-07-19b-shift-find.md) — documents covered crosses (arithmetic/shift/bitwise/cast/signed-div-mod/index-mutation/sum-match/container/option/global in loops); ĐỪNG re-probe these. Next probing → new axes (strings-in-loop, 3-level generics, defer×loop, closures-over-loop-state).
- [🟢 probe batch 2026-07-18-b CLEAN](probe-batch-clean-2026-07-18-b.md) — 6 crosses (sum-match/u32-bitwise-boundary/nested-continue/15-temp-spill/fib-recursion/signed→u8-roundtrip) all correct O0==O1 trên driver 1C2E3D6A; plateau xác nhận, ĐỪNG re-probe.
- [ℹ️ inline match arm = unsupported](inline-match-arm-unsupported.md) — `fa9e76a`: inline STATEMENT arm reject sạch; inline-stmt thật=RFC, defer.
- [✅ RFC 0018 P1 for-in-array](rfc0018-for-in-array-shipped.md) — `for x in <fixed-array>` element iteration SHIPPED (`96dd586`). Vec/HashMap iteration = P2.
- [✅ BUG#89 for-continue](bug89-for-continue-increment.md) — `continue` trong `for` HANG (bỏ qua increment); fix emit +1 inline trước jump-to-cond. `3e26928` B==C.
- [🤖 HARNESS autopilot](harness-autopilot.md) — `.claude/` có agent+skill tự chủ; "tiếp tục"→`axiom-autopilot` tự chọn task→gate→auto-commit. CLAUDE.md §24.
- [✅ CSE redef-operand miscompile](bug-cse-redef-operand-miscompile.md) — cse_func reuse expr qua redefinition operand → `x=x op x` sai ở -O1; AIR non-SSA. Fix scan-down-stop-at-redef. `f65addb` B==C.
- [📋 Backlog OPEN items](backlog-open-items.md) — danh sách task còn mở đã đối soát (next-step-15 + RFC follow-ups); ĐỪNG làm lại phần đã ship.
- [✅ narrow-int const-fold wrap FIXED `633913E9`](bug-const-fold-narrow-int-wrap.md) — -O1 fold u8/u16/u32/i8/i16/i32 add/mul/neg ko wrap về width (tính 64-bit) → O0-vs-O1 miscompile; fix mask fold result GUARDED operand-fit (type_id AIR ko tin cậy; negated i64::MIN=tid i32). Oracle t_foldu32wrap(205). Bài học: A==B ≠ correctness.
- [🔍 Probe 2026-07-18 CLEAN (0 bugs)](probe-batch-1012-clean-2026-07-18.md) — 3 batch ~22 feature-cross (O0-O3) KHÔNG bug thật; mọi mismatch = lỗi tự viết test (`#`≠comment dùng `//`; sum=`type T=A|B`; inline match-arm cấm). Banked oracle t_optvecnest(42). Yield≈0 → dừng probe, re-plan milestones. ĐỪNG lặp lại crosses này.
- [✅ RFC 0019 multi-field variant SHIPPED](rfc0019-multifield-variant-shipped.md) — `Rect(i64,i64)` (desugar→synth-struct); ĐÓNG BUG#81. `59bc731`, A==B==C, 125/125.
- [✅ BUG#91 FIXED generic-sum match](bug91-generic-sum-ctor-inference-open.md) — unannotated `let p = One(42)` cho `Opt[T]` trả 0: match GENERIC_INST-of-sum không lower. Fix `resolve_sum_type`. `57b2218`.
- [✅ BUG#93 silent-segfault FIXED](bug93-qualified-str-call-segfault.md) — unresolved module-qualified call (`std.math.gcd` typo) giờ REJECT sạch (gốc=native strip-import bundling). `468f581`. CÒN: import-driven bundling.
- [✅ BUG#90 Option-method — CLOSED (re-verified 2026-07-17)](bug90-option-method-segfault-open.md) — `.unwrap()/.is_some()` TYPED=Option `1f203bc` + untyped `m.get(k).unwrap()` deterministic (re-verify daily driver: 42). Spawned bug93-contains ĐÃ FIXED `9f89b64` (contains_key/HashSet.contains 42/42). Cả cụm đóng.
- [RFC 0016 ✅ RESOLVED](rfc0016-p2prime-cfg-liveness.md) — P2' CFG-aware liveness (`e3f9539`, `compute_liveness` live-in/out, mở khóa lowering tạo block giữa expr) + P3 short-circuit (`755d7b8`), đóng BUG#86.
- [✅ RFC 0024 block strings SHIPPED](backlog-open-items.md) — `72e4858`: `"""..."""` multi-line (escapes, verbatim, no dedent). 3 site: lexer/unescape/strip_quotes. A==B `21ED17B1`.
- [✅ RFC 0023 if-expressions SHIPPED](rfc0023-if-expressions-shipped.md) — `4c3ee6b`: `if c: a elif …: b else: c` value-expression, mandatory else, NUD-only (statement-if bất biến), chạy trong lambda body. Value-diamond air (model RFC 0016 P3). 290/290.
- [✅ lambda param field/method capture false-positive](bug-lambda-param-field-capture.md) — `b37c14a`: `param.field`/`param.method()` trong lambda bị reject "captures ''"; fix=scan bỏ qua FIELD_EXPR field-name child payload==0. Cùng họ `7e5965a`.
- [Feedback: memory→./knowledge](feedback-memory-location.md) — Lưu MỌI memory vào `./knowledge` trong repo (ko dùng global .claude memory). Index chính = `knowledge/MEMORY.md`.
- [Feedback: auto-commit](feedback-auto-commit.md) — Tự git commit+push thẳng main mỗi khi fix xong bug.
- [Feedback: auto-run bash](feedback-auto-run-bash.md) — Tự chạy shell (gate/probe/build/git) ko hỏi; enforcement=settings.json `permissions.allow` USER tự set. ĐỪNG dùng `-ExecutionPolicy Bypass`.
- [Feedback: fixpoint-async rule](feedback-fixpoint-async-rule.md) — Frontend-thuần: commit sau regression GREEN, fixpoint async. Backend/ABI/linker: fixpoint bắt buộc TRƯỚC commit.
- [Feedback: làm việc tự chủ](feedback-autonomous.md) — Ko hỏi lại; tự chọn hướng tối ưu; chờ build thì làm việc build-independent.
- [Feedback: chạy liên tục](feedback-continuous-loop.md) — Self-perpetuating loop, KHÔNG ngủ đông: arm persistent `Monitor` heartbeat 5 phút (ScheduleWakeup ko fire idle); mỗi tick → tự chọn+chạy task kế, ko hỏi; chỉ TaskStop khi user bảo. CLAUDE.md §24.
- [Feedback: auto-compact context](feedback_compact.md) — Tự động /compact khi context gần đầy.
- [Feedback: KHÔNG làm phình exe](feedback-no-exe-bloat.md) — User 2026-07-22: "không muốn làm phình các file thực thi". Mọi thay đổi codegen/linker/section/bundling phải ĐO size exe trước-sau; tăng size = finding cần lý do. Ưu tiên thiết kế "không dùng thì tốn 0 byte".
- [Feedback: ergonomics ngôn ngữ](feedback-ergonomics.md) — User ghét boilerplate (`as u32` khắp nơi); ưu tiên inference/sugar an toàn → RFC 0005.
- [Fast fixpoint workflow](fast-fixpoint-workflow.md) — `scripts/fast_fixpoint.ps1` ~9s gate (seed axc_native→A→B, **A==B**=fixpoint). Daily-driver rebuild sau mỗi commit trước khi AXC= regression.
- [DESIGN: struct reference semantics](axiom-struct-reference-semantics.md) — ⚠️ AXIOM struct/aggregate = REFERENCE (RFC 0001 §5): `mut cpy:=src`/`b=a`/param struct đều ALIAS, ko copy (chỉ scalar). ĐÚNG, đừng "fix".

## Trạng thái nền / lịch sử
- [next-step-15 self-host status](next-step-15-selfhost-status.md) — 🎉 SELF-HOST FIXPOINT (lịch sử): chain #24→#31 + RFC 0002-0007 đóng; bignum=pure library; float native OK.
- [next-step-16 fn-ptr shipped](next-step-16-fnptr-shipped.md) — 🎉 BUG#49 fn pointers + std.numerical/sort + float BUG#45/46/48; RFC 0008 closures drafted.
- [next-step-17 closures P1 shipped](next-step-17-closures-p1-shipped.md) — 🎉 Closures P1 + BUG#50 (zero-capture; capture=BUG#73 reject).
- [BUG#51+52 FIXED](bug51-hunt-progress.md) — 🎉 stale aggregate-alias qua mono vec-grow; mở khóa daily-driver `bin/axc_native.exe` (8s self-build).
- [next-step-13 optimizer](project_next_step_13.md) — native codegen + alg_simp_func + cse_func passes.
- [next-step-14 sumtype size bug](next-step-14-sumtype-size-bug.md) — stage2 crash: builder_type_size_and_align sai size Result generic-inst.

## FFI / separate compilation
- [FFI/dynamic-linking](ffi-dynamic-linking-priority.md) — RFC 0009 P1+P2 SHIPPED: nhập (`extern "C" from`) + xuất (`#[export]`/`--shared` DLL); còn P3 ELF.
- [RFC 0011 static libs](rfc0011-static-libs.md) — P1+P2 SHIPPED: `--staticlib` COFF `.lib` + `-l` link tĩnh.
- [RFC 0011 P4 separate-comp](rfc0011-p4-separate-compilation.md) — import-driven auto-libs (Go-style) XONG + cache: `library` keyword, `--auto-lib`, DLL `_ffi.ax` gen. Còn: struct field ptr/nested + type-alias.

## Ownership / CTGC / bignum
- [🧭 CTGC P3 scoping + `-ctgc-free-report` SHIPPED](ctgc-p3-scoping-2026-07-18.md) — general-free activation DEFERRED (un-enumerated missed borrow-locals UAF self-build; inert marking already shipped P2.1). Shipped dump-only `-ctgc-free-report` (prints freeable set, no OP_DESTROY, byte-identical) as the P3 measurement precursor. Kickoff note for the future activation session.
- [bignum vs CTGC](bignum-ctgc-conflict.md) — std.bignum ngoài CTGC → rò rỉ; gốc ở [[bug69-ctgc-ownership-escape-noop]].
- [BUG#69 — RFC 0015 P1-P3 SHIPPED](bug69-ctgc-ownership-escape-noop.md) — 🎉 Ownership/Escape/Ctgc từng NO-OP (guard node_idx==0). P1 mutability-only (E4002); P2/P2.1 escape active; P3 free scoped drop-typed (general free defer). Chi tiết đầy đủ ở đây.
- [RFC 0014 drop-glue ✅ SHIPPED c149872](rfc0014-drop-glue-blocked.md) — `drop(self)` hook opt-in `-ctgc-free`; gọi trước OP_DESTROY cho non-escaping owned drop-typed local. Mở khóa bignum leak.

## Cụm tagged-type / Option / generics (ĐÓNG)
- [BUG#60/61/62/63](bug60-61-option-followups.md) tagged-type/Option-match+generics; HashMap/HashSet/Vec native (≤8B). [BUG#57+59](bug57-match-option-native.md) native match dispatch. [BUG#58](bug58-sum-struct-payload.md) sum payload=STRUCT reorder. [BUG#56](bug56-nested-sum-payload.md) payload-SUM box-8B báo 16B.
- [BUG#54+55](bug54-qualified-variant.md) qualified variant segfault. [BUG#66](bug66-hashmap-i64-value-corruption.md) HashMap[K,i64]=0 first-binding-wins. [BUG#67](bug67-option-struct-payload-unwrap.md) `Some(x)` top-level GENERIC_INST. [BUG#74](bug74-generic-struct-inferred-ctor-args.md) `Pair[A,B]` field 2+ đọc 0 (combo type-size là chìa khóa).

## Cụm aggregate/16-byte repr (ĐÓNG)
- [BUG#77 16B repr UNIFIED](bug77-16byte-struct-byaddress-unified.md) ROOT 75/76/77: chỉ `str` inline-16, mọi aggregate 16B by-address. [BUG#76](bug76-return-16byte-struct-field.md) `return o.inner` OP_RETURN theo KIỂU TRẢ VỀ. [BUG#64+65](bug64-vec-big-aggregate-element.md) Vec element >8B regalloc OP_INDEX +RFC0013 generic_args.
- [BUG#84 `&a[i]` `c7072f0`](bug84-scalar-array-elem-address.md) + [BUG#85 TWIN `&s.f` `80f09e6`](bug85-scalar-field-address.md) — scalar addr trả TEMP; fix OP_INDEX_ADDR/FIELD_ADDR LEA ko load.

## Cụm None-in-container (ĐÓNG)
- [BUG#78](bug78-array-of-option-none.md) `[Some(3),None]` segfault store/load 8B ptr. [BUG#79](bug79-vec-option-none-mono.md) `v.push(None)` unresolved ax_push, arg-generic chặn khi param chưa resolve.

## Silent accept-then-miscompile → reject/fix (convention BUG#53)
- [BUG#53 FIXED](bug53-single-line-if-miscompile.md) — driver HALT khi diags_count>0 trước codegen; inline `if COND: <stmt>` reject; `;` separator first-class.
- [BUG#83 FIXED](bug83-underscore-digit-separator.md) — 1fb6397: digit separator `_` cắt cụt literal ở CẢ HAI parser; fix skip `_`. Oracle t_underscore.
- [BUG#80 FIXED](bug80-free-call-overload-collision.md) — 6b310f4: free-fn trùng tên stdlib (`get`) miscompile về 0; resolve theo arg[0]. Bài học: đổi TÊN hàm khi thu hẹp repro.
- [BUG#81 FIXED](bug81-multifield-variant-payload.md) — df2f867: variant >1 payload field drop field 2+; reject (superseded RFC 0019).
- [BUG#88 FIXED](bug88-str-concat-operator.md) — 9b45355: `str+str` cộng con trỏ (OP_IADD) thay vì concat; fix lower → `std.string.concat`. Oracle t_strconcat.
- [BUG#68 FIXED](bug68-struct-eq-no-overload.md) — struct `==` ko có `eq` overload so sánh ĐỊA CHỈ; reject. Bài học: diagnostic cùng cơ chế resolution với lowering.
- [BUG#70 FIXED + array literals](bug70-array-literal-shipped.md) — `[e0,e1,...]` parser NUD+typecheck; fix lower_array_lit stride.
- [BUG#71 FIXED](bug71-interface-dynamic-dispatch.md) — method qua interface-TYPED value segfault; reject dynamic dispatch (chưa vtable).
- [BUG#72 FIXED](bug72-range-index-reject.md) — `arr[x..y]` slicing segfault (`..` ko có Range type); reject.
- [BUG#73 FIXED](bug73-closure-capture-reject.md) — closure capture (`|x| base+x`) tính SAI âm thầm; free-var scan + reject (zero-capture only).

- [BUG#87 FIXED](bug87-for-range-loop-var.md) — ✅ 694da03 (A==B): `for i in a..b` loop var đọc 0/rác — lower_for key local_map sai (name_id vs sym_idx). Oracle t_forrange(28).
- [BUG#86 FIXED](bug86-short-circuit-open.md) — 755d7b8 (RFC 0016 P2'+P3, B==C): short-circuit `and`/`or`; cần nền P2' CFG-aware liveness [[rfc0016-p2prime-cfg-liveness]]. Bài học: -O0 pass/-O1 fail ⇒ soi ssa_opt.

## Language-feature correctness (accept-then-miscompile → reject)
- [✅ `defer` in control flow REJECTED `dcac520` (A==B 1a934e34)](bug-defer-in-control-flow.md) — `defer` registered STATICALLY at lowering (self.defers, textual order) → ran under a non-taken `if` branch, and once-regardless-of-iterations in a loop. Reject nested defer (typecheck check_defer_placement); top-level unconditional defer still works. Found via probing (F3/G1). Dynamic defer = future RFC. Oracles t_deferctrl(reject)/t_defertop(2).

## Codegen miscompiles (non-SSA class)
- [✅ variable-shift-in-loop miscompile FIXED `5a22500` (B==C af3ba8e3)](bug-variable-shift-in-loop.md) — `x << k`/`x >> k` with a loop-induction-variable count computed `x << 0` (count frozen at loop-ENTRY value); `const_shift_amount` lacked the non-SSA def-count guard its twin `const_divisor_pow2` had. LOAD-BEARING (A!=B, compiler's own source hit it). Surfaced by autopilot probing; O-level-INDEPENDENT so O0-vs-O1 blind — hand-oracle caught it. Oracle t_shiftloop(63). Same class as [[bug-cse-redef-operand-miscompile]].

## OPEN
- *(none — the last one closed 2026-07-22d, below)*

## CLOSED 2026-07-22d
- [✅ FIXED `bf67c7d` — two array-payload Option instantiations SIGSEGV](bug-two-array-payload-instantiations.md) — ROOT: `get_type_name_recursive` (mono.ax) returned `pool.get(entry.name_id)` BEFORE its own structural branches, and for an ARRAY `name_id` is the **LENGTH** → `[i32;2]` and `[i64;2]` both mangled to `Box__T` (intern id 2 = "T"), so two instantiations SHARED one monomorphized type and the second reused the first's layout. **Third instance of the `name_id`-is-not-a-name footgun** (after `82d0565`, `b966f73`). Fix = exclude ARRAY (length) + RESULT (err type id) from the early name return so the existing `arr_<len>_<elem>` branch becomes reachable, and give OPTION a name (it had none → every `Option[..]` type arg mangled to the shared fallback `"type"`). ⚠️ The two hypotheses previously logged as REFUTED were rejected for the WRONG reason: the collision IS keyed by length, but is only OBSERVABLE when the merged layouts differ — **silent-but-harmless aliasing is a real outcome, so "predicts collisions that don't crash" does NOT refute a theory**. One trace of the mangled name settled what the behaviour table could not. Shipped with 2 related silent miscompiles: (a) generic-ctor array element WIDTH (`let b: Box[[i64;2]] = Box(v:[5,6])` inferred `Box[[i32;2]]` from the VALUE → 4B stores read at 8B stride, sum 6 not 11; `try_instantiate_struct_ctor` now binds params from the `expected` instantiation's field types, which coerces the literal too); (b) `Box[[i32;3]](v: ..)` was a PARSE ERROR while the same spelling in TYPE position worked (the `[` led now parses a bracketed arg as a TYPE when it holds a `;` at bracket depth 1 — an array literal never can, so `f[[1,2][0]]` still parses as an expression). A==B `44688E18`, **508/508**, oracles t_twoarraypayload(104) + t_arrayctorgeneric(80), both SIGSEGV on the pre-fix compiler, O0–O3 identical. Also closed as STALE the same pass: the *"`Option[[i32;3]]` still returns 8"* residual — already fixed by `82d0565`, measured 48 before any change.

## Historical (closed earlier)
- [✅ boxed-aggregate-payload probe 2026-07-22 — ALL findings closed](probe-boxed-payload-2026-07-22.md) — see also [✅ array type-arg mono payload FIXED `82d0565`](bug-array-typearg-mono-payload.md): `register_array` overloads `name_id` as the LENGTH so an array type has NO name; the monomorphizer substituted that length as an intern id → empty variant payload → stride-8 reads. Reused the existing `__tup` `extra_idx` stash. A==B `29BAF82C`, 500/500, oracle t_arrpayloadwidth(166). **Footgun: `TypeEntry.name_id` is the LENGTH for arrays and the ERR TYPE ID for results, not a name.** Auditing that footgun found a SECOND bug, fixed `b966f73`: the generic-instantiation scan for a mangled name matched an ARRAY entry whose length equalled the mangled id (`[i64;974]` beside `Pair[i64]`) → SIGSEGV. A==B `48EF9CC1`, 501/501, oracle t_manglearraylen(43) — CALIBRATED, degrades to a trivial pass if interning drifts, see its header.
- [🟡 boxed-aggregate-payload probe 2026-07-22 — all 3 addressed (`7bb826f`, `6667c68`, `1146b75`); ctor arity also fixed; ALL residuals now CLOSED](probe-boxed-payload-2026-07-22.md) — sweeping the region right after the 16B-tuple deref fix found three MORE pre-existing silent miscompiles (all verified identical on the pre-fix backend, so not fallout): (1) ✅ **FIXED `7bb826f`** — array-payload element reads: the literal is inferred TWICE and the hint-less re-visit re-registered `[i32;N]` last, so elements stored 4B while reads used the declared stride; added the sticky guard `NODE_TUPLE_EXPR` already had. A==B `EB02B23D`, 497/497, oracle t_arrpayload(232). ✅ RESIDUAL CLOSED (STALE NOTE, re-measured 2026-07-22d: returns 48, fixed by `82d0565`, pinned by t_arrpayloadwidth). Historical text: a NARROW-element payload (`Option[[i32;3]]`) returned 8. Traced: for an ARRAY payload `pl_type` recovery finds nothing and defaults to i64, and `OP_INDEX` then falls back to stride 8 — **so `[i64;3]` is right only BY ACCIDENT** (8 == i64 size); the sticky fix repaired the STORE side only. Real fix = make the mono Option SUM carry its array payload_type (tuples already do), self-host-critical, dedicated session; (2) ✅ **FIXED `6667c68`** — a TUPLE payload in a USER sum read field 1 as 0 because the non-generic variant ctor never received an expected type, so bare int literals stayed i32 (8B into a 16B slot); fix `coerce_variant_ctor_payload`, A==B `7764E7F9`, 494/494, oracle t_sumtupctor(102). TWO structural hypotheses were refuted first — read the doc before theorising. ✅ variant-ctor ARITY also FIXED `97c54ac` (was unchecked in every direction; a tuple payload given 2 args SIGSEGV'd) — A==B `73AA3969`, 499/499, oracles t_ctorarity(reject)+t_ctorarityok(58); (3) ✅ **FIXED `1146b75`** — RFC 0019 multi-field variant bound to ONE name SEGFAULTed; pattern arity now checked against the variant (sound because flagged `__mfv_` and unflagged `__tup` payloads ARE distinguishable — the earlier "they collapse to one type" claim was wrong). A==B `FBC2AA3F`, 496/496, oracles t_mfvarity(reject)+t_mfvarityok(58). Clean on the same sweep: Result Ok/Err 16B tuple, `Option[(f64,f64)]`, plain arrays/tuples, user sum with struct payload.

## CLOSED (was OPEN)
- [✅ FIXED `3319bdc` — 16-byte tuple payload deref clobbered a CALLER stack slot](bug-opt-tuple16-deref-caller-clobber.md) — two calls to a fn taking `Option[(i64,i64)]` whose body read a field of the match-bound payload zeroed one adjacent 8-byte caller local (deterministic O0–O3). `OP_DEREF` gated `deref_is_agg` on `type_size != 16`, using dest SIZE as a proxy for "is str", so an EXACTLY-16-byte aggregate took the str-style inline copy into an 8-byte home. Real discriminator = the SOURCE being a tagged box (`type_is_pointer_repr(src_type)`); a genuine `ptr[Struct]` deref still loads 16. A==B==C `907958ED`, 493/493, oracle t_optupclobber(110). Attempt #1 held B==C yet broke working cases — B==C proves self-reproduction, NOT correctness.
- [✅ CLOSED (definitively mitigated): 3+ HashMap monomorphizations → intermittent TEARDOWN segfault](bug-3hashmap-mono-teardown-crash.md) — the crash is ELIMINATED by the shipped teardown-skip (successful self-link build flushes + exits, skipping the free-chain — a standard technique; OS reclaims). Re-verified 2026-07-19b: **0 crashes / 60 builds** of c_3map, output exe exit 0. Residual root-cause OOB (mid-block indexed write during hash-container mono) is bounded + output-invisible; independent fresh pass (strides correct, clone index-safe, substitute/remove don't grow tree, HashMap struct K/V-independent) corroborates "not findable by inspection"; Windows-heisenbug tooling doesn't apply. CLOSED as disproportionate for a low-sev output-correct mitigated bug. **No OPEN bugs remain.**
- [✅ narrowing-cast compare width — FIXED `aede254`](bug-narrowing-cast-compare-width.md) — `300 as u8` left DIRTY upper bits (scalar OP_CAST skipped wrap) → 64-bit `==` read 300; fix normalize after cast. Oracle t_castwidth.
- [✅ f32 binop mixed-width — FIXED `ec8a0d0`](bug-f32-binop-mixed-width.md) — `y*2.0` (y:f32) op F64→mulsd sai; fix float-lit bidirectional width inference. Residual: concrete mixed-width var (hiếm, defer).
- [✅ consecutive float width-cvt calls — CLOSED (verified 2026-07-17)](bug-consecutive-float-cvt-call-regalloc.md) — từng nghi backend xmm-regalloc miscompile 2 back-to-back f32↔f64 cvt calls; re-verify trên daily driver `38a9d81`: 10 (want 10) ở O0+O1, identity=12. Fix incidental qua float-lit width `ec8a0d0`. Oracle `t_floatcvt`.
- [✅ f32 boundary miscompile — FIXED `c7f6b78`](bug-f32-boundary-abi-open.md) — f32 param/return đọc 0; root FRONTEND: NODE_FLOAT_LIT luôn F64; fix coerce float lit→F32 khi expected==F32. Oracle t_f32boundary.
- [✅ `Vec[(i64,T)]` tuple-generic-element mono — FIXED `aa419a2`](bug-vec-generic-tuple-element-mono-open.md) — Vec element=tuple chứa generic T segfault; fix `is_generic` đệ quy field `__tup`.
- [✅ cross-module close mis-link — FIXED `a9cfd91`](bug-crossmodule-resolution-fragility-open.md) — UFCS match-by-name chạy cả khi resolved (flag 2048) → mis-link; fix skip UFCS khi 2048.
- [✅ inline lambda × generic — FIXED `2cc67ed`](bug-lambda-generic-fntype-infer-open.md) — `apply2[T,U]`/`Option.map(|..|)` link-error; mở khóa Vec HOF.
- [✅✅ tuple as generic-arg payload — CLOSED](bug-tuple-generic-payload-unwrap-open.md) — CTOR `1aff7ca` + method-arg `3d2aab0`: element-width gap; re-infer tuple-lit với `__tup`. +assign-coercion `803a806`.
- [✅ Option/Result element trong array/Vec — FIXED `bf1361c`](bug-option-array-element-read-open.md); [✅ Option/Result arith miscompile REJECT `313cb51`](bug-option-arith-miscompile-open.md) — quên unwrap giờ reject.
- [✅ generic-inst field mono — CLOSED `a02db69`](bug-inferred-generic-field-type-open.md); [✅ instance+static method MIX `491575a`](bug-instance-static-mixed-call-segfault-open.md) — static-form bogus receiver SIGSEGV, skip synth khi kind=type.
- [BUG#82 FIXED](bug82-global-var-semantics-open.md) — 9e00177 (RFC 0017 P1, B==C): module-level globals có STATIC STORAGE thật (`.data`+OP_GLOBAL_ADDR). Oracle t_globals. Còn P2: aggregate/ELF → [[backlog-open-items]].
