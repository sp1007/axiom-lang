---
name: next-step-15-selfhost-status
description: AXIOM self-hosting ĐÃ ĐẠT FIXPOINT (stage3==stage4, 2026-06-26) sau fix BUG#29/#30/#31 + Family C (struct by-value); repro nhanh + cách verify + cách dùng gdb/dump-air cô lập
metadata:
  type: project
  originSessionId: 9720703b-c141-45a7-9477-748fe53e183d
---

Tiếp nối [[next-step-14-sumtype-size-bug]]. Mục tiêu: deterministic self-host. **MỌI CHI TIẾT ở knowledge/bugs.md.**

**🎉 ĐÃ ĐẠT SELF-HOST FIXPOINT (2026-06-26):** stage3==stage4 byte-identical (SHA cf5a7c6a…), compiler native compile chính nó tái tạo bit-for-bit. Chain bug #24→#31 đã fix hết. Golden binaries: bin/axc_stage{2,3}_selfhost_fixpoint.exe. (stage2≠stage3 bình thường: builder stage2 = stage1/gcc.)

**Chain bug đã fix:** #24 cast-shift → #25 optimizer addr-taken REX-loss → #26 strip self-mangle → #27 byte-store REX-loss → #28 stack 1MB→16MB → #29 (bỏ redirect std.string.replace/slice→ax_str_* C-ABI sret) → #30 (regalloc_is_16byte OP_COPY: chỉ str inline-16, aggregate by-address 8-byte) → **#31 (copy_prop_func thiếu guard addr_taken)**.

**BUG#31 (blocker cuối, FIXED commit sau 98f8ee8):** `copy_prop_func` (ssa_opt.ax) THIẾU guard `addr_taken` mà fold_func đã có. Với `mut modrm:=0; &modrm`, front-end sinh `%6=copy %5; mkref %6`. copy_prop lập copy_map[%6]=%5 dù %6 addr-taken → đổi read %6→%5 (init 0) trong khi mkref/store ở %6 → read STALE. Trong x86_encode_mov_rr: modrm→0 ⇒ mọi `mov reg,reg`=48 89 00 ở stage3 ⇒ binary hỏng. stage1(gcc) propagate nhất quán (đúng) còn stage2(native) không nhất quán (META). FIX: thêm `and not addr_taken[inst.dest] and not addr_taken[inst.src1]` khi lập copy_map + free addr_taken.

**Cách cô lập META bug (vàng):** repro nguyên văn hàm vào file standalone (bin/t_movrr.ax = copy x86_encode_mov_rr) → build bằng axc_stage1 vs axc_stage2 -O1, so output. Rồi `dump-air` cả 2 stage, diff hàm → thấy ngay divergence AIR. KHÔNG cần build 2.5h để tìm root.

**Family C — ✅ ĐÃ FIX (2026-06-26, RFC 0001, commit a937971):** struct 16/24-byte BY-VALUE param+return. Guard `not type_is_aggregate` 4 điểm x86_selector.ax: `regalloc_is_16byte` param + call-return direct/dynamic; **`emit_param_prologue` register+stack** (đây mới là thủ phạm chính — materialize param: aggregate lưu con trỏ 8-byte thay vì deref+copy 16-byte inline). KHÔNG phải OP_COPY lazy path (chết vì param_idx_processed quá cuối). Root tìm bằng gdb. Kết quả tsp=7/tsp2=9/tsp3=12, fixpoint stage3==stage4=d7f14c2c GIỮ. (TODO nhỏ: value-semantics — truyền con trỏ object gốc, khớp reference-semantics struct của ngôn ngữ.)

**BUILD/VERIFY:**
- rebuild stage1 (C backend qua stage0): `scripts/rebuild_stage1.ps1` (~25s, regen tmp_concatenated_air.ax). KHÔNG dùng `-ExecutionPolicy Bypass` (bị chặn) — gọi `& "scripts\rebuild_stage1.ps1"` trực tiếp.
- full verify: `bash scripts/verify_bug29_selfhost.sh` (stage1→2→3→4 + SHA). stage1→stage2 ~2h (native codegen 753 funcs); stage2→3, 3→4 mỗi cái ~7s.
- ⚠️ Windows Defender realtime ON → mỗi self-link build scan exe/obj ~51s wall (CPU~0). KHÔNG phải hang; timeout ≥90s. Là nhiễu nhỏ với build compiler đầy đủ.
- ⚠️ orphan axc.exe giữ lock exe → kill trước rebuild (`Get-Process axc* | Stop-Process -Force`).
- ⚠️ self-link build ghi axiom_temp.obj (tên cố định) → KHÔNG chạy 2 self-link build song song (đè obj). Đừng rebuild stage1 khi verify đang chạy.
- repro -O0 = proxy stage2 (optimizer khác); nhưng META bug optimizer chỉ hiện -O1.

**next-step-15 (mở rộng feature ngôn ngữ) — PHÁT HIỆN: grammar ĐÃ implement đủ.** Mọi "gap" tưởng tượng (inline method, interface, match) hoá ra do probe SAI syntax (untyped self/`;`/braces/enum). Method=typed-self+UFCS, inline method, interface, match, sum type, generics ĐỀU works. Files fail (iter/json/log/net/fmt + ~13 std + compliance suites) viết DIALECT ngoài grammar (braces/enum/Self/assoc-type) → nợ aspirational, KHÔNG block. Xem docs/next-step-15.md + std/MODULE_STATUS.md.
- **T3 done:** std/MODULE_STATUS.md phân loại working (string/io/collections/os/result/option/process...) vs aspirational. Build chỉ import std.string/io/collections.
- **T1 done — RFC 0002 untyped `self` sugar:** `fn m(self)` trong struct → suy type=struct cha. Thuần PARSER (Parser.current_struct_tok; synth NODE_TYPE_EXPR trỏ token struct-name vì type-resolution đọc TOKEN BYTES không phải payload — bài học quan trọng). self=struct type chạy giống ptr[T] (by-address). Fixpoint MỚI = **fc5e6f48** (stage3==stage4) giữ. Commit 80a106b.
- **Bài học probe:** muốn biết feature có support không, probe bằng syntax GRAMMAR-CONFORMANT (xem docs/GRAMMAR.ebnf), hoặc compile file std grammar-đúng; đừng probe bằng syntax tự nghĩ.
- **regression gate giờ 20 repro** (thêm 5 ADT + t_litinfer=111).
- **RFC 0005 (int literal inference) DONE — fixpoint MỚI = 37a035ad** (stage3==stage4, commit 7bfa81b). `b+1`, `x<10` không cần `as u32`: literal adopt kiểu toán-hạng-anh-em cụ thể (typecheck.ax NODE_BINARY_EXPR, guard t1!=t2 → code đã-cast không đổi → fixpoint giữ).
- **RFC 0006 PART 1 (f64 numeric codegen) DONE — fixpoint MỚI = 5334362c** (stage3==stage4, commit 78c2cd6). Float backend CÓ sẵn nhưng CHƯA TỪNG chạy đúng; 3 bug đào ra: (1) lower_float_lit truncate float→int + OP_FCONST sai (giờ lưu IEEE bits, materialize qua GPR+movq); (2) **movq xmm↔gpr thiếu 0x66 prefix** → encode MMX (mm0-7 alias x87) thay vì XMM; (3) **spill reload float vào GP scratch r10/r11** → float encoder (cvttsd2si/addsd) đọc nhầm register file (r10 alias xmm10 hw-idx 10) → giờ reload XMM scratch xmm0/xmm1 + movsd. air_builder: lower_binary_expr phát OP_FADD/F* khi result f32/f64 + OP_ITOF promote int; lower_cast int→float=OP_ITOF, float→int=OP_FTOI. Matrix 216/232 (16 fail = f32 deferred); regression 20/20. **Compiler giờ DOGFOOD f64** (print_helpers/typecheck có float) → SHA đổi là đúng. **Deferred:** f32 (cần họ encoder `ss` + width 4-byte), float dst-spill (DST_UNUSED cho MACH_FADD/ITOF/MOVDQ), float compare. Chi tiết knowledge/bugs.md BUG#33.
- **RFC 0006 PART 2 (unsigned div/mod + signed-aware shift) DONE — fixpoint MỚI = f4def14b** (stage3==stage4, commit eb248d0). BUG#34.1/.2. OP_IDIV/IMOD chọn theo `sel_type_is_unsigned(get_register_type src1)`: unsigned → zero rdx + MACH_DIV (mới, x86_encode_div_r F7/6); signed → cqo+IDIV. OP_SHR → SAR (signed) vs SHR (unsigned). **BẪY đã sửa:** divisor của MACH_DIV phải forbid_rax_rdx như IDIV (x86_regalloc.ax:344) — quên → divisor vào rdx → `mov rdx,0` đè → div-by-0 crash exit 127. Repro mới bin/t_udiv.ax (exit 5) trong regression (giờ 21). matrix 216/232 (16 f32 deferred). **CÒN LẠI RFC 0006:** unsigned COMPARE (JB/JA), lan kiểu 2 chiều, narrowing/sign diagnostics, BUG#36 mask-về-width, + f32 (part 1 deferred). Chi tiết knowledge/bugs.md BUG#34.
- **Bài học vận hành (2026-06-27):** (1) verify chạy detached nohup KHÔNG harness-track → khi xong không đánh thức agent (đêm 06-26→27 ngồi im tới sáng). FIX: dùng **Monitor persistent** (harness-track) phát heartbeat mỗi 600s + detect `SELF-HOST OK` → đánh thức commit ngay. (2) ĐỪNG sửa file script bash khi nó ĐANG chạy (bash đọc file lúc execute → "unexpected EOF"). (3) ĐỪNG chạy 2 self-link build song song (đè axiom_temp.obj). (4) **`pgrep` KHÔNG tồn tại trên Git-bash box này (rc=127)** → check liveness trong Monitor phải dùng `ps -W 2>/dev/null | grep -iE "axc_stage|axc\.exe"`, KHÔNG dùng pgrep (luôn báo "process gone" sai → Monitor break sớm). Tiến trình native build = `bin/axc_stage1` hiện trong `ps -W`.
- **RFC 0006 PART 3 (unsigned compare) DONE — fixpoint MỚI = 4122a9f3** (stage3==stage4, commit e0b28c5). BUG#34.3. select_comparison: nhánh integer chọn unsigned CC (B/BE/A/AE) nếu `sel_type_is_unsigned(src_type)`, signed giữ L/LE/G/GE. CC unsigned trùng CC float (param đổi tên float_cc→unsf_cc). Branch test bool từ setcc → không có fused signed-cmp-branch. Repro bin/t_ucmp.ax (exit 6); regression 22; matrix 216/232.
- **Phát hiện cho BUG#35:** typecheck.ax KHÔNG có cơ chế emit error nào (grep error/diag = rỗng) → conversion diagnostics (float→int phải `as`) cần XÂY hạ tầng error trong typecheck trước → lớn hơn dự kiến.
- **KẾ TIẾP đã chọn: f32** (giá trị thấy rõ nhất: matrix 216→232; self-contained, mirror f64). Cần: họ encoder `ss` (addss/subss/mulss/divss F3; cvtsi2ss/cvttss2si F3; cvtss2sd F3 0F5A/cvtsd2ss F2 0F5A; movss F3 0F10/11; movd 66 0F6E/7E KHÔNG REX.W; comiss 0F2F không 66) + thread width 4-byte qua MachInst (field `padding`) để emitter chọn ss vs sd; float load/store hiện LUÔN movsd (8B) bất kể size → f32 đọc hỏng slot 4B. f32 vẫn cần verify (đụng shared float path mà compiler dogfood qua f64).
- **RFC 0006 PART 4 (f32) DONE — fixpoint MỚI = ee057a13** (stage3==stage4, commit 89c5b4d). **MATRIX 232/232 (F=0!)** — toàn bộ ma trận số hoàn tất. Thêm họ encoder `ss` + thread width 4-byte qua MachInst.padding (4=ss/movd, else sd/movq): emitter dispatch FADD/FSUB/FMUL/FDIV/FCMP/ITOF/FTOI + movss/movsd load/store + movd/movq FCONST; selector set padding theo float type (FTOI/FCMP theo SOURCE). f32 const = 32-bit pattern via movd; f32↔f64 = MACH_CVTSS2SD/MACH_CVTSD2SS mới. f64 path BYTE-IDENTICAL (padding 8→sd) → compiler không đổi behavior. Repro bin/t_f32.ax (exit5); regression 23.
- **AUDIT feature toàn ngôn ngữ (2026-06-27, BUG#37/38/39 trong bugs.md) — pass TĨNH, chưa vét cạn.** Gap grammar-vs-codegen, đều LATENT (compiler không dùng → self-host xanh): #38.1 compound assign `+=` → lower_assign bỏ op → chạy thành `=` (SAI âm thầm, CAO); #38.2/#37 match int/literal KHÔNG codegen → không có "C switch"; #38.3 `**` → OP_NOP; #38.4 defer parse_stmt thiếu nhánh; #38.5 unsafe: / #38.6 arena không parse; #38.7 closures không typecheck/codegen; #38.8 tuple pat; #38.9 spawn/await/Isolated/Future stub. **BUG#39: KHÔNG có operator overloading** → bignum/scientific số tùy biến (`a+b` trên struct) KHÔNG khả thi; cũng KHÔNG có const generics (`bignum[256]` không được, generic param chỉ là kiểu). Hướng: operator overloading (RFC, mở khóa) → BigInt động → [xa] const generics.
- **BUG#38.1 (compound assign) + #38.2/#37 (match-int = C SWITCH) DONE — fixpoint MỚI = 560fe56e** (commit aaf51df). #38.2: lower_match thêm is_int → so scrut_reg trực tiếp + NODE_LITERAL_PAT (ICONST typed+split + OP_EQ + branch), `_`/binding default; typecheck else cho binding non-sum. #38.1: helper assign_value (air_builder) — `a op= b` → lower rhs + lower lhs hiện tại + emit binary (float→OP_F*, unsigned div selector); 4 nhánh lower_assign route qua. Behavior-identical cho compiler (plain `=`/sum-match không đổi) → fixpoint giữ SHA mới. Repro bin/t_switch.ax(4), bin/t_cassign.ax(12); regression 25.
- **BUG#38.5 unsafe + #38.6 arena DONE — fixpoint MỚI = 71276119** (stage3==stage4, commit b9f5433). parser.ax: parse_unsafe_block (NODE_UNSAFE_BLOCK) + parse_arena_block (NODE_ARENA_BLOCK, capture tên arena) dispatch từ parse_stmt; typecheck.ax infer_node recurse vào body 2 block; air_builder lower_stmt lower block con (không runtime ẩn); resolver đã push BLOCK scope sẵn. Repro bin/t_unsafe.ax (exit 20 = (7+3)*2, test unsafe+arena+compound assign); regression 26.
- **BUG#38.3 `**` power DONE — fixpoint MỚI = b0270df1** (stage3==stage4, commit be6a383). lower_binary_expr nhận `**` → lower_power (air_builder.ax): result type chọn OP_IMUL/OP_FMUL, base int→OP_ITOF khi result float; **số mũ hằng ≤64 unroll chuỗi nhân** (n=0→emit_one 1; n=1→COPY; n≥2→chain), **số mũ biến → loop đếm-lùi** acc/cnt loop-carried qua OP_COPY vào vreg cố định (register-machine, không phi). Âm/phân số KHÔNG hỗ trợ. Repro bin/t_pow.ax (exit 59); regression 27.
- **BUG#38.4 `defer` DONE — fixpoint MỚI = f9ace7c6** (stage3==stage4, commit 5be272b). parser parse_defer_stmt (NODE_DEFER_STMT); FuncLowering thêm `defers` vec; lower_defer push (không chạy); flush_defers emit LIFO (không clear) ở lower_return (sau ret_val) + ensure_return (cuối hàm); typecheck infer expr con; resolver qua fallback. Ngữ nghĩa function-level LIFO (Go). GIỚI HẠN: defer trong nhánh không-chọn vẫn emit ở exit sau (cần runtime defer-list). Repro bin/t_defer.ax (stdout start/early/B/A/end + exit 7); regression 28.
- **BUG#36 mask-to-width DONE — fixpoint MỚI = 65504637** (stage3==stage4, commit 274b4cc). x86_selector.ax: emit_wrap_to_width(dest,type_id) tái dùng emit_load_extend (unsigned→AND mask, signed→SHL/SAR) gọi cuối OP_IADD/ISUB/IMUL/SHL + OP_NEG(int); narrow int i8/u8(1)/i16/u16(2)/i32/u32(4); range-preserving ops (AND/OR/XOR/SHR/DIV/MOD) không mask (bất biến: producer normalize). RỦI RO cao (compiler dùng i32 nhiều + bloat → stage2 chậm hơn ~150m) NHƯNG fixpoint hội tụ lại OK (giá trị compiler luôn in-range → kết quả không đổi). Repro bin/t_mask.ax (exit 15); regression 29.
- **#38.8 tuple = KHÔNG phải fix rẻ:** tuple KHÔNG phải feature thật (chỉ NODE_TUPLE_PAT + enum TYPE_KIND_TUPLE; thiếu tuple type parse, literal/construct, field .0/.1, ABI). Cần RFC implement tuple như aggregate hoàn chỉnh. Hoãn.
- **BUG#39 operator overloading DONE (RFC 0007) — fixpoint MỚI = bfe6dcd2** (stage3==stage4, commit 40a0d53). TOÀN BỘ trong air_builder.lower_binary_expr (không đổi grammar/parser/typecheck): node_types[lhs] là user type (STRUCT/SUM/GENERIC_INST) → op_to_method_name (+→add ... ==/!=→eq, <→lt...) → resolve_op_method (scan SYM_FUNC + match_mangled_method_raw_bytes + first-param-type==rec — KHÔNG dùng resolve_method_sym vì nó chỉ tìm generic) → lower_op_overload emit OP_CALL [self,rhs] (type_id=msym,src1=0); != = eq+OP_NOT. Result type = lhs (numeric fallback) / bool. **GIỚI HẠN MVP (user nêu):** dispatch lhs-only (`5+big` sai), không kiểm kiểu rhs (`big+5` mismatch), không ép ngầm primitive→user. Repro bin/t_opover.ax (exit44). MỞ KHÓA bignum (còn cần carry/u128 + stdlib BigInt limb).
- **Byte/bit extraction VERIFIED:** `(a>>(8*i)) as u8` trích byte i; `(a<<k) as u8`==0 với k≥8; `(a&1)` bit thấp. Idiom compiler dogfood (emit machine code) → fixpoint validate. Repro bin/t_bytes.ax (exit69: 65555→b0=19,b1=0,b2=1,b3=0,s4=48,bit0=1).
- **BIGNUM VISION CHỨNG MINH end-to-end:** examples/bigint128.ax + repro bin/t_u128.ax (exit91) — U128 = 2 limb u64, add carry-propagate (carry=detect unsigned wrap `lo<self.lo`), operator `+`/`==`, **16-byte struct by-value return (RFC 0001)**. Kỹ thuật limb+carry scale lên N-limb heap BigInt. Commit 9412aff. Test-only (compiler không đổi). regression 32 (test-only commit, KHÔNG cần fixpoint verify).
- **Operator overload resolve theo rhs type DONE — fixpoint MỚI = fb547bfc** (stage3==stage4, commit 4f5ab86). resolve_op_method nhận rhs_type, khớp param0==lhs VÀ param1==rhs → hỗ trợ operator method mixed-type đơn (`add(self,o:i64)` cho `Num+i64`, t_opmix exit13) + tránh gọi method sai kiểu rhs. **GAP: method overloading (2 method CÙNG TÊN) collision 1 symbol (mangling theo tên) → segfault; cần signature-based mangling (RFC riêng, RỦI RO CAO vì đụng mọi method call compiler).**
- **🎯 BIGNUM = PURE LIBRARY WORK (kết luận 2026-06-28).** Đã chứng minh ĐỦ building blocks, KHÔNG cần đổi compiler thêm: (1) **add** limb+carry (carry=`sum<operand`) — examples/bigint128.ax exit91; (2) **multiply** 64×64→128 tách u32 half-limb (4 tích u32×u32→u64) — examples/mul128.ax exit7 (commit fdc26f8); (3) **operator overloading** `+`/`*`/`==`/`<` trên user type (RFC 0007); (4) struct by-value mọi cỡ: **16-byte (U128) VÀ 32-byte (U256, 4×u64) đều chạy** (probe t256 exit110). → Full N-limb BigInt (fixed-width struct HOẶC heap ptr[u64]) viết được 100% bằng AXIOM. KẾ TIẾP: lắp ráp module BigInt (stdlib hoặc examples) — pure library, zero fixpoint risk, build+run test trực tiếp (KHÔNG cần verify 2.5h).
- **mulhi/u128 intrinsic:** KHÔNG còn blocker (multiply làm được bằng half-limb); chỉ là tối ưu tốc độ (~4×). Để sau nếu cần perf.
- **CÒN LẠI (compiler features, độ ưu tiên):** method overloading big+prim (signature-based mangling — RỦI RO CAO đụng mọi method call, để sau); #38.7 closures; #38.9 async; BUG#35 error-infra (conversion diag + reject operator sai kiểu). Fixpoint chain phiên 06-27→28: 71276119→b0270df1→f9ace7c6→65504637→bfe6dcd2→fb547bfc (6 verify, tất cả xanh). (TB: loop int / call pow float), #38.4 defer (codegen STUB SAI chạy ngay thay vì scope-exit — cần track per-scope), #38.6 arena, #38.7 closures (lớn). RFC 0006 numeric còn: **BUG#36 mask-to-width** (RỦI RO CAO: mask sau mọi op <64-bit, đổi codegen integer compiler nhiều — verify kỹ), **BUG#35 conversion diag** (cần error infra trong typecheck — print_helpers dùng `as` tường minh nên source CÓ THỂ sạch, kiểm tra trước). **BUG#39 operator overloading** (RFC lớn, mở khóa bignum) + const generics (`bignum[N]`). Tất cả latent (compiler không dùng) → fixpoint an toàn trừ BUG#36. Spec ở rfcs/0006 + knowledge/bugs.md BUG#33/34/35/36. Bug số học PHÁT HIỆN qua tests/arith/ (matrix 232 ca: float casts/ops/mixed HỎNG HẾT = BUG#33; int in-range + int↔int cast PASS). Quyết định user: toán hạng quyết định phép (kiểu khai báo KHÔNG lan xuống); int→float & f32→f64 NGẦM (cvt thật), float→int & f64→f32 phải `as`; overflow = wrap runtime + LỖI nếu tràn hằng; unsigned div phải DIV (BUG#34), narrow op phải mask về width (BUG#36). Test: tests/arith/{arith_gen,matrix_gen}.py + diag/. **Chạy test cần self-link → KHÔNG khi verify đang chạy.**
- **T2 enum + ADT codegen DONE (RFC 0003+0004, BUG#32 FIXED v1, commit 645310c):** `enum` sugar (parser-only, desugar==sum type) + codegen native cho user sum type. Representation: sum value = ptr 8-byte tới heap box [tag i64 @0, payload 8B @8] (size 16, kind6 aggregate). Dùng thẳng sum id cho OP_ALLOC/SET_FIELD/GET_FIELD (field_offset fallback idx*8 → field0@0/field1@8); KHÔNG sửa selector. lower_variant_construct + lower_match ở air_builder.ax. **Option/Result CŨNG `type Option=Some(T)|None` → phân biệt CHỈ bằng TÊN; path tên Ok/Err/Some/None chạy TRƯỚC user-variant path, ĐỪNG reorder.** Fixpoint MỚI = **692ba8e9** (stage3==stage4) giữ. Hạn chế v1: multi-field variant, str payload, generic user sum + match Option/Result (follow-up). Chi tiết: docs/next-step-15-sub-1.md, rfcs/0004, knowledge/bugs.md BUG#32.

- **BUG#40-43 float native pipeline DONE — fixpoint MỚI = 52a6d9f8** (stage3==stage4, commit a784952). 4 bug chặn pure-AXIOM math, đào ra khi viết thử Newton sqrt/Taylor: **#40** ssa_opt eval_binary fold so sánh float như int (`1.41>10.0` sai -O1) → mảng `is_flt` guard fold_func; **#41** `comisd` đọc dst nhưng FCMP thiếu trong `is_two_operand_read` (x86_regalloc) → 2 toán hạng trùng XMM → mọi so sánh strict=false (-O1; -O0 có COPY che); **#42** float RMW (addsd/mulsd...) cũng đọc dst, thiếu liveness → chuỗi `(a+b)*0.5` hỏng → thêm FADD/FSUB/FMUL/FDIV vào is_two_operand_read; **#43** tham số float đọc từ RCX (int reg) thay vì XMM trong **emit_param_prologue** (KHÔNG phải lazy-path select_inst — path đó CHẾT vì param_idx_processed=len; cùng nơi Family C) → dùng abi_float_arg_reg cho f32/f64 (snapshot+store). Repro t_fcmp=15/t_farg=44/t_fret=75; regression 37. So sánh/arith-chuỗi/đối số/return float ĐÚNG -O0&-O1.
- **🔴 BUG#44 CHƯA FIX + TRẦN STAGE0 (BLOCKER QUAN TRỌNG):** float HỎNG qua lời gọi hàm (win64 XMM6-15 callee-saved KHÔNG được prologue lưu — allocator cấp XMM8-15 cho float, callee clobber). Test `let a=3.0; noop(9.0); (a*100) as i32` = 132≠44. Lý do sqrt_newton (gọi fabs trong loop) sai. **FIX đã thiết kế** (save/restore XMM bằng movsd vào frame slot — push/pop không dùng được cho XMM) NHƯNG **bị chặn bởi TRẦN STAGE0**: stage0 (bin/axc.exe C-backend) FAIL compile stage1 khi thêm đủ code — biểu hiện LỖI Ở `read_file_content` (suy generic Result<str,str>.unwrap → type 0). Bisect chính xác: +1 hàm rỗng = FAIL; +~60 dòng (không hàm) = FAIL; chỉ +field struct = PASS. → Trần TỔNG symbol/IR-size, KHÔNG chỉ số hàm. **HỆ QUẢ: không thể thêm hàm top-level / nhiều code vào stage1 qua stage0 nữa.** Phải: nâng giới hạn stage0 TRƯỚC, hoặc viết cực gọn inline (đã thử bitmask inline ~60 dòng vẫn vượt). Đây là blocker cứng cho BUG#44 + mọi mở rộng stage1. Chi tiết knowledge/bugs.md BUG#40-44.
- **Bài học vận hành thêm (2026-06-28):** (1) exit code mask 8-bit (300→44) — dùng so-sánh-trả-giá-trị-nhỏ khi debug, đừng trả số lớn. (2) `axc build` cần subcommand `build` + path Windows (không /tmp). (3) build 1 chương trình nhỏ qua stage1 = ~37s (compile 147 func prelude) — timeout phải ≥60s, đừng nhầm slow với hang. (4) dump-air để xem AIR thật (recv param = `%1=copy %1` self-copy). (5) disasm objdump axiom_temp.obj (obj trung gian) để xác nhận codegen thật (caller vs callee reg).

- **std.math + bignum lib (2026-06-28, commits e790dd3→cdbf30b→73068e6, PURE LIBRARY zero fixpoint risk):** (1) std/math.ax viết lại THUẦN AXIOM bỏ extern libm (sin/cos/tan/exp/ln/log2/10/pow/sqrt/floor/ceil/round/fmod + int helpers); examples/math_pure.ax + bin/t_math exit127. Pattern self-contained né BUG#44 (hàm-lá OK; `ln(x)/LN2` tail-compose OK; pow/tan INLINE). (2) Bignum scale tùy ý: bin/t_bignum(63) U128 fib/fact/pow2/gcd; bin/t_bn256(31) U256 4-limb add+mul+fact(40)>128bit + Q64.64; bin/t_bf128(15) **f128 fixed-point Q64.64** add + 128×128→256 mul (1.5×2=3, .5×.5=.25). BUG#44 KHÔNG ảnh hưởng bignum (GPR/struct, callee-saved GPR được lưu) → compose tự do. f256/u512 = thêm limb; true bigfloat = mantissa bignum + exponent.
- **🔴 `import std.math` CHƯA chạy — import bundler HARDCODE (main_air.ax concatenate_stdlib chỉ đọc 7 file: result/mem.alloc/scheduler/runtime/os/string/io).** `import std.X` user KHÔNG load file tùy ý → math.ax không vào chương trình; std.math.sqrt "chạy" do rơi xuống extern libm; hàm non-libm → SEGFAULT. Thêm math.ax vào bundler = sửa main_air.ax → đụng TRẦN STAGE0 + đổi fixpoint. TẠM: dùng math IN-FILE. Bug phụ phát hiện: u64 `%`// sai ở -O0 + trong loop (i64 OK); so u64 literal ~19 chữ số lệch.
- **TRẦN STAGE0 đặc tả thêm:** stage0 = compiler GO (./cmd/axc, ./compiler/sema 23 file). KHÔNG phải u16 overflow (intern.go dùng uint32 + panic-guard; Monomorphizer uint32 + string cache key — đều robust). Là bug generic-inference order/type-id-shift trong sema (mono.go/inference.go): khi stage1 thêm symbol, type-id dịch → `read_file_content` r2.unwrap() (Result<str,str>) suy ra type 0. Cliff sắc "thêm 1 hàm = fail" → nghi off-by-one/collision theo symbol-count. Fix cần debug Go sema sâu (phiên riêng). Đây là KEYSTONE mở khóa BUG#44 + std.math bundling + mọi mở rộng stage1.

- **PHIÊN 2 (2026-06-28) — dynamic bignum + std.math +41 hàm (commits 99cd609, a56cc42, +bugs docs):** ⚠️ **COMPILER ĐỂ DÙNG = bin/axc_stage1.exe** (KHÔNG phải bin/axc.exe). Đã `go build` mới: stage0 Go THỰC SỰ thiếu method(`self,o`)/operator-overload + bug literal u64 → các tính năng RFC 0007 CHỈ ở stage1. Build mọi test/example bằng axc_stage1. (1) **std/bignum.ax + std/xmath.ax (commit 99cd609):** bignum ĐỘNG — heap `ptr[u64]` limbs + count n (width=n*64 chọn RUNTIME), default 128 (n=2)→u4096(n=64)→tùy ý; unsigned+signed(two's-comp bi_*)+fixed-point(bf_*); ops add/sub/mul(trunc+full)/cmp/shl/shr/bitlen + xmath pow/factorial/fib/gcd-binary/isqrt/log2_floor/is_perfect_square. **Fixed array `[u64;N]` HỎNG** (codegen→ax_slice_void) → bắt buộc heap. examples/bignum_dynamic.ax + bin/t_bndyn.ax exit99 @O0/O1, trong regression (42 pass). (2) **std/math.ax +41 hàm (commit a56cc42):** trig-inverse(atan/asin/acos/atan2)/hyperbolic(sinh..atanh)/cbrt/hypot/exp2/exp10/log1p/lerp/smoothstep/sigmoid/relu/.../bit-math(popcount/clz/ctz/rotate/bit_width)/mul_hi + const LOG2E/LOG10E/SQRT1_2/EPSILON. bin/t_mathx.ax exit28. (3) **BUG#44 PATTERN rõ:** `CONST op f(call)` HỎNG (const giữ qua call); `f(call) op CONST`/`let r=f(call);..r` AN TOÀN → quy tắc lib trong header math.ax (acos/atanh viết lại theo). (4) **CODEGEN BUG MỚI chặn number-theory bignum:** in-place `q.limbs[i]+=x` qua loop KHÔNG persist (→ build kết quả bằng reassign struct mỗi vòng); struct-return field-bind-rồi-mutate / struct aliasing trong loop → segfault/0 (chặn divmod-in-loop: modpow/lcm-Euclid/binomial — divmod standalone ĐÚNG, để prototype scratch). Footgun: hàm tên `close`→link libc close(fd)→segfault. (5) **Roadmap toán user (36 nhóm ~300 hàm):** float-compose diện rộng (trig/hyperbolic/special/complex/quaternion) GATED bởi BUG#44; bignum number-theory (modpow/primality/mod_inverse) GATED bởi codegen struct-loop bug; cấu trúc (Vec/Mat/FFT/stats/random) cần infra struct/array — phân pha, ưu tiên giải KEYSTONE (BUG#44 + stage0 ceiling) trước.

- **🔓 KEYSTONE GIẢI (2026-06-29, commit 23188e5, fixpoint MỚI = f8b1b97f):** "Trần stage0" KHÔNG phải giới hạn size — tái hiện: +40 hàm tầm thường build OK, nhưng +1 STRUCT hoặc +1 hàm dùng Result/.unwrap() → fail tại read_file_content `return content`. Root-cause (debug printf trong compiler/sema/inference.go): biến `let content = r2.unwrap()` (let-bound generic-method-call) đọc type=0 ở pass CHECK (check_stmt TypeOf) khi type-table lớn, dù inference tính đúng str(12) — **bug multi-pass type-inference Go stage0**. FIX 1 dòng tại main_air.ax: `return r2.unwrap()` trực tiếp (bỏ biến trung gian; call ở return-position cache đúng). CHỨNG MINH: concat-fixed + struct + Result-fn build OK qua stage0 (trước fail). **Fixpoint BẢO TOÀN: stage3==stage4=f8b1b97f, regression 43/43.** ⇒ stage1 lại mở rộng được → mở khóa BUG#44 + import-bundler + mọi growth. Quy tắc source stage1: tránh `let x=<generic-call>` rồi dùng x ở chỗ cần type chính xác; trả/dùng call trực tiếp. (Root fix Go sema = ổn định multi-pass inference let-bound generic-call — để sau, workaround đủ.) ⚠️ verify hiện ~3h (stage1→stage2 chậm hơn baseline 2h do tải máy). **KẾ TIẾP:** BUG#44 (XMM save/restore, design ở bugs.md) HOẶC import-bundler on-demand (parse `import std.X`→load std/X.ax, hết "khóa chết std", dùng được std.math/bignum/xmath đã build) — cả hai giờ unblocked, mỗi cái cần 1 verify ~3h.

- **🟢 BUG#44 FIXED (2026-06-29, commit 670a23e, fixpoint MỚI = fca6d684):** Sau khi nâng trần stage0, land được fix. Allocator cấp XMM8-15 (callee-saved win64) cho float vreg nhưng prologue chỉ lưu GPR → float qua call hỏng. FIX 3 chỗ x86_regalloc.ax ("treat XMM như GPR push"): (1) get_used_callee_saved cover phys 0-31 + mark XMM callee-saved (>=XMM6) trả về SAU GPR cùng mảng; (2) emit_prologue: XMM → `sub rsp,8`+`movsd [rbp-(i+1)*8],xmm` (rbp-relative, reuse spill encoding) thay PUSH; (3) emit_epilogue ngược lại. **LOW-RISK vì XMM đếm trong callee_saved_len → mọi offset (spill/str-lit/pushed_bytes) tự đúng, KHÔNG đổi total_size/StackFrame/insert_spill/emitter/call-site.** Verified: repro 44/54/155 (trước hỏng), regression 43/43 (+bin/t_b44.ax=54), fixpoint stage3==stage4=fca6d684. **HỆ QUẢ: float COMPOSE qua call an toàn → pattern `CONST op f(call)` (acos/atanh) hết hỏng → mở khóa thư viện float diện rộng (trig/hyperbolic/special/complex/quaternion) cho task toán ~300 hàm.** std/math.ax leaf-impl hiện tại vẫn đúng (giữ); có thể nới quy tắc + thêm hàm compose. ⚠️ verify ~2h45 (tải máy).
- **CHUỖI FIXPOINT phiên 2026-06-28→29:** ...→f8b1b97f (nâng trần stage0, commit 23188e5)→**fca6d684 (BUG#44, commit 670a23e)**. 2 KEYSTONE đã giải. **CÒN LẠI để hoàn tất task toán lớn:** (a) mở rộng std.math hàm compose (special/erf/gamma/trig đầy đủ) — LIBRARY-ONLY, không cần fixpoint, test trực tiếp bằng axc_stage1; (b) import-bundler on-demand (`import std.X`→load std/X.ax) hết "khóa chết std", dùng được std.math/bignum/xmath — stage1 change, cần 1 verify ~3h; (c) cấu trúc Vec/Mat/Complex/Quaternion/FFT/stats/random (cần infra struct/array). Mỗi library-only batch KHÔNG cần verify → ưu tiên để tránh ngủ đông.

- **std.math compose unblocked (2026-06-29, commit aaabbd8):** Sau BUG#44, hàm float gọi hàm float qua frame an toàn. Đã CHỨNG MINH: `acos=HALF_PI-asin(x)` + `atanh=0.5*ln(...)` (pattern CONST-op-call cũ hỏng) giờ đúng (exit 3). Thêm **erf/erfc** (Abramowitz-Stegun 7.1.26, ~1e-7) vào std/math.ax — bin/t_erf.ax exit15 (O0/O1), library-only KHÔNG cần fixpoint. **TODO precision:** tgamma/lgamma (Lanczos g=7) viết xong + đúng VỀ TỈ LỆ (beta(2,3)=0.0833 đúng) nhưng lệch ~0.3% tuyệt đối (tgamma(5)≈23.94 thay 24) — do pow/exp Taylor; cần pow chính xác hơn hoặc lgamma tính trực tiếp (không qua tgamma) trước khi ship. Special funcs ở scratch sfb.ax. **Footgun tên hàm MỚI:** `ok` (như `close`) → collision builtin/Result → exit 127; tránh đặt hàm tên `ok`/`close`/libc-names. **CÒN LẠI task toán:** tgamma precision; trig/hyperbolic đầy đủ (đã có, compose được); complex/quaternion/Vec/Mat (cần struct, BUG#44-immune cho int, float-fields giờ OK); import-bundler on-demand (stage1 change, 1 verify ~3h). Library-only batches KHÔNG cần verify → ưu tiên.

- **THƯ VIỆN TOÁN mở rộng (2026-06-29, tất cả LIBRARY-ONLY zero-fixpoint, sau BUG#44):** commits aaabbd8(erf/erfc)→6aa2436(gamma)→801b063(complex)→b00f170(vec). (1) **std.math** thêm erf/erfc (A-S 7.1.26) + **gamma family** tgamma/lgamma/beta qua **log-form Lanczos g=7 (BỎ pow → chính xác**; pow-form lệch 0.3%). (2) **std/complex.ax** Complex f64 (add/sub/mul operator-overload + c_div/conj/abs/norm/arg/exp/ln/sqrt/from_polar). (3) **std/vec.ax** Vec2/Vec3 (add/sub operator + dot/cross/len/normalize/scale/dist/lerp). Repro: bin/t_erf(15)/t_gamma(63)/t_complex(63)/t_vec(63), **regression 46→ (thêm t_complex/t_vec) PASS**. Mọi module compose std.math (sqrt/exp/ln/sin/cos/atan2) qua call — an toàn nhờ BUG#44. **Pattern verify library-only:** test self-contained (own helpers) cho repro + bundle (cat std/math.ax+module+main) cho composition; KHÔNG cần fixpoint (compiler không import std.*). ⚠️ build std.math+module ~90s (lớn); ⚠️ KHÔNG build khi regression đang chạy (đè axiom_temp.obj). **CÒN LẠI task toán:** Quaternion (category 23, như complex), Mat3/Mat4 (cần nhiều field, struct lớn), statistics (mean/var/stddev — cần array/ptr), số học (combinatorics chính xác qua lgamma), import-bundler on-demand (stage1, 1 verify). Tiếp tục library batches (không verify) để tránh ngủ đông.

- **🔴 BUG#45 (2026-06-29, documented, CHƯA fix):** struct 32-byte (4×f64) miscompile nhiều kịch bản (method self+f64-struct-arg đọc field→0; struct call-result làm arg; constructor field=call-result+sret). U256(4×i64) OK, Complex(16B)/Vec3(24B f64) OK → đặc thù >16B + f64. CHẶN Quaternion/Mat3/Mat4. Quaternion ĐÃ HOÃN (không ship module hỏng; algebra free-fn q_mul/q_conj/q_norm chạy nhưng q_from_axis_angle/rotate hỏng). Fix = backend struct-ABI cho >16B f64 (họ Family C + float), cần phiên riêng + fixpoint. Workaround: giữ struct toán ≤24B (≤3 f64). Là KEYSTONE backend tiếp theo (như BUG#44) để mở khóa quaternion/matrix.
- **std.stats (commit 939029f):** sum/mean/variance/stddev/min/max trên ptr[f64]+len (scalar, BUG#45-immune). bin/t_stats exit63. variance inline mean để né **BUG#46**.
- **🔴 BUG#46 (documented):** `let m=f()` (float call-result) đọc qua VÒNG LẶP sau đó (loop không call) → m hỏng (variance sai); inline giá trị thì đúng. Liveness/regalloc float-call-result sống qua loop bị sai (khác BUG#44). Workaround: đừng giữ float-call-result qua loop. Cần debug x86_regalloc liveness float.
- **CHIẾN LƯỢC library tiếp:** module BUG#45/46-immune: scalar special-fn thêm, number-theory bignum (né divmod-in-loop), ≤24B struct. Quaternion/Matrix chờ BUG#45; tránh float-call-result-qua-loop (BUG#46). Library-only KHÔNG cần fixpoint.
- **2 KEYSTONE backend tiếp theo (như BUG#44 đã giải):** BUG#45 (>16B f64 struct → quaternion/matrix) + BUG#46 (float-call-result-qua-loop) — mỗi cái cần debug x86 backend + fixpoint verify ~3h. Giải xong → float library hết hạn chế.
- **THƯ VIỆN TOÁN phiên 2026-06-29 TỔNG (8 commit library, tất cả zero-fixpoint):** std.bignum/xmath (u/i/f→4096), std.math (41 scalar + erf/erfc + gamma/lgamma/beta), std.complex, std.vec (Vec2/3), std.stats. Repro regression: t_bndyn/t_mathx/t_b44/t_erf/t_gamma/t_complex/t_vec/t_stats. Pattern: self-contained repro + bundle-verify; KHÔNG cần fixpoint (compiler không import std.*).

- **🟢 BUG#47 FIXED (2026-06-29, commit e31c490, fixpoint MỚI = 5c398609 stage3==stage4):** unsigned `%`/`/` trong loop làm hỏng giá trị sống qua phép chia. Root: span-clobber pass (x86_regalloc.ax ~L428) cấm RAX/RDX cho vreg bắc qua phép chia CHỈ quét MACH_IDIV (signed); đường unsigned phát MACH_DIV → bỏ sót → giá trị sống (vd dividend `n`) ở RDX bị `mov rdx,0;div` xoá. Triệu chứng: trial-division trả HẰNG (`return 0/false`) → hợp số (91,25) thành nguyên tố; trả biến `i` thì che lỗi. Lặp ở O0+O1 (tất định, không phải opt). FIX: span pass quét cả MACH_DIV (ngặt hơn, không đổi ngữ nghĩa). Ship **std/numtheory.ax** (gcd/lcm/is_prime/next_prime/mod_exp/totient/divisor_count/sum/is_perfect — integer, BUG#44/45/46-immune) + bin/t_numtheory exit127 (regression guard). Cùng họ BUG#34.1. ⚠️ **Vận hành mới:** Monitor heartbeat-10m fire NGAY khi verify đã xong từ trước (verify ~2h45 chạy nền trong lúc làm việc khác) → tốt, commit ngay.
- **📌 bignum vs CTGC (để xử lý sau, [[bignum-ctgc-conflict]]):** std.bignum dùng `@alloc`+raw ptr, KHÔNG free → đứng ngoài CTGC, RÒ RỈ; copy-by-value struct ôm con trỏ sở hữu nghịch single-owner. Không phá luật (raw ptr là tầng thủ công hợp lệ) nhưng nghịch tinh thần. Hướng: owned-type-có-destructor (cần RFC drop-glue cho field con trỏ — chưa có) / một-khối-@alloc / bu_free+arena tạm.
- **LIBRARY BATCH tự động 2026-06-29 (sau BUG#47, zero-fixpoint):** chuỗi auto-chain module BUG#45/46-immune, mỗi cái test self-contained O0+O1=exit đúng + repro + regression row + commit/push: std.numtheory (e31c490, gcd/lcm/is_prime/next_prime/mod_exp/totient/divisor — kèm BUG#47 fix) → std.combinatorics (0b6a596, factorial/perm/binom/catalan/multichoose, integer) → std.interpolation (8fd6434, unlerp/remap/smootherstep/catmull/bezier/bilerp/easing, scalar) → std.geometry (7f12deb, 2D scalar dist/cross/dot/triangle/orient/point-in-tri) → std.probability (ad73c6a, normal/logistic/exp pdf+cdf, compose erf/exp/sqrt). Vận hành: Monitor heartbeat 10' (git HEAD+repro count) + ScheduleWakeup 600s drive chuỗi, không ngủ đông. Còn lại immune: color, coordinate-systems, number-theory mở rộng (modinv/ext-euclid), special-fn. Hết immune → keystone BUG#45. **📋 SCOPE ĐẦY ĐỦ user yêu cầu (~300 hàm/36 nhóm) = docs/MATH_LIBRARY.md (commit d3d9e03)** — bản đồ coverage tổng (✅/🔶/⬜/🔒) + work queue; user nhắc "đưa đầy đủ" → bám doc này, mỗi batch cập nhật status. Blocked: Matrix#21/Quaternion#23/Vec4#36 (BUG#45), numerical-analysis generic#25 (closures), SIMD#32/intrinsics#35 (HW).
- **KIỂM CHỨNG TOÁN CẨN THẬN (2026-06-29, commit 4e6d6ba, [[feedback-math-verify]]):** dựng tests/mathlib (oracle Python `math`): gen_verify.py (sweep lưới → validator bundle) + accuracy_probe.ax + run.sh (gate nhanh, band oracle, check ở VỊ TRÍ ĐỐI SỐ → exit 127). **KẾT QUẢ: std.math sqrt/sin/exp/erf/asin/cbrt/tgamma CHÍNH XÁC ~1e-5** (công thức đúng). **PHÁT HIỆN BUG#48** (documented, chưa fix): giữ ≥8 float-call-result trong local ĐỒNG THỜI → giá trị spill (vd `a`) hỏng (R11/GPR-mov 8-byte cho dest float spill thay vì XMM+movsd; reload đã đúng, store chưa; nghi is_float_vreg không nhận vreg bắt return XMM0). N≤5 OK. Cùng họ BUG#46. HẸP (thư viện không dính). Fix thử (XMM2+movsd) chưa đủ → ĐÃ REVERT giữ stage1 mốc xanh 5c398609. Keystone backend float (gộp BUG#45/#46/#48) cho phiên chuyên sâu + fixpoint ~3h. **Bài học: verify toán bằng probe đối-số-trực-tiếp; tránh ≥8 float-local đồng thời; tránh main khổng lồ (spill-all path chậm/treo).**
- **BATCH oracle-verified (2026-06-29, sau chuẩn verify): combinatorics(0b6a596)→interpolation(8fd6434)→geometry(7f12deb)→probability(ad73c6a)→numtheory2(72c577d)→coordinates(f61eada)→color(6c25ebb)→stats-ext(0278ec8)→signal(525941a)→ml(c25736a).** Mỗi module: oracle Python tính ref → test self-contained check ĐỐI-SỐ → O0+O1=127 → ship + bin/t_* + regression row + cập nhật docs/MATH_LIBRARY.md. **BUG#48 LỘ Ở CODE THẬT (-O1):** st_correlation (5 float-accum) + ml_bce (2 ln-result) HỎNG -O1, đúng -O0 → sửa bằng **helper-call / vị trí đối số** (giảm float sống đồng thời) → O1 xanh. ⇒ quy tắc: hàm float-nặng phải tách helper, đừng giữ ≥~5 float-result đồng thời. MATH_LIBRARY.md nhóm ✅: 1-19,22,24(part),27,28,29,15,26(part),30(part),34(part). Còn ⬜ immune: math-fillins(expm1/log_base/normalize_angle/copysign), bit-fillins, special-fn(digamma/bessel), crypto(miller-rabin), numerical(trapezoid/simpson). 🔒 chờ BUG#45: matrix/quaternion/vec4; chờ BUG#48-fix: float-heavy an toàn hơn.
- **BATCH oracle-verified phần 2 (2026-06-29): math-fillins(8e58758: expm1/log_base/normalize_angle/copysign/round_to)→bit-fillins(66909d5: rotate_right/reverse_bits/bit_floor/bit_ceil/parity)→miller-rabin(fed262b: nt_is_probable_prime det<2^32, bắt Carmichael).** MATH_LIBRARY.md ✅: nhóm 1-11,13,15-19,22,27,28,29,31,34,35; còn ⬜ immune: special-fn(digamma/bessel_j0), numerical(trapezoid/simpson/bisection). Bài học verify: test `lnf` naive (atanh series) hội tụ chậm x lớn → log_base sai → phải range-reduction (std.math.ln ĐÃ đúng). Regression 53. **Khi hết ⬜ immune → keystone BUG#45 (matrix/quaternion/vec4) + BUG#48 (float-heavy) cần fixpoint ~3h.**
- **🏁 HẾT ⬜ IMMUNE QUEUE (2026-06-29): special-fn(f7d36b3: factorial_real/digamma/bessel_j0 vs scipy)→numerical(6f9fe69: trapezoid/simpson/diff sampled).** TỔNG thư viện toán: 17 module (math/xmath/stats/random/vec/complex/numtheory/combinatorics/interpolation/geometry/probability/coordinates/color/signal/ml/numerical/bignum), regression 55, tất cả oracle-verified O0+O1. MATH_LIBRARY.md: ✅ 1-11,13,15-19,22,24,27,28,29,31,34,35; 🔶 14,20,25,26,30,33,36 (còn fill-in immune: random gaussian/next_range_f64, geometry 3D/circle/line, color HSL, decimal); 🔒 21,23(BUG#45 matrix/quaternion), 36-vec4(BUG#45), 32(SIMD HW), 25-generic(closures). **KEYSTONE còn lại = BUG#45 (struct>16B f64 → matrix/quaternion/vec4) + BUG#48 (float-heavy spill) — mỗi cái cần debug x86 backend + fixpoint ~3h.** Bài học verify: tự bắt được ref sai (central-diff x³=3.0625 not 3.015625), lnf naive sai x lớn → range-reduction; BUG#48 lộ ở O1 với float-heavy → tách helper.
- **FILL-IN immune (2026-06-29 tiếp): geometry-ext(93a1cde: circle/polygon-area/segment-dist #20✅)→color-HSL(fabd61d: rgb↔hsl #30✅).** Regression 57. Còn fill-in immune: random gaussian/next_range_f64 (#14 — verify qua Box-Muller transform tất định), decimal (#33). SAU ĐÓ chỉ còn 🔒 keystone: #21 matrix/#23 quaternion/#36 vec4 (BUG#45), #25 generic (closures), #32 SIMD (HW) + BUG#48 (float-heavy). MATH_LIBRARY.md ✅: 1-11,13,15-20,22,24,27,28,29,30,31,34,35.
- **🏁🏁 HOÀN TẤT TOÀN BỘ FILL-IN IMMUNE (2026-06-29): rng(5be5cee #14✅ xorshift64 working — random.ax cũ là aspirational)→decimal(2e60a42 #33✅).** TỔNG: 49 std module, 59 regression repro, MATH_LIBRARY.md = 29✅/4🔶/3🔒. **MỌI nhóm làm-được-không-sửa-backend ĐÃ XONG, oracle-verified O0+O1.** Loop tự động DỪNG (TaskStop heartbeat monitor b2b4397v2). **CÒN LẠI 100% là keystone cần backend+fixpoint ~3h, ĐÃ HỎI USER chọn (không tự chạy verify):** (A) BUG#45 struct>16B-f64 → mở khóa Matrix#21/Quaternion#23/Vec4#36; (B) BUG#48 float-spill áp lực cao → float-heavy an toàn mọi -O; (C) closures #38.7 → numerical/SIMD tổng quát; #32 SIMD cần HW. Đề xuất ưu tiên A (giá trị cao nhất: 3 nhóm) rồi B.
- **🔓 MỞ KHÓA #21/#23/#36 QUA WORKAROUND (2026-06-29, KHÔNG sửa backend): matrix(f3b8b2a Mat3/Mat4 flat ptr[f64] — array-based hoàn toàn immune)→quaternion(581a04a core algebra free-fn)→vec4(b1b6250 free-fn).** MATH_LIBRARY.md: 31✅/4🔶/1🔒 (chỉ còn #32 SIMD khóa HW). 🔶 nhỏ: #12 ieee_remainder, #23 quaternion rotation, #25 generic-f (closures), #26 FFT. Regression 62.
- **🔬 BUG#45 THU HẸP CHÍNH XÁC (root-cause, CHƯA sửa backend):** repro tối thiểu 4×f64 struct construct/sret-return/call-arg/field-read ĐỀU CHẠY. FAIL chỉ khi: (1) **METHOD trả struct** (`fn mul(self,o)->Quat`) — free-fn tương đương CHẠY (AIR + signature GIỐNG HỆT → bug ở CALL-SITE method receiver + aggregate-return, không phải callee); (2) **field constructor = float-call-result** ở -O1 (q_from_axis_angle: `Quat(w: cosf(h),...)`); (3) **chuỗi struct-return call** ở -O1 (q_rotate). Cả (2)(3) cùng họ float/aggregate-liveness -O1 (BUG#48). Workaround: FREE-FN thuần arithmetic (không method, không float-call-in-constructor, không chuỗi struct-call) → O0+O1 xanh. **Fix backend thật (method-call+sret ABI + float-spill dest) = keystone cần phiên riêng + fixpoint ~3h; KHÔNG làm ở context khổng lồ này để tránh compiler nửa-vời chưa verify.** dump-air: `bin/axc_stage1.exe dump-air f.ax -O0`.
- **🟢 BUG#48 FIXED + BUG#45 case(2)(3) FIXED (2026-07-01, commit 476b352, fixpoint MỚI stage3==stage4 = fa35176b, x86_regalloc.ax `is_float_vreg`):** ROOT-CAUSE THẬT (khác giả thuyết store-path cũ): AIR CALL overload `type_id` — với DIRECT call `type_id` = **symbol-index của callee**, KHÔNG phải kiểu trả về. `is_float_vreg` cũ chỉ check `type_id==9/10` → với float-returning call trả false → vreg kết quả bị đẩy vào **GPR pool** thay vì XMM. Copy (movq) thì bit-preserve OK nên che lỗi, nhưng khi dùng trong `addsd/mulsd`, encoder mã hoá operand theo **hardware-index** (vd rsi index 6) → đọc nhầm XMM6 (register-file sai) → rác. Fix: `is_float_vreg(fn_ptr, vreg, table, symbols)` resolve callee return type (mirror `get_register_type`/`regalloc_is_16byte`) + xử lý param float. 4 call-site đều có sẵn table/symbols. Repro: bin/t_fspill(78, ≥8 float-call-result), bin/t_fctor(40, float-call trong constructor -O1), bin/t_ffree(110, free-fn sret control) — thêm vào regression_repros.sh. Disassembly xác nhận: trước fix `movq rsi,xmm0` (def) rồi `addsd xmm8,xmm6` (dùng) = mismatch. Verify: rebuild stage1 (scripts/rebuild_stage1.ps1) → fspill O1 13→78, fctor O1→40.
- **🔧 BUG#45 case(1) method-trả-struct — FIX VIẾT XONG + verify logic (2026-07-01, CHƯA commit, vướng 2 blocker):** Fix = typecheck.ax: helper `method_ret_type(self, field_name_id, rec_type)` (scan SYM_FUNC khớp `match_method_name` + first_param==unwrapped_rec, KHÔNG pre_infer/không mutate node — chỉ trả return type) + block ở NODE_CALL_EXPR (khi result UNKNOWN + callee FIELD_EXPR + method trả AGGREGATE → set result_type). Root: `resolve_method_sym` fallback (typecheck L558) CHỈ match method GENERIC → method thường trả 0 cho MỌI method; scalar chịu được (RAX + copy 8B), struct-return → copy size 0 → zero. **Verify logic ĐÚNG: fmeth(V4 32B)=110, fsmall(P2 16B)=37, ffree=110; native-built fixpoint stage2==3==4 TRUE fixpoint (SHA 8671c919).** ⚠️ **BLOCKER 1 = TRẦN STAGE0:** +~55 dòng đẩy qua ngưỡng infer của stage0/axc.exe (Go, `compiler/sema/inference.go` ~L955-1094 generic-method-call → type 0 khi type-table lớn) → `return r2.unwrap()` (main_air read_file_content) mis-type → axc.exe KHÔNG build stage1 nữa. Phải build stage1 qua compiler NATIVE (axc_stage4). ⚠️ **BLOCKER 2 = t_mathx native crash:** compiler NATIVE-built (method-fixed) **segfault khi build bin/t_mathx.ax** (O0+O1); bản C-backend-built build t_mathx=28 OK. t_mathx KHÔNG có method call → code tôi không chạy cho nó → nghi bug NATIVE-BACKEND tiềm ẩn (main khổng lồ spill) bị dịch chuyển/lộ do +code, có thể PRE-EXISTING (đang chạy isolation: native-clean có crash t_mathx không). Nếu fix được trần stage0 → build method-fixed stage1 qua C-backend → t_mathx=28 + regression pass. **ĐÃ BẢO TOÀN: branch `feat/method-return-struct-wip` (commit c0aa0c1, pushed origin) chứa full method fix + concat + bin/fmeth.ax(110)/bin/fsmall.ax(37). MAIN giữ nguyên mốc BUG#48 476b352 (verified: t_fspill=78/t_fctor=40/t_mathx=28/t_method=24), KHÔNG merge method fix cho tới khi giải trần stage0 (nếu không rebuild_stage1.ps1 qua axc.exe sẽ fail).** ĐƯỜNG SHIP: fix compiler/sema (generic-method infer→0 khi type-table lớn) → axc.exe build lại được method-fixed stage1 → C-backend build t_mathx=28 → regression pass → merge branch. Isolation native-clean build KHÔNG hoàn thành (timeout 30' — native cold build treo, đúng "main khổng lồ spill-all").
- **🔬 TRẦN STAGE0 — ROOT-CAUSE TRUY SÂU (2026-07-01, chưa fix, dọn debug):** Reproduce nhanh: `go build -o /tmp/axc_dbg.exe ./cmd/axc` (~10s) rồi `/tmp/axc_dbg.exe build /tmp/concat_mf.ax -o x -O1` (concat_mf = `git show feat/method-return-struct-wip:bootstrap/stage1/tmp_concatenated_air.ax`). Lỗi: `return type mismatch: expected 12, found 0` tại `return r2.unwrap()` (read_file_content). TRACE (inference.go NodeFieldExpr L786-1094): node `r2.unwrap` → `objType` sau pointer-unwrap = **354 kind=1 (KindStruct, monomorphized Result — KHÔNG phải KindGenericInst)** → L898 `if entry.Kind==KindGenericInst` FALSE → genericArgs=[]; L940 KindStruct → StructInfo(354).GenericParams=[] → genericParams=[]. First method block (L977) `substitutedRet=SubstituteGenericType(method.Return=7, [], [])` = 7 (KHÔNG substitute vì rỗng) → resultType=RegisterFunction(params,7)=func 3291 Return=7. NodeCallExpr: funcReturn=7. Nhưng error='found 0' (7→0 downstream, hoặc 2-pass — chưa chốt). **GỐC: r2's type resolve thành plain struct 354 (mất generic-args) thay vì Result generic-inst → unwrap generic-return không substitute.** getMethodsOfStruct(interfaces.go L95-231) trả unwrap Return=7 sai cho monomorphized struct. Bug ở monomorphization/generic-inst resolution khi type-table lớn. Debug technique: fmt.Println (KHÔNG dùng `\n` — sed phá escaping; và `is_verbose_debug` nuốt fmt bắt đầu `[T`/`[M`/`[C`/`[D` ở .ax nhưng Go fmt.Printf thì in thẳng). nodeIdx 124770 (source L24770) ổn định trong 1 build. ⚠️ GenericInstArgs(objType) PANIC nếu objType không phải genericinst.
- **⏳ BUG#45 case(1) method-trả-struct (root-cause cũ, giữ tham chiếu):** `v.add(o)` với method trả struct >16B → return 0 (sai). Callee AIR `fn @7(t22,t22)->t22` GIỐNG HỆT free-fn (chạy đúng); receiver ĐƯỢC prepend (air_builder L1446). Khác biệt DUY NHẤT: `let r = v.add(o)` sinh `%14 = copy %13` **type_id=0 (untyped)** trong khi free-fn cho `%14: t22 = copy` → copy 0 byte + getfld/`iadd` (integer thay vì fadd) → r toàn zero → 0. ⇒ typecheck KHÔNG set `node_types[method_call]` = kiểu return struct (nghi `resolve_method_sym` tại typecheck-time trượt cho case này, để result_type=UNKNOWN; air_builder resolve được qua fallback nên vẫn call nhưng type đã mất). Fix ở typecheck.ax NODE_CALL_EXPR/NODE_FIELD_EXPR (L1599-1604 / L2114-2120) — cần chu kỳ fixpoint riêng, KHÔNG gộp với backend fix. Repro: bin/fmeth.ax (want 110, hiện 0). Lưu ý cú pháp method: `fn add(self,...)` PHẢI indent trong struct block (không phải `fn V4.add`).
- **🟢🔓 TRẦN STAGE0 GIẢI DỨT ĐIỂM (2026-07-02, commit 78a3c2a, pushed main) — ROOT-CAUSE THẬT khác hẳn giả thuyết cũ (monomorphization/generic-inst ở L97 là SAI/lạc hướng — "type" trong debug chỉ là `TypeID.String()` default cho MỌI id≥17, KHÔNG phải generic).** THẬT: trong `inference.go` NodeConstDecl+NodeVarDecl, con trỏ `sym := SymbolAt(idx)` (=`&st.Symbols[idx]`) lấy TRƯỚC `inferNode(initExpr)`. Khi init là generic-method-call (`let file=r.unwrap()`), inferNode → InstantiateFunction append symbol → **`Symbols` slice REALLOC** → con trỏ `sym` STALE (trỏ backing-array cũ) → `sym.TypeID=<inferred>` ghi vào array chết, table sống giữ 0 → `file`=0 → `r2=file.read_all()`=0 → `r2.unwrap()`=0 → `expected 12 found 0`. Realloc chỉ xảy ra khi table đủ lớn (lenSyms≈8539) ⇒ đúng "thêm code=fail". **FIX: ghi TypeID qua INDEX (`ie.symtable.Symbols[symIdx].TypeID=...`) vào slice sống, cả 2 block.** AXIOM stage1 typecheck KHÔNG dính (copy struct by value `let sym=...data[idx]`) → đó là lý do native chain đã tới fixpoint. **Verify: regression 75/75; full self-host HỘI TỤ ĐÚNG fixpoint cũ fa35176b (== main) → fix semantics-neutral, bootstrap an toàn.** Kỹ thuật debug quyết định: STALE_PTR check `sym != &Symbols[idx]` sau inferNode = true. **HỆ QUẢ: axc.exe lại build được stage1 mở rộng tùy ý → trần biến mất vĩnh viễn.**
- **🟢 BUG#45 case(1) method-trả-aggregate SHIPPED (2026-07-02, commit 07c51f0, pushed main, fixpoint MỚI stage3==stage4=ede11f23).** Sau khi nâng trần stage0, land được method fix (typecheck.ax: `method_ret_type` + block NODE_CALL_EXPR set result_type CHỈ cho aggregate return). BLOCKER 1 (trần) GIẢI bằng 78a3c2a; BLOCKER 2 (t_mathx native crash) KHÔNG còn vì build stage1 qua **C-backend axc.exe** (đã fixed) → t_mathx=28 OK. Verify: fmeth=110, fsmall=37, t_method=24, t_mathx=28; regression **77/77**; fixpoint converged ede11f23 (runtime t_param5=A38/t_strip xanh). **Fixpoint ede11f23 KHÁC branch's 8671c919 vì build-path khác (C-backend-via-fixed-axc vs native), CẢ HAI hợp lệ (stage3==stage4 self-reproduce).** Branch `feat/method-return-struct-wip` giờ THỪA (đã vào main) — còn trên origin, xoá được nếu muốn. **MỞ KHÓA: method trả struct >16B (Quaternion/Matrix/Vec4 dạng method thay vì free-fn).**
- **🏁 THƯ VIỆN TOÁN — QUEUE BUG-IMMUNE ĐÓNG HẲN (2026-07-02, sau khi ship stage0-ceiling+method fix).** Batch cuối (library-only, oracle-verified O0+O1, mỗi cái repro+regression+push): PHI+sec/csc/cot (7ac5de2) → ieee_remainder round-half-to-even (e84e615) → zeta Euler-Maclaurin s>1 (1775859) → **quaternion from_axis_angle/rotate (ad2c588 — MỞ KHÓA nhờ BUG#45/#48 đã fix; t_quatrot=8 xác nhận field=cos/sin-call + chained q_mul chạy đúng -O1 trên struct 32B f64)** → **FFT radix-2 Cooley-Tukey std.fft (f368f24, t_fft=18; twiddle recompute per-iter = BUG#46-immune)**. MATH_LIBRARY.md giờ 34/36 ✅. **CHỈ CÒN 2 mục KHÓA CỨNG (không lịch được, cần keystone mới):** #25 generic numerical (root-find/quadrature của f bất kỳ) + higher-order math ⇐ **closures/function-pointers (#38.7) CHƯA implement**; #32/#35 SIMD ⇐ HW vector ABI. Mọi thứ khác XONG. **KẾ TIẾP nếu muốn mở tiếp toán: implement closures (#38.7) = keystone ngôn ngữ/backend.** Regression giờ ~83 repro. Pattern library: KHÔNG cần fixpoint (compiler không import std.*).
Standing: tự fix→commit→push main mỗi bug; báo cáo qua monitor (lọc milestone+error, tránh spam per-func). Sau mọi đổi backend/parser: chạy scripts/regression_repros.sh + verify_bug29_selfhost.sh (fixpoint). Library-only (std/*, examples/) KHÔNG cần fixpoint. ⚠️ verify stage1→stage2 ~2h45 (native spill-all, func nặng cuối chậm); stage2→3/3→4 mỗi cái ~10s. Defender scan exe mới làm codegen chậm ~lúc đầu (không phải hang).
