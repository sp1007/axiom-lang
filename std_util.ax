// Fixture (not a standalone test): the imported half of bin/t_stdprefixmod.ax.
// Its NAME is the whole point -- it begins with the three letters `std` but is not a std
// module. The driver's strip_imports used to test `match_prefix(s, i, "import std")` with
// no separator boundary, so this file's import line was blanked and the user met
// `undefined name 'std_util'` for a module that exists and compiles (B2).
pub fn answer() -> i64:
    return 42
